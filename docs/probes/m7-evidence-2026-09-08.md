# M7 evidence — 2026-09-08

Execution plan §4 "M7 — Signed authorization, sleeves and full lifecycle", exit gate: "P7 acceptance: same strategy code in paper and live with only the `ExecutionAdapter` differing; browser closure has no effect; live arming requires all four conditions; restart recovers open positions; the tiny-live variant binds to a `LIVE_APPROVAL` Release in test. Reconciliation suite (§24.4) green." M7 ran on `S0_SAFE` as the cleared input (M6 feeds it but does not gate it).

Commits: `36894a7` → `HEAD` (state projector, live intent path, release lifecycle + capital attestation + tiny-live variant, staged finality + chain health, manual actions + restart recovery + single-sleeve arming guard, ADR-0009 P2 clearance provenance). Every commit CI green; hosted migrations applied through `20260908002500`.

## Scope items (plan §4 M7)

| Item | Where | Evidence |
| --- | --- | --- |
| Signed, sequenced `RiskStateProjection` | `libs/risk/src/projection/build.ts`, worker role `state-projector` | `build.spec.ts`, `state-projector.spec.ts`, `projection-repo.integration.spec.ts`; hosted sequence advancing every minute |
| Sleeves and lots, lot-scoped exits, reconciliation to physical + custody | `trading.strategy_sleeves`, `trading.position_lots`, `allocateLots`/`applyExit`, reconciliation role | `lots-custody.spec.ts` (conservation, no unnamed lots, same-mint lot of another strategy untouched), `position-monitor.spec.ts` (lot-scoped intents, pro-rata reduce), `positions-repo.integration.spec.ts`, `reconciliation.spec.ts` + `libs/onchain` `engine.spec.ts` (ledger vs chain per mint) |
| Full intent path (cleared cycle → evaluation → signed envelope → mode check → LIVE_APPROVAL grant with step-up and reaction floor → intent → executor) | risk-authorizer `authorizeEntry`, `libs/risk` `approvals/grant.ts`, worker roles `live-entry`/`approvals`, executor `verifyAuthority` | `authorize.spec.ts`, `grant.spec.ts`, `live-approvals.spec.ts`, executor `harness.spec.ts` (authority, duplicate delivery, tamper, mode gate re-read before submit) |
| Tiny-live `S0_SAFE` variant (ADR-0004) | `S0_TINY_LIVE_VERSION_ID`, `s0TinyLiveVersion` | `release-controls.spec.ts` "tiny-live variant binds to a LIVE_APPROVAL Release" (T1 tier, 120 s intent expiry above the 30 s floor, LIVE_APPROVAL eligible, no LIVE_AUTO; DRAFT → PAPER_VALIDATED → ELIGIBLE_LIVE → ARMED through the approvals role; `armingPreconditions` accepts LIVE_APPROVAL only with the ARMED Release, valid ARM attestation, readiness and step-up) |
| Releases + attestations; authorizer verifies the attested digest; §15.9 arming preconditions | `libs/risk/src/release/lifecycle.ts`, `research.release_attestations`, authorizer `release/verify.ts`, session `SET_AUTHORITY` | `lifecycle.spec.ts`, `release-controls.spec.ts`, `verify.spec.ts`, `session.spec.ts`; readiness hook and deployment live capability are false until M8a so every arming fails closed with the missing list recorded |
| Capital attestation ceiling and `CAPITAL_REATTEST_REQUIRED` | `ops.capital_attestations`, projector `capitalAttestation`, authorizer step 5 | `capital-attestation.spec.ts`, `authorize.spec.ts` (ceiling, re-attest), pgTAP 16 |
| Staged confirmed/finalized, REORG_PENDING, RPC divergence, chain health | executor `trackFinality`, `libs/execution/src/chain/{quorum,health}.ts`, worker role `chain-health`, `ops.chain_health` | `finality.spec.ts`, `quorum.spec.ts`, `health.spec.ts`, `chain-health.spec.ts`, `recovery-repo.integration.spec.ts` (`advanceAttemptFinality`), pgTAP 17; hosted: `SOLANA_CHAIN` HEALTHY, lag ~30 slots, single view |
| Restart recovery (§21.3); manual close/reduce/pause; `EXPIRED_BY_LATENCY` / `CHASE_REJECT` recording | worker `runStartupRecovery`, role `manual-actions`, session pause/resume, live-entry expiry, live adapter `REJECTED:CHASE_EXCEEDED` on the attempt row | `recovery.spec.ts`, `recovery-repo.integration.spec.ts` (open position and lot survive; terminal attempts settled; stale authorizations EXPIRED), `manual-actions.spec.ts`, `session.spec.ts`, `live-adapter.spec.ts` |
| ADR-0009 P1 | authorizer `AuthorizationLedger` | `ledger.spec.ts`, `authorize.spec.ts` (second in-flight authorization refused; pending exposure spent) |
| ADR-0009 P2 | `recordClearedTransition`, projector `auditHead`, authorizer step 3b, worker role `audit-checkpoint` | `authorize.spec.ts` "clearance provenance" (missing, rehashed, mismatched, tampered proposal/verdict/cutoff/Release, head behind, chain unverified), `authorizer.spec.ts`, `audit-clearance.integration.spec.ts`, `audit-checkpoint.spec.ts`; hosted: head replicated to the external file and verified each minute |
| ADR-0009 P3 | authorizer reads the session gate and the Release/attestation at authorization; executor re-reads the mode gate before submit | `authorizer.spec.ts` (sticky PAUSED denies `SESSION_NOT_ACTIVE`), `harness.spec.ts` (pause after signing stops submission) |
| `SINGLE_SLEEVE_PER_MINT` at arming (ADR-0007) | approvals role ARM path, `sleeveConflicts` | `release-controls.spec.ts` |

## Exit gate

| Acceptance | Status | Evidence |
| --- | --- | --- |
| Same strategy code in paper and live, only the `ExecutionAdapter` differs | Pass | `libs/execution/src/adapter/parity.ts` scenario table run over the paper adapter (`paper-adapter.spec.ts`) and the live adapter over the fake chain (`live-adapter.spec.ts`); strategies, risk core and the position monitor take an `ExecutionAdapter` and never branch on it |
| Browser closure has no effect | Pass | `libs/wallet-ui` is importable only by `apps/web` (module-boundary lint, `check-artifacts`); attended presence comes from the presence tool, not the browser (`session.spec.ts`); no worker, authorizer or executor role reads browser state |
| Live arming requires all four conditions | Pass | `lifecycle.spec.ts` and `release-controls.spec.ts`: deployment live capability, ARMED Release with a valid admin ARM attestation, Live Readiness verdict, operator step-up; each missing item is named in the refusal; `session.spec.ts` `SET_AUTHORITY` |
| Restart recovers open positions | Pass | `recovery-repo.integration.spec.ts` (an open position and its lot are untouched by recovery while stale intents settle), `recovery.spec.ts` (reconcile before resume; trading roles withheld when reconciliation fails with open exposure), executor `harness.spec.ts` crash-at-every-boundary cases, `finality.spec.ts` restart case; hosted worker restarts log `recovery_complete` with `resume: true` |
| Tiny-live variant binds to a `LIVE_APPROVAL` Release in test | Pass | `release-controls.spec.ts` (see scope table) |
| Reconciliation suite (§24.4) green | Pass with two conditional cases pending | table below |

## §24.4 reconciliation tests

| Case | Test |
| --- | --- |
| DB says open, wallet empty | `libs/onchain` `engine.spec.ts` "a token balance that disagrees with the ledger … MISMATCH that pauses" |
| Wallet holds token, DB has no position | `engine.spec.ts` "an airdropped token account the ledger does not know is UNEXPECTED …" |
| Protective order filled while worker offline | pending: Trigger V2 lifecycle (Probe B, M8a conditional); `MONITORED_EXIT` is the only protection mode until then (ADR-0004) |
| Manual external wallet transfer | `reconciliation.spec.ts` "an inbound token transfer no lifecycle claims is an UNKNOWN movement: MISMATCH, pause" |
| Authorized wallet→registered-vault movement does not false-positive | `reconciliation.spec.ts` "the same transfer inside an authorized lifecycle between registered custody is EXPECTED and clean"; `lots-custody.spec.ts` |
| Unmatched vault/wallet movement pauses trading | `engine.spec.ts` "a movement no authorized lifecycle claims pauses even when balances happen to agree" |
| Strategy lots sum to aggregate physical/custody holdings | `lots-custody.spec.ts` (exits conserve quantity), `positions-repo.integration.spec.ts` (lot, position, sleeve and intent move together); reconciliation compares per-mint position quantity to chain |
| Transaction finalized after service restart | `finality.spec.ts` "restart: a CONFIRMED_PROVISIONAL attempt survives in the journal …"; `harness.spec.ts` "crash after submit but before response persistence" |
| DB outage emergency close reconciles correctly after recovery | `emergency.spec.ts` (no-database close, ledger release, replay refused after restart); journal import into the audit ledger is M8a |
| Same mint held by two strategies, one provider fill reduces only the originating lot | `lots-custody.spec.ts` "a provider fill attributed to one lot leaves a same-mint lot of another strategy untouched" |
| Provider protection mode without lot isolation falls back to `MONITORED_EXIT` | pending with Trigger V2 (M8a conditional); today every lot is `MONITORED_EXIT` |
| DB outage evaluates stops from the latest durable position shadow, chain truth caps the sell | `emergency.spec.ts` "shadow stops (§15.10A)" |
| Stale/corrupt shadow cannot create/increase exposure | `emergency.spec.ts` "shadow stops" (stale shadow refused, exposure-increasing request refused) |
| Confirmed then missing before finalized → REORG_PENDING, no duplicate submission | `finality.spec.ts` "confirmed then missing before finality enters REORG_PENDING …" |
| Independent RPCs disagree → entries pause until reconciliation | `finality.spec.ts` "independent views that contradict each other pause entries until they agree"; `quorum.spec.ts`; `chain-health.spec.ts` divergence |
| Chain/finality stall preserves provisional exposure without promoting P&L | `finality.spec.ts` "stalled finality keeps the provisional exposure and never promotes accounting"; `health.spec.ts` |

## Operator items carried forward

- `SOLANA_RPC_SECONDARY_URL` (worker) and `SOLANA_RPC_SECONDARY` (executor): without a second independent view, cross-RPC divergence cannot be observed (logged as `chain_health_single_view` / `finality_single_view`).
- `AUDIT_CHECKPOINT_PATH` for the risk-authorizer in every live profile; the worker replicates to the same path locally today.
- Deployment live capability is M8a; the Live Readiness verdict now exists (worker role `readiness`, next changelog entry) and arming reads it, so arming and live authority fail closed until the evidence rows are recorded against the Profile 2 binding.
- Adversarial reviews #1 and #2 remain to be run by a fresh session with GUARDRAILS Part 3.
