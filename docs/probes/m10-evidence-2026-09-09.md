# M10 evidence — replay and research framework (2026-09-09)

Execution plan §4 M10, blueprint P9, §18, §19, §30. Author: implementing session (Claude Code); the adversarial review of this milestone belongs to review #4 at M11, by a different session.

## Exit gate (P9 acceptance)

| Acceptance item | Evidence | Status |
| --- | --- | --- |
| A replay test that intentionally attempts future evidence fails | `libs/replay/src/point-in-time/guard.spec.ts` (any read past the clock throws `LookAheadError`, past the cutoff `DatasetCutoffError`; skill sources refused before the repository is read); `apps/worker/src/replay/engine.spec.ts` "a strategy that reaches for evidence after the replay clock fails the run"; `apps/worker/src/agents/guarded-sources.spec.ts` (every live `ContextSources` method mapped, a future `asOf` throws against the wall clock). INV-13 mapped to all five specs. | **Pass** |
| Baseline and AI strategies run against the same timeline | Engine spec: every candidate is decided by S0_RAW, S0_SAFE and the latency-matched baseline, each from an isolated book with identical starting capital. Hosted run `6c3fa605` below: 107 candidates, 7 decided by each stream. | **Pass** |
| Results identify strategy/model/prompt/data versions | `research.replay_runs.versions` on run `6c3fa605`: git SHA `af16fb8d…`, contract-set digest `9e9f0a4d…`, `features-v2`, `risk-v1`, `s0-gate-v1`, `replay-cost-v1`, `trading-skill@1.0.0`, `guide-v1`, provider dataset versions `{birdeye: ohlcv-v3, helius: parsed-events-v1, jupiter: quote-v1}`; every decision and trade row carries `strategy_version_id` and `variant`. | **Pass** |

## What landed

- Contracts: `ReplayRun`, `ReplayDecision`, `ReplayCostModel` (the paper fill policy for fees/slippage/impact/path MEV allowance, plus decision latency, candle availability lag, seeded execution-failure rate), `ReplayRequestPayload`, `ReplayResults`, §11.14 confidence bins, model-weight look-ahead label, control kind `RUN_REPLAY` (FAST).
- `libs/replay`: `SimulatedClock`, look-ahead guard (`guardedRows`, `guardedCandles`, `guardSources`, `liveGuardContext`), `ExecutionFailureSampler`, reproducibility record and digests, §19.1 core metrics, §19.2 attribution over eleven dimensions, §19.3 incremental value, disagreement attribution, latency cost, calibration, strategy comparison with hold-out split, three-layer economic P&L.
- Worker: replay engine (`apps/worker/src/replay`) over the unchanged live pipeline; role `replay` (requests → version-bound queued runs; execution → decisions, trades, results, digests); dataset loaders; `guardedContextSources` around the agents role's skill sources; `tools/replay.mjs`.
- Database: `research.replay_runs` / `replay_decisions` / `replay_trades` (003700, 003800; immutable when done; pgTAP 27) and the §30 views (003900; pgTAP 28), applied locally and on the hosted project.

## Hosted Level B run — `6c3fa605-9ac5-4014-adda-fdf121f6e5e4`

Filed with `node tools/replay.mjs --env deploy/profile-0/.env.worker --name "S0 gate value, 2026-09-08 (Level B)" --from 2026-09-08T00:00:00Z --to 2026-09-09T00:00:00Z --fidelity B_CAPTURED --strategies S0_RAW@1.2.0,S0_SAFE@1.2.0 --baseline S0_RAW@1.2.0 --holdout 2026-09-08T12:00:00Z --seed 7`; queued at 02:11:00 UTC with dataset cutoff 02:10:59, completed 02:13:27 (146 s, 1441 ticks, 107 candidates, 21 decisions, 17 closed trades). Every figure below is read from the §30 views on the hosted project.

Leaderboard (`research.replay_leaderboard`, USDC):

| Strategy | Variant | Sample | Trades | Net P&L | Win rate | Expectancy | Max drawdown |
| --- | --- | --- | --- | --- | --- | --- | --- |
| S0_RAW@1.2.0 | FULL | ALL | 6 | −67.63 | 0.17 | −11.27 | 67.63 |
| S0_RAW@1.2.0 | FULL | IN_SAMPLE | 3 | −26.87 | 0.33 | −8.96 | 32.65 |
| S0_RAW@1.2.0 | FULL | HOLD_OUT | 3 | −40.76 | 0.00 | −13.59 | 40.76 |
| S0_RAW@1.2.0 | LATENCY_MATCHED | ALL | 6 | −70.05 | 0.17 | −11.67 | 70.05 |
| S0_SAFE@1.2.0 | FULL | ALL | 5 | −47.28 | 0.20 | −9.46 | 47.28 |
| S0_SAFE@1.2.0 | FULL | IN_SAMPLE | 3 | −26.87 | 0.33 | −8.96 | 32.65 |
| S0_SAFE@1.2.0 | FULL | HOLD_OUT | 2 | −20.40 | 0.00 | −10.20 | 20.40 |

Gate value (`research.replay_incremental_value`, Q1): of 7 shared candidates the gate filtered 1 loser (baseline −20.35 on it) and rejected 0 winners; 5 traded by both, 1 refused by both (RAW's risk core tripped `CIRCUIT_BREAKER` after consecutive losses); incremental net expectancy +2.91 USDC per candidate before model cost. Verdicts: S0_SAFE REJECTED 2 candidates on `OVEREXTENDED_1H`, both recorded beside RAW's non-blocking review of the same objection.

Latency (`research.replay_latency_cost`, Q17): S0_RAW decided at 0 ms; the latency-matched baseline (30 000 ms, the slowest non-baseline decision budget in the run — S0_SAFE@1.2.0's) realized 2.42 USDC less on the same fills — the edge attributable to speed alone on this day. No expiry, chase or stale-quote losses at S0's speed.

Exits (`research.replay_exit_outcomes`, Q13): every closed trade exited on `HARD_STOP` after 13–18 minutes; no target or trailing exit fired. Economic layers (`research.replay_economic_pnl`, Q19): platform share for the window 1.52 / 1.28 USDC by turnover; both strategies negative at every layer on this day. Attribution rows exist for session, liquidity band and regime with every group flagged `sample_supported = false` (fewer than ten trades), which is the honest reading of one day.

Reading: a single losing day for the momentum baseline with the safety gate removing one of the losers, exactly the counterfactual split §12.1 asks for; nothing here is a performance claim.

## Reproducibility

The first reproduction (`6d80c380`) produced the same 21 decisions and 17 trades but a different digest: synthetic candidate ids derive from the run id, and the digest included them. Fixed in `libs/replay/src/reproducibility/record.ts` (candidate ids excluded; rows ordered by moment, strategy, variant, asset, state and reason codes). Digest check runs `f7bdf054` and `7b33f93d` on the fixed bundle (same request, same seed, same dataset cutoff 02:24:58.908 UTC, executed one after the other):

| Run | Decisions | Trades | Decisions digest | Results digest |
| --- | --- | --- | --- | --- |
| f7bdf054 (02:25:05 → 02:27:35) | 21 | 17 | c4ee5b5e0058dbb1429ad88cd84d65e367f7267f940decf60850ddf0b5ea62db | d6216b92da488b840d3c8f353a27f885ef51a028ab7c6e9e22488e5190f6bf1b |
| 7b33f93d (02:27:35 → 02:30:17) | 21 | 17 | c4ee5b5e0058dbb1429ad88cd84d65e367f7267f940decf60850ddf0b5ea62db | d6216b92da488b840d3c8f353a27f885ef51a028ab7c6e9e22488e5190f6bf1b |

Both digests are identical, and the results digest also equals that of the first run `6c3fa605` (whose decisions digest differed only by the excluded candidate ids). Reproducibility holds at the level §18.5 asks for: same inputs, versions and seed, same decisions.

## Open items carried forward

- §30 Q2, Q21 (catalyst continuation by source-time age) and Q22 (attended vs unattended) need live evidence rows the replay does not carry yet; the views are added when M6 intelligence keys and Profile 1B sessions exist rather than faked now.
- Direct model/data/RPC cost per strategy is 0 in replay results until the D43 spend ledger is joined per strategy version (the three layers are computed; the second layer equals the first for now).
- Level A impact modelling uses the latest eligibility probes; a Level A run over a window without eligibility rows falls back to the cost model's default impact (50 bps) and is labelled A_HISTORICAL throughout.
- M9 step 8 (Strategy Lab, Replay Lab, Trade History, Attribution) renders these views next.
