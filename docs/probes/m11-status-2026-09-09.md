# M11 status and §33 Definition-of-Done evidence (2026-09-09)

Execution plan §4 M11 (adversarial hardening and Live Readiness closure) and blueprint §33. This is a status record, not a closure: M11's exit gate is the §33 checklist with evidence links, and several items are operator work by design (keys, probes, drills in the isolated environment, a review by a different session). Everything key-free that the milestone lists is landed and linked below; what remains is named with its owner.

## M11 deliverables

| Deliverable | State | Evidence |
| --- | --- | --- |
| Every P10 drill automated where possible, exposed via Live Readiness `Run drill` | **Done for the automatable set** | `EXECUTE_READINESS_DRILL`: CRITICAL_ALERT_DELIVERY (worker), DB_DOWN_EMERGENCY_CLOSE and PERSIST_BEFORE_SUBMIT_DRILL (executor drill routes); `apps/worker/src/drills`, `apps/execution-service/src/api/internal.ts`, Live Readiness `Run drill`; specs `drills.spec.ts`, `readiness.spec.ts`, `api.spec.ts`. SIGNER_OUTAGE_DRILL, BREAK_GLASS_SWEEP_DRILL, OUT_OF_BAND_CONTROLS, CREDENTIAL_ISOLATION stay operator-run in the isolated environment (they need the signer, the break-glass principal, the emergency key or the target hosts). |
| Full container/SBOM tooling and transitive forbidden-package policy | **Done** | `tools/check-transitive.mjs` (CycloneDX 1.5 SBOM per deployable, transitive closure policy with chain reporting), CI step with planted negative test and SBOM artifact; `tools/check-artifacts.mjs` (bundle scan, since M8a); egress and digest checks in CI since M8a (`ARTIFACT_EGRESS_DIGEST` row). |
| Terraform for Profile 3/4, applied only at promotion | **Written** | `deploy/terraform/` (module, profile-3, profile-4, README); `validate`/`apply` at promotion (ADR-0002). |
| Invariant map: every applicable invariant mapped and green for the enabled capability set; disabled capabilities NOT_APPLICABLE | **Mechanism done; three rows wait on Turnkey** | `tools/check-invariant-map.mjs` enforces applicability (ADR-0010); 25 of 28 mapped; the three unmapped are LIVE_SIGNING invariants whose owning modules (Turnkey adapter, break-glass verbs) do not exist until Probe A/C. |
| Runbooks: infrastructure loss chain-first, wallet reserve/manual funding, signer outage, executor compromise | **Done** | `docs/runbooks/` (index + four runbooks + recoveryctl skeleton). |
| Adversarial review #4 (full §32 incl. compositional cases), by a fresh session or human | **Operator** | Reviews #1–#4 are run by a different session with `GUARDRAILS.md` Part 3 as the checklist; findings go to `docs/reviews/`. |
| Live Readiness verdicts `READY_FOR_ATTENDED_TINY_LIVE` (re-confirmed), `READY_FOR_UNATTENDED_LIVE_PILOT`, `READY_FOR_HARDENED_LIVE_AUTO` | **Operator** | The verdicts are computed from rows; the rows that remain FAIL/NOT RUN need keys, probes and drills in the isolated environment (Probe A/C, signer rows, out-of-band controls, credential isolation, break-glass). |
| §33 DoD checked with evidence links | **This document** | below. |

## §33 Definition of Done — evidence

| # | Item | State | Evidence |
| --- | --- | --- | --- |
| 1 | Live market/on-chain/news/social pipeline operates continuously; 1B unattended | Partial | Market, on-chain and eligibility pipelines run on the attended P1A paper worker since M4/M5a (`docs/probes/m4-exit-gate-2026-09-07.md`, `m5a-pipeline-evidence-2026-09-08.md`); news/social ingestion is built (M6, `m6-status-2026-09-08.md`) and waits on CryptoPanic/LunarCrush keys; Profile 1B promotion is M12 (operator). |
| 2 | Deterministic eligible universe and candidate engine | Done | M4/M5a evidence; `libs/signals`, `libs/risk/eligibility`. |
| 3 | S0–S4 implemented, speed-tiered, versioned | Done (S1–S4 need model keys to run live) | `libs/strategies`, `research.strategy_versions`, Strategy Lab. |
| 4 | Versioned Trading Skill, guidelines, automations with typed point-in-time tools | Done | M6 (`m6-status-2026-09-08.md`), Autonomy workspace, `guardedContextSources` (M10). |
| 5 | Every discretionary action incl. HOLD passes the adversarial loop; mandatory reductions cannot be LLM-blocked | Done | INV-14/15/16/17 tests, action-cycle machine (ADR-0001), position monitor D31. |
| 6 | Risk policy deterministic, cryptographically authorized, independently tested | Done | `libs/risk`, risk-authorizer envelopes, `RISK_AUTHORIZER_ISOLATION_TAMPER` CI evidence. |
| 7 | Paper and live share lifecycle and strategy code | Done | parity suite (`libs/execution/src/adapter/parity.ts`), same roles for both books. |
| 8 | Jupiter live execution behind the isolated signer | Code done; signer pending | live adapter, executor pipeline, order client (M3/M7); Turnkey adapter after Probe A/C (operator). |
| 9 | Protective exits incl. Trigger lifecycle when enabled, plus provider-independent emergency path | Done for the emergency path; Trigger conditional | M8b direct-pool adapters and dry runs (`m8b-evidence-2026-09-08.md`); `TRIGGER_LIFECYCLE` NOT_APPLICABLE until provider protection is enabled (ADR-0004). |
| 10 | Idempotency/reconciliation survive timeout/restart; durable identity before submission | Done | executor journal, harness specs, `PERSIST_BEFORE_SUBMIT_DRILL` automated audit. |
| 11 | Continuous held-asset safety and exit compatibility | Done | held-asset-safety role (M4/M5a), position safety evaluations. |
| 12 | Full §20 UI/UX | Done | M9 closed 2026-09-09 (plan §8; Playwright suite 27 tests). |
| 13 | Replay enforces first-seen point-in-time semantics across proposer, adversary and all tools | Done | M10 (`m10-evidence-2026-09-09.md`), INV-13. |
| 14 | Baseline-vs-AI, proposer-vs-adversary, latency and economic-cost attribution visible | Done | Replay Lab, Strategy Lab, Attribution / Economics; §30 views. |
| 15 | All critical invariants have automated tests | 25 of 28 | invariant map; three LIVE_SIGNING invariants wait on Turnkey. |
| 16 | Emergency pause/close on mobile; signed out-of-band path without Next.js or Postgres | Done | mobile surface (M9), `traderctl` + executor out-of-band endpoint (M3/M8a), harness tests. |
| 17 | Sleeves and lots reconcile to wallet/custody | Done | reconciliation role, lot-scoped accounting (INV-22), M8a. |
| 18 | Wallet reserve monitoring live, funding manual, no treasury pull | Done | notifications role reserve alerts, `WALLET_RESERVES` row, funding connector (M9), runbook. |
| 19 | Infrastructure-loss recovery documented and exercised from chain/custody truth | Documented; exercise is operator | `docs/runbooks/infrastructure-loss-chain-first-recovery.md`; the exercise records `BREAK_GLASS_SWEEP_DRILL` / `OFFLINE_CARRY` rows from the isolated environment. |
| 20 | Live capability can stay disabled with no missing implementation | Done | profiles P0/P1 `liveCapabilityEnabled=false`; executor guardrails; every live path exists behind readiness. |
| 21 | Adversarial review has no unresolved critical/high defect | Operator | reviews #1 (M3), #2 (M6), #3 (M8a), #4 (M11) by a fresh session; `NO_OPEN_CRITICAL_FINDING` row. |
| 22 | Risk authorization isolated; live Release attestation enforced | Done | risk-authorizer deployable and trust tags; Release attestations, arming review (M9). |
| 23 | PROTECTION_ONLY, spend circuit breakers, shared-cutoff refresh | Done | ADR-0001 machine, D43 spend budgets (M6), D40 cutoff tests. |
| 24 | Direct-pool emergency exit continuously dry-run for LIVE_AUTO assets | Done | `emergency-dry-run` role, `EMERGENCY_DIRECT_POOL` capability rows (M8b). |
| 25 | Step-up security, out-of-app critical alerting/escalation, Live Readiness UI | Done (Telegram channel needs the operator's bot token) | operator-security role, notifications role, `/readiness`, automated `CRITICAL_ALERT_DELIVERY` drill. |
| 26 | Provider protective fills preserve lot attribution | Done in code; Trigger conditional | lot-scoped protection (INV-22), `TRIGGER_LIFECYCLE` when enabled. |
| 27 | Wallet Standard / Solana Kit funding connector with typed signed funding and chain-reconciled events | Done | M9 funding connector (commit 6030803), `policy/funding.ts`, reconciliation FUNDING classification. |
| 28 | External signer-side policy, signer-outage controls, tested revocation/break-glass, pinned cold-recovery | Controls done; signer and break-glass pending Turnkey | D33 cap, signer health rows, `recoveryctl` skeleton, runbooks; Probe A/C and the break-glass verbs are operator work (plan MP, M3). |
| 29 | Risk authorization rejects tampered/stale projections and verifies chain state before exposure increases | Done | authorizer D45/D52 reads, projection tamper tests (`RISK_AUTHORIZER_ISOLATION_TAMPER`). |
| 30 | Capital above the attested ceiling blocks new exposure pending re-attestation | Done | D56 capital attestation (M8a), funding review label, `TINY_ATTESTED_CAPITAL` row. |

## What closes M11

1. Operator: Turnkey Probe A/C in the isolated environment → Turnkey adapter → the three remaining invariants mapped → signer rows green.
2. Operator: out-of-band controls, credential isolation, signer-outage and break-glass drills recorded from the isolated environment; Telegram bot token for the out-of-app channel.
3. A fresh session: adversarial review #4 with `GUARDRAILS.md` Part 3, findings in `docs/reviews/`, no unresolved critical/high finding.
4. Then the three Live Readiness verdicts compute from rows, and the operator ticks M11 with this document as the §33 evidence.
