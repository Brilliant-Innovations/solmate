# M6 status — 2026-09-08

Execution plan §4 "M6 — Intelligence layer and autonomous agent layer", exit gate: "P4 + P5 acceptance. Adversarial review #2 (AI-boundary section of §32). S1–S4 run in PAPER alongside S0 with separate virtual books. Run-rate recorded."

This is a status document, not a gate claim. Every key-free deliverable is built and tested; the items that need operator-provided model and intelligence keys, live data, or a fresh review session are listed at the end and the §8 box stays open until they close.

## P4 deliverables (blueprint §35 "P4 — News/social intelligence")

| Deliverable | Where | Evidence |
| --- | --- | --- |
| LunarCrush adapter | `libs/intelligence/src/providers/lunarcrush.ts` | `providers.spec.ts` (coin metrics → hour-bucketed SOCIAL event with provider time as source time; repeat inside the hour shares one source id; 404 → null; non-200 → provider error) |
| CryptoPanic adapter | `libs/intelligence/src/providers/cryptopanic.ts` | `providers.spec.ts` (posts → raw events with URL, publication time, currencies as symbols, bounded vote sentiment; key redacted from logged URLs; malformed body refused) |
| Normalization / deduplication | `libs/intelligence/src/normalize.ts`, `dedupe.ts` | `intelligence.spec.ts` (two clocks kept apart, URL or wording → DUPLICATE, shared assets + words → CORROBORATION; order-independent property) |
| Source quality | `intel-v1` domain table, `sourceQualityFor` | `intelligence.spec.ts` (quality from the pinned domain table only, never from text) |
| Novelty / first-seen | `assignCluster` novelty, `upsertEvent` (repeat only advances `last_seen_at`), migration 002100 guard | `intelligence-repo.integration.spec.ts`, pgTAP 14 |
| Asset entity matching | `EntityIndex` (mint > cashtag > unique symbol > unambiguous name; provider tickers matched like cashtags) | `intelligence.spec.ts`, `providers.spec.ts` |
| Ingest role | worker `roles/intel-ingest.ts` | `intel-ingest.spec.ts` (syndicated copy → DUPLICATE with novelty < 0.2, unmatched news not stored, repeat → SEEN_AGAIN, one provider's failure isolated) |

## P4 acceptance

| Acceptance | Status | Evidence |
| --- | --- | --- |
| Repeated syndicated story becomes one catalyst cluster | Pass (tests) | `intelligence.spec.ts`, `intel-ingest.spec.ts`; live confirmation waits on a provider key |
| Source / event / first-seen time retained | Pass | migration 002100 (immutable clocks), pgTAP 14, `intelligence-repo.integration.spec.ts` |
| Replay query cannot expose future evidence | Pass | `listEventsVisibleAt` enforces `first_seen_at <= at` in SQL (INV-13); `context-repo` as-of reads; `context.spec.ts` property: nothing first seen after the cutoff reaches a packet or tool response |

## P5 deliverables (blueprint §35 "P5 — Autonomous Trading Skill + adversarial agent layer")

| Deliverable | Where | Evidence |
| --- | --- | --- |
| Model gateway | `libs/agents/src/gateway/` (`ReasoningModel`, Anthropic forced tool call, OpenAI strict JSON schema, `createReasoningModel`) | `gateway.spec.ts` (recorded responses, outage vs timeout, missing key → no model) |
| Versioned Trading Skill and typed tool manifest | `tools-v1`, `libs/skills/src/tool-manifest/`, `tradingSkillVersion` | `tool-manifest.spec.ts` (INV-16 property: any name outside the manifest refused before a handler runs; extra argument keys refused; ids resolved against the cycle scope; hallucinated evidence ids refused) |
| Versioned guidelines | `guide-v1`, `libs/skills/src/guidelines/v1.ts` | `prompt.spec.ts` (all fourteen §11.5 rules pinned) |
| Candidate and open-position automations | `automations-v1`, `libs/agents/src/automations/engine.ts`, `automations-repo` | `engine.spec.ts` (property: never invokes inside cooldown, interval, D39 backoff, in-flight or blocked budget), `automations-repo.integration.spec.ts` |
| Proposer action schema / runtime | `TradingActionProposal` (strict), `runDiscretionaryCycle` | `runner.spec.ts` (malformed, hallucinated, out-of-scope, low-confidence, SHORT, amounts → UNRESOLVED(MALFORMED_OUTPUT)) |
| Mandatory Action Adversary schema / runtime | `AdversarialReviewOutput`, runner adversary step, `AdversarialReview` rows | `runner.spec.ts` (adversary cannot add packet-foreign evidence, review a different cutoff or carry a proposal) |
| Bounded challenge / revision loop | action-cycle machine + runner (one refresh, cutoff v2 shared) | `action-cycle.spec.ts`, `runner.spec.ts` (second CHALLENGE → UNRESOLVED(REVISION_EXHAUSTED); stale v1 proposal under v2 is malformed, INV-19) |
| Prompt-injection / tool boundaries | `libs/skills/src/prompt/render.ts`, tool schemas, scope rules | `prompt.spec.ts` (fixtures and property: no evidence text can open or close a block or start a role line), `tool-manifest.spec.ts` |
| Model / version / cost logging | `AgentRun` per call (provider, model, prompt version, tokens, cost, latency, schema errors), `agents-repo` | `runner.spec.ts`, `agents-repo.integration.spec.ts` |
| Strategy integrations S1–S4 | `llmStrategyVersions`, five-family candidate scan, agents role per strategy | `llm-versions.spec.ts`, `families.spec.ts`, `candidates.spec.ts`, `agents.spec.ts` |
| Speed / latency-tier contracts | per-version `maxDecisionLatencyMs`, `maxCandidateAgeMs`; runner EXPIRED/TIMEOUT endings; tier heartbeats | `runner.spec.ts`, `engine.spec.ts` |
| EVENT_WINDOW proposal → deterministic cap | `EventWindowRequest` on the proposal, `capEventWindow` / `extendEventWindow` | `cap.spec.ts` (property: never past the policy ceiling, never on an old or untrusted catalyst), `agents.spec.ts` |

## P5 acceptance

| Acceptance | Status | Evidence |
| --- | --- | --- |
| Malformed model output fails closed | Pass (tests) | `runner.spec.ts`: every malformed shape ends UNRESOLVED(MALFORMED_OUTPUT) with the schema errors on the run; a position then enters PROTECTION_ONLY (`agents.spec.ts`) |
| Model has no wallet/executor/general HTTP/SQL/shell tool | Pass | `tool-manifest.spec.ts` (manifest pinned to nine read/proposal tools; a manifest naming a forbidden capability is refused at load; INV-16 property); the worker artifact scan carries no signer package |
| Every discretionary exposure action receives adversarial review | Pass (tests) | machine: ENTER/HOLD/REDUCE/EXIT/ADJUST_PROTECTION reach PROPOSED and need ADVERSARY_REVIEWED; IGNORE is the only action that clears without one (`runner.spec.ts`) |
| Adversary can reject/challenge but cannot create or authorize a trade | Pass (tests) | `runner.spec.ts` "the adversary cannot add evidence, review a different cutoff, or upgrade the proposal"; `canAuthorize` requires CONFIRM on the proposer's action at the latest cutoff |
| Hard risk exits do not wait for an LLM | Pass | position monitor executes mandatory exits with a deterministic non-blocking review (`position-monitor.spec.ts`); INV-15/INV-21 bypass scan green |
| Open-position HOLD is recorded as an action cycle | Pass (tests) | `agents.spec.ts` (cleared HOLD persisted with runs, proposal, review; position REVIEWED with the cycle id) |
| Every agent action reproducible from stored records | Pass (structure) | one-transaction persist of cycle + runs + proposals + reviews + tool invocations/refusals (`agents-repo.integration.spec.ts`); live reproduction waits on the first keyed run |

## Not yet met (operator-dependent)

- **Model keys.** `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` in the worker environment turn the `agents` role on; until then it logs `roles_disabled` with the reason on every start. The first keyed S1 paper cycle, its stored runs and its reproduction are the live evidence for the P5 acceptance rows marked "(tests)".
- **Intelligence keys.** `CRYPTOPANIC_API_KEY` / `LUNARCRUSH_API_KEY` turn on `intel-ingest`; S2 (catalyst) and the CATALYST_RESPONSE family need it, S3 (smart money) needs tracked-wallet flow (Helius key is present; wallets must be registered).
- **Run-rate.** Model and provider spend go to `docs/costs.md` after the first keyed day; `AGENT_MODEL_PRICING` must be set so agent runs carry a cost.
- **Adversarial review #2.** A fresh session works the AI-boundary section of GUARDRAILS Part 3 against this code and writes findings to `docs/reviews/`; not this session.
- **Separate virtual books.** S1–S4 have their own sleeves on the paper account (paper-entry registers all four); "run in PAPER alongside S0" is observable only once cycles clear.
- **EVENT_WINDOW activation.** The cap is applied and logged (`agents_event_window`, `event_window_capped`); opening `EVENT_WINDOW` on the runtime session through the session machine lands with the S2 paper run.
