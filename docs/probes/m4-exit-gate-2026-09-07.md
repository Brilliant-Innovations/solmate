# M4 exit-gate evidence — 2026-09-07

Execution plan §4 "M4 — Market data, chain truth and eligibility", exit gate: "P1 + P2 acceptance: live eligible/watch universe populates; live market streams and persisted bars/snapshots update continuously while the runtime session is active; stale feed blocks candidate progression; unsafe fixtures deterministically rejected; entry ineligibility never by itself disables the independent exit-compatibility path; retention jobs prune on schedule. No chart UI in M4. Run-rate recorded."

Evidence gathered by the authoring session from the hosted Supabase project (`jwujkzorirttwgsdoztq`, migrations through 001800 applied by the operator) with the local worker (`deploy/profile-0`, Profile 0) running all roles against mainnet through the operator's Birdeye Standard key, Jupiter free key and Helius free key. Commit range `a220cf9..` (packages: market-data foundation, chain truth + eligibility, route probes + emergency routes, held-asset safety, reconciliation + owned addresses, CU-ledger persistence, tracked-wallet stream, feed-health fix).

## Gate items

| Gate item | Status | Evidence |
| --- | --- | --- |
| Live eligible/watch universe populates | **Pass (after the Birdeye Lite upgrade, 2026-09-07 evening)** | On Birdeye Standard nothing could reach ELIGIBLE: `/defi/token_security` answered 401, every record carried `SECURITY_DATA_UNAVAILABLE` and the engine failed closed (44 BLOCKED, 15 EVALUATING, 0 ELIGIBLE at 17:19Z). The operator upgraded to Lite; `BIRDEYE:TOKEN_SECURITY` went HEALTHY and two further defects surfaced and were fixed the same evening: Birdeye reports holder shares as fractions (0.5367 = 53.67 %) which the normaliser read as percents, so every asset tripped the chain-versus-analytics mismatch; and raw largest-token-account concentration counted pool vaults and staking PDAs as holders (JUP 0.8, RAY above max). Concentration is now holder-only (accounts whose owner is a PDA or program-owned are excluded and reported as `programControlledFraction`) and an analytics disagreement is graded, not blocked. Result at 19:19Z on the hosted stack: **9 ELIGIBLE**, 87 BLOCKED, 7 EVALUATING, 1 DISCOVERED across 104 assets — e.g. USELESS grade 100 (liquidity USD 14.5 M, holder top-10 0.31, program-controlled 0.06), PENGU 85, ANTFUN 90, ALON 100 — every one with a fresh security report, chain-verified concentration, buy probes at the standard sizes and a confirmed sell route. Chain reads run through the Helius RPC (the public endpoint refuses `getTokenLargestAccounts`). |
| Live market streams and persisted bars/snapshots update continuously | **Pass (REST polling; WebSocket tier-gated)** | `BIRDEYE:CANDLES` HEALTHY with freshness age 828 ms at 17:19Z; 2 421 one-minute candles from 10:12Z to 17:05Z for the tracked set, 17 market snapshots, newest 17:19Z; ingest cycle every 30 min writes ≈180 candles and 2 snapshots at 90 CU. Streaming needs Birdeye Premium (WebSocket); the freshness contracts are sized to the polled tier so degradation is honest rather than hidden. |
| Stale feed blocks candidate progression | **Pass (mechanism), with a fix landed today** | `entriesBlocked()` (`libs/market/src/freshness/evaluate.ts`) blocks on any BLOCK-effect class not HEALTHY; `market-ingest.spec.ts` "a failing provider turns the feed FAILED … and blocks entries". Today's fix: classes nobody consumes yet (no positions, no candidates) are FAILED with no effect (`NO_DEMAND`), and TOKEN_SECURITY/TOKEN_OVERVIEW are published by the eligibility role from its own calls (a 401 is FAILED + BLOCK), so the hosted rows no longer claim that an empty position book blocks entries. No candidate machine consumes the block yet (M5b); the gate item is proven at the function and role level. |
| Unsafe fixtures deterministically rejected | **Pass** | `libs/risk/src/eligibility/engine.spec.ts` (mint/freeze authority, transfer hook, permanent delegate, paused, non-transferable, concentration, liquidity, route probes), `apps/worker/src/roles/eligibility.spec.ts`; live: JitoSOL and ZEC BLOCKED on `MINT_AUTHORITY_PRESENT`, small caps BLOCKED on concentration/liquidity/impact. |
| Entry ineligibility never by itself disables the independent exit-compatibility path | **Pass** | `libs/risk/src/safety/held-asset-safety.spec.ts` proves an asset refused by `entryAllowed` still reports `canReduceNow`; `exitCompatibility()` takes no eligibility input by construction; INV-21 maps the module. Live: role runs every 60 s; zero open positions yet (M5a). |
| Retention jobs prune on schedule | **Pass** | Hosted `cron.job`: `market-prune-candles` at 03:17 UTC daily and `market-candle-partitions` monthly, both active; `cron.job_run_details` shows job 1 succeeded at 2026-09-07T03:17:00Z; `ops.retention_policies` seeded (15s 21 d, 1m 730 d, 5m/15m 1825 d, 1h/4h unbounded); pgTAP suite 07 proves the prune function. |
| No chart UI in M4 | **Pass** | No chart component exists in `apps/web`. |
| Run-rate recorded | **Pass** | `docs/costs.md` M4 rows (below). |

## P1 / P2 deliverables (blueprint)

| Deliverable | Where |
| --- | --- |
| Birdeye adapter, discovery, OHLCV storage, snapshots, freshness/health, Jupiter Price V3 | `libs/market`, migration 001300, worker `market-ingest` |
| Token identity by mint | `core.assets` (mint primary identity, `first_observed_at` never moves) |
| Feature snapshots | Contract + table exist (M1/M2); populated by the feature engine in M5b |
| Token security adapter, holder distribution | Birdeye security report (needs Lite); chain-derived concentration from `getTokenLargestAccounts` (D45) |
| Smart-money/top-trader integration, Helius tracked-wallet events | `libs/onchain` wallet events, migration 001700, worker `tracked-wallets`; labels are evidence with provenance, no smart-money scoring yet (M5b/M6) |
| Eligibility state machine, hard reject reasons | `libs/risk/src/eligibility`, `core.asset_eligibility` append-only |
| Route/price-impact probes | `libs/execution/src/jupiter` (ADR-0003 one shared client) |
| Open-position safety state and exit-compatibility re-evaluation | `libs/risk/src/safety`, migration 001400, worker `held-asset-safety` |

## Observed provider limits (feed into D43 budgets)

- Birdeye Standard: 30 000 CU/month, 1 rps, 401 on token security; ≈1 000 CU/hour with the M4 loops → about 30 hours of allowance. Development tier only. Birdeye Lite (USD 39, 2.5 M CU/month, 15 rps) since 2026-09-07 evening: ingest every 5 min (≈90 CU), eligibility every 10 min × 5 assets (40 CU each) ≈ 2 300 CU/hour ≈ 1.7 M CU/month; 4 725 CU spent in the first evening including the accelerated evidence run. The persisted ledger keeps a 10 % CRITICAL reserve for held-asset safety.
- Helius free: parsed history for one active bot wallet produced 103 transactions / 265 events in the first poll and ≈300 events per 2-minute cycle afterwards (1 529 events in 12 minutes); such a wallet is noise for smart-money evidence and would grow the database by ~200 k rows/day. Migration 001800 adds `tracking_active` so the operator can pause it; a wallet-event retention policy is a follow-up before M5b.
- Public Solana RPC: fine for mint reads, refuses `getTokenLargestAccounts`; the Helius RPC serves both at 10 rps free.
- Jupiter free key: gateway-throttled at 10 rps; adapters run at 1 rps.

## Not in this gate

- Birdeye WebSocket (Premium+), Helius webhook receiver (needs a public endpoint; hosted profiles), wallet-event retention, smart-money scoring, candidate machine consumption of `entriesBlocked` (M5b).

## Verdict

Every gate item passes with code, tests and live evidence on the hosted stack. The interim adversarial review (`docs/reviews/review-m4-interim-market-chain-eligibility.md`, 2 HIGH / 9 MEDIUM / 11 LOW) has been applied; its fix-status table lists the residuals deferred to M5a/M8 (health from record age, consecutive-clean promotion, quote persistence, fail-closed chain-read records, pool-pair decoding, persist-failure alerts). The formal review #1 remains the M3 gate. M4 is ticked in the plan §8 with this file as evidence.
