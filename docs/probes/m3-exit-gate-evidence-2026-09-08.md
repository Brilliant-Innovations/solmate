# M3 exit-gate evidence — 2026-09-08

Milestone: **M3 Financial boundary skeletons** (execution plan §4). Status: **code complete except two operator-dependent items**; the §8 checkbox stays unticked until adversarial review #1 passes and the Turnkey adapter lands after Probe A/C (plan MP).

Authoring session: the same session that built M3. Per GUARDRAILS ground rule 4 this document is evidence for the reviewer, not the review.

## Exit-gate items (plan M3, "P6 acceptance items needing no market data")

| Gate item | Where it is enforced | Tests that break it |
| --- | --- | --- |
| Cannot sign arbitrary transactions | `libs/execution/src/validate/structure.ts` (wallet is fee payer and sole signer, programs static and allowlisted, no System transfer to an unapproved recipient), `libs/execution/src/validate/order.ts`, live adapter order → structure → simulation → deltas before any signature; internal API has no signing route; `SoftwareDevSigner` refuses live capability on mainnet | `validate/structure.spec.ts`, `validate/order.spec.ts`, `adapter/live-adapter.spec.ts` (tampered order, foreign program, extra signer: nothing signed), `harness/harness.spec.ts` (signer policy denies a foreign program even when a compromised executor calls the signer directly), `api/api.spec.ts` (`/v1/sign`, `/v1/send`, `/v1/transfer`, `/v1/call` are 404) |
| Duplicate deliveries do not double-enter | `apps/execution-service/src/idempotency/intents.ts` rebuilt from the journal, `pipeline.ts` claims the key before any work and treats a redelivered envelope failing only on its own nonce as the existing attempt | `idempotency/intents.spec.ts`, `harness/harness.spec.ts` (duplicate before and after restart; unknown result then redelivery), `api/api.spec.ts` (redelivery through the API), `risk-authorizer/src/service/authorizer.spec.ts` (a cycle is authorized once, also across a restart) |
| DB tamper detected on envelope and projection | `apps/execution-service/src/authority/verify.ts` (recomputed intent hash, stored row compared field by field), `apps/risk-authorizer/src/projection/verify.ts` (signature, sequence, freshness, release/policy match, chain agreement) | `authority/verify.spec.ts`, `projection/verify.spec.ts`, `harness/harness.spec.ts` (edited stored row → `DB_TAMPER_DETECTED`, nothing prepared) |
| Signed transaction durably identifiable before submit | `journal/journal.ts` hash-chained lines with a fencing token; the live adapter writes `SIGNED_NOT_SUBMITTED` (signed tx hash, expected signature, last valid block height) and only then re-reads the gate and submits | `journal/journal.spec.ts`, `adapter/live-adapter.spec.ts` (journal before `/execute`), `harness/harness.spec.ts` (crash after the signed line: recovery resolves from chain truth, INV-23) |
| Malicious transaction fails simulation | `libs/execution/src/simulate/deltas.ts` semantic balance-delta assertions over wallet-owned pre/post state | `simulate/deltas.spec.ts` (eleven P7 violations + property), `harness/harness.spec.ts` (simulation moving an unrelated token refused with nothing signed) |
| DB-down emergency close reduces only | `libs/execution/src/emergency/plan.ts` (held non-settlement asset → settlement mint, capped at chain custody; property-tested), `pipeline.ts` `emergency()` (no database read on the path, local pause afterwards), out-of-band endpoint authenticated by the pinned operator key alone | `harness/emergency.spec.ts` (close, close-all, capped, partial, refusals, replay after restart, shadow stops, property), `api/api.spec.ts` (out-of-band plane with no shared secret) |

Invariant map: 23 of 28 mapped (`node tools/check-invariant-map.mjs`); INV-01/04/05/06/08/09/10/12/23 carry M3 tests.

## Process evidence (Profile 0, local test database, devnet RPC, throwaway keys)

Run 2026-09-08 03:28–03:29 UTC from `deploy/profile-0/.env.execution-service` and `.env.risk-authorizer` (gitignored; generated by a scratchpad script, never printed). Both bundles are the ones the artifact scan passes (`node tools/check-artifacts.mjs`: risk-authorizer and execution-service allowlisted, ok).

- **Startup order observed (executor):** env validated → signer built and matched to the guardrails wallet → journal fenced → `recovery_complete` (0 unresolved) → `listening` on `127.0.0.1:8791` (internal) and `127.0.0.1:8792` (out-of-band). Warning logged because primary and simulation RPC shared an origin.
- **Startup observed (authorizer):** env validated → `authorizer_ledger_restored` (0 open) → `listening` on `127.0.0.1:8781`. Both processes reported the same contract-set digest after rebuild; before the rebuild they differed, which is the mismatch readiness must refuse (§31).
- **Internal plane:** signed `GET /v1/health` on both services answered 200; a bare request answered 401 `MISSING_HEADERS`; `POST /v1/sign` answered 404; `POST /v1/recover` answered `{ recovered: [] }`; `POST /v1/authorize` for an unknown cycle answered `DENIED CYCLE_NOT_FOUND`.
- **Out-of-band plane:** `node tools/traderctl.mjs <operator env> pause` signed a `PAUSE_NEW_ENTRIES` command with the throwaway operator key and the executor answered `PAUSED`; out-of-band health then showed `localPause.active = true` with the command id as reason; `POST /v1/pause/clear` through the authenticated plane cleared it.
- **Journal after the drill:** `EMERGENCY_COMMAND_RECEIVED` → `PAUSE_APPLIED` → `PAUSE_CLEARED`, hash-chained.

Two defects surfaced only by running the processes and were fixed the same day: the executor's listen-address regexes had lost their backslash, and the executor's own forbidden-credential list refused the development signer key it must hold. Both are now covered by `libs/contracts/src/config/config.spec.ts`.

## Not covered by this evidence

- Turnkey adapter, signer policy digest verification and deny-export checks: Probe A/C (operator-run, plan MP).
- Direct-pool emergency adapter, Trigger lifecycle, explicit stop slippage, signer outage with provider-protected lots: M6/M8b.
- Approval grants: the executor verifies them, but no operator approval key list is configured yet (`approverKeys` is empty in `main.ts`), so `LIVE_APPROVAL` would fail closed.
- The authorizer's reference quote source is a stub until M7 wires the worker's captured probe; the risk core sizes without a quote until then.
- No live mainnet run of either service; Profile 0 only.
