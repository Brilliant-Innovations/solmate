# Change log

One line per session, newest first. Format: `date · milestone · what landed · evidence`.

- 2026-09-08 · M7 · ADR-0009 P2 and P3 evidence: clearance provenance in the hash-chained audit ledger (§6.22, §20.25, §13.7; INV-07): every CLEARED transition (S0 decisions, discretionary outcomes, deterministic exits) now writes an `ACTION_CYCLE_CLEARED` event carrying the proposal hash, cutoff version, verdict, strategy version, Release digest and lot inside the same transaction, and the cycle row points at it (`cleared_audit_sequence/hash`, migration 002500, pgTAP 18); the state projector carries the ledger head (`RiskStateProjection.auditHead`); the risk-authorizer reads the event itself, recomputes the proposal hash, checks head coverage and verifies the chain against the external checkpoint replica (`AUDIT_CHECKPOINT_PATH`, verdict cached per ledger head) before sizing, denying `AUDIT_CLEARANCE_MISSING`, `AUDIT_EVENT_MISSING/HASH_MISMATCH/MISMATCH`, `AUDIT_CLEARANCE_TAMPERED`, `AUDIT_HEAD_BEHIND_CLEARANCE` or `AUDIT_CHAIN_<reason>`; worker role `audit-checkpoint` verifies and replicates the head outside Postgres (`checkpointAuditChain`). P3 is evidenced at the service boundary (sticky PAUSED read at authorization denies `SESSION_NOT_ACTIVE`; the executor already re-reads the mode gate before submit) · `apps/risk-authorizer/src/authorize/authorize.spec.ts` (tamper cases), `apps/risk-authorizer/src/service/authorizer.spec.ts`, `libs/db/src/server/audit-clearance.integration.spec.ts`, `apps/worker/src/roles/audit-checkpoint.spec.ts`
- 2026-09-08 · M7 · Manual actions, restart recovery and the single-sleeve arming guard (§14.8, §20.8, §21.3, ADR-0007): worker role `manual-actions` resolves MANUAL_CLOSE / MANUAL_REDUCE / EMERGENCY_CLOSE_ALL control requests from operators and admins through the position monitor's exported deterministic exit path (audited cycle, evaluation, intent, adapter; no AI review, no step-up for risk reduction), refusing viewers, malformed payloads, closed positions and reductions that round to zero; `runStartupRecovery` runs once before any entry or monitoring loop starts: loads what the database believes is open, runs one reconciliation pass over chain and custody truth, settles EXECUTING intents whose newest attempt is FINALIZED/NOT_LANDED, expires stale authorizations (EXPIRED_BY_LATENCY), fails EXECUTING intents with no attempt past expiry, leaves in-flight attempts to the executor journal, and withholds the trading roles when reconciliation itself failed with open exposure; `libs/db` `recovery-repo` (facts, settle, expire, orphans, `sleeveConflicts`); ARM_RELEASE now refuses `SINGLE_SLEEVE_PER_MINT` when a mint is held under two sleeves · `apps/worker/src/roles/{manual-actions,recovery}.spec.ts`, `libs/db/src/server/recovery-repo.integration.spec.ts` (also covers `advanceAttemptFinality`), `release-controls.spec.ts`
- 2026-09-08 · M7 · Staged finality, REORG_PENDING, RPC divergence and chain health (§14.7, §40.3, §24.4, D49; INV-22, INV-23): the live adapter now returns a `confirmed` provisional fill when finality misses the budget; the executor pipeline gained `trackFinality()` (a timer loop, default 5 s) that re-reads every open attempt from the primary plus an optional `SOLANA_RPC_SECONDARY` view through `libs/execution` `chain/quorum` (behind-vs-contradicting classification, success/failure split, INV-23 death proven on every view), promotes to FINALIZED (finalized fill row copied by `advanceAttemptFinality`, intent COMPLETED), moves confirmed-then-missing or contradicted transactions to REORG_PENDING with a journaled local pause that blocks new entries until nothing is uncertain and custody was re-read (`CUSTODY_RECONCILED`), and settles NOT_LANDED only from proof; journal kinds `ATTEMPT_OBSERVED`, `ATTEMPT_REORG_PENDING`, `CUSTODY_RECONCILED`. `libs/contracts` `ChainHealthPolicy` (chain-health-v1), `ChainHealthSnapshot`; `libs/execution` `chain/health` evaluator (halt, finality lag warn/block, view divergence, unavailable); worker role `chain-health` samples confirmed/finalized heads and block height per view, appends `ops.chain_health` (migration 002400, pgTAP 17) and mirrors the verdict into `ops.provider_health` as `SOLANA_CHAIN` so the existing BLOCK gate (cold start, paper entry, session facts) fails entries closed on a halt, stalled finality, divergence or an unreadable chain · `apps/execution-service/src/harness/finality.spec.ts`, `libs/execution/src/chain/{health,quorum}.spec.ts`, `apps/worker/src/roles/chain-health.spec.ts`
- 2026-09-08 · M7 · Release lifecycle, live arming and capital attestation (§12.4, §15.6, §15.9, D41, D56; ADR-0004 tiny-live variant; INV-18, INV-28 groundwork): `libs/risk` `release/lifecycle.ts` (pure `releaseTransition` DRAFT → PAPER_VALIDATED → ELIGIBLE_LIVE → ARMED → RETIRED with evidence-carrying events, `attestationFromStepUp`, `armingPreconditions`); `libs/contracts` `CapitalAttestation` entity and `S0_TINY_LIVE_VERSION_ID`; `libs/strategies` `s0TinyLiveVersion` (T1, 120 s live expiry above the 30 s reaction floor, LIVE_APPROVAL eligible, no LIVE_AUTO); migration 002300 `ops.capital_attestations` (immutable, positive ceiling; pgTAP 16); `libs/db` `release-repo` (`applyReleaseStatus` compare-and-set, attestation + capital rows, `stepUpEvidenceFor` with passkey fingerprint); worker `approvals` role now resolves PROMOTE_RELEASE (paper evidence → admin PROMOTE attestation) and ARM_RELEASE (admin ARM attestation + readiness verdict + live capability + ceiling → ARMED and recorded ceiling), `session` role accepts SET_REQUESTED_MODE through `SET_AUTHORITY` with §15.9 facts, projector reads the attested ceiling. Readiness hook and deployment live capability are false until M8a, so every live arming fails closed with the missing list recorded on the request · `libs/risk/src/release/lifecycle.spec.ts`, `apps/worker/src/roles/release-controls.spec.ts`, `supabase/tests/16_capital_attestations.test.sql`
- 2026-09-08 · M7 · Live intent path on the worker: authorize → LIVE_APPROVAL grant → executor (§13.7, §15.3–15.6, §20.8, D38, D41; INV-10; ADR-0009 P3): `libs/execution` `AuthorizerClient` (HMAC-signed, nonce'd calls to the risk-authorizer's `/v1/authorize`; the reply is schema-validated and the worker still treats the envelope as untrusted until the executor verifies it). `libs/risk` `approvals/grant` (`buildApprovalGrant`: only a verified, unexpired authorization naming the requested intent yields a grant; the authorization hash is recomputed from the verified envelope; exposure-increasing approvals need a step-up assertion; the grant expires no later than the intent and no later than `APPROVAL_MAX_VALIDITY_MS`; `signApprovalGrant` with the worker-exclusive approval key). `libs/db` `live-repo` (`listAuthorizedIntentsAwaitingExecution`, `loadAuthorizationForIntent`, `insertApproval` which also moves the intent to APPROVED, `listLiveAccounts`). Worker `roles/live-entry` (for a LIVE account while the session gate is open with LIVE_APPROVAL or LIVE_AUTO: cleared S0_SAFE ENTER cycles go to the authorizer, denials are counted by code; every unexpired authorization executes through the executor once its mode allows it, LIVE_APPROVAL only with a grant bound by `checkApprovalBinding`; an authorization that expires unexecuted is marked EXPIRED and logged `live_entry_expired_by_latency`; a gate that closed after authorization executes nothing) and `roles/approvals` (resolves APPROVE_AUTHORIZATION / REJECT_AUTHORIZATION control requests: verified step-up → signed grant → approval row; a tampered, foreign-key, expired or missing authorization, a missing step-up or a non-approver is refused with the reason; a rejection cancels the intent). Env: worker `RISK_AUTHORIZER_URL`, `RISK_AUTHORIZER_PUBLIC_KEYS`, `APPROVAL_SIGNING_KEY_PKCS8` (new exclusive credential, forbidden in web, authorizer and executor), `APPROVAL_SIGNING_PUBLIC_KEY`, `LIVE_ENTRY_INTERVAL_MS`, `APPROVALS_INTERVAL_MS`, `APPROVAL_MAX_VALIDITY_MS`; executor `APPROVAL_PUBLIC_KEYS` now pins the approver keys (previously an empty list, so no approval could verify). Both roles disable themselves with a logged reason on the paper account (no LIVE account, no keys). Specs cover grant binding and every refusal, the live gate, LIVE_AUTO execution, LIVE_APPROVAL waiting/unbound/bound, expiry by latency and the closed-gate case. The tiny-live S0_SAFE variant (ADR-0004), Releases/attestations promotion, capital attestation and staged finality are the next M7 packages.
- 2026-09-08 · M7 · Worker state projector: signed, sequenced `RiskStateProjection` per account (§6.14A, §13.6, D21, D52; ADR-0009 P1 and P5; first M7 package, runs on S0_SAFE): `libs/risk` `projection/build` (`buildRiskStateProjection`: pending exposure counted as spent, signer-dependent exposure = every lot whose protection is not an ACTIVE provider order, cohort and cluster capacity from open lots and equity against the policy caps, drawdown from day-start and rolling-high equity, canonical ordering so the same facts always hash the same; `signProjection` with the worker's projection key; `freshnessSummaryFrom` per data class from provider health; property: valid and deterministic for any lots, verifies only with the signing key and only unmodified). `libs/strategies` `release` (`releaseBindingFor`/`releaseFor`: the immutable version binding with a digest-derived id, status DRAFT until the operator promotes). `libs/db` `projection-repo` (`ensureRelease` by digest, `nextProjectionSequence`, append-only `insertProjection` with the unique (account, sequence), `listOpenLotSummaries`, `latestReconciliation`, `heldAssetEligibility`). Worker `roles/state-projector` and env `STATE_PROJECTOR_INTERVAL_MS` (60 s): custody and chain slot come only from a fresh, non-UNAVAILABLE custody reconciliation, otherwise no custody and slot 0 so a live entry cannot rest on stale truth; the S0_SAFE Release is registered at start and its digest carried by every projection. The risk-authorizer's `verifyProjection` already refuses signature, sequence rollback, age, Release and chain disagreements. Unit spec on fakes and integration spec on the local database.
- 2026-09-08 · M6 · S2–S4 strategy versions, the three remaining candidate families and the EVENT_WINDOW deterministic cap (§9.3–9.5, §12.1, §12.3A, D26, D64, INV-11): contracts `CatalystTriggerPolicy` `catalyst-v1` (fresh, trusted-time, novel, non-social catalyst with market confirmation), `SmartMoneyTriggerPolicy` `smart-money-v1` (independent tracked buyers over 4h, net accumulation, not dominated by one wallet, buyers outnumber sellers, structure confirms; owned wallets excluded upstream), `HybridTriggerPolicy` `hybrid-v1` (two independent families aligned inside 30 min; market-data families count once), `EventWindowCapPolicy` `event-window-v1` and `EventWindowRequest` (an optional field on the proposal; the skills scope rule refuses it unless the catalyst is packet evidence and the action is ENTER), S2–S4 ids. `libs/signals` `triggers/catalyst|smart-money|hybrid` evaluators and `detectCatalystCandidate`/`detectSmartMoneyCandidate`/`detectHybridCandidate` (the catalyst id and the aligned families are recorded on the candidate). `libs/agents` `event-window/cap`: `capEventWindow` (T0 from the trustworthy source time, duration = min(request, twice the half-life, policy max), policy cadence/retest/actions, refusals for untrusted time, stale age, recycled stories, evidence mismatch and already-expired) and `extendEventWindow` (once, on a NEW catalyst first seen after the window opened, never past the ceiling); property: no window ever exceeds the policy ceiling or opens on an old or untrusted catalyst. `libs/strategies` `llmStrategyVersions` builds S1–S4 from one spec table (S2 T3_CATALYST with a 5 min budget and the event-window policy; all PAPER/OBSERVE, bound to Trading Skill v1). Worker: the candidates role scans five families (events visible at now, tracked-wallet flow with owned wallets excluded, other families' recent detections for the hybrid); the agents role runs one tick per LLM strategy with its own families, automation binding and budgets, and applies the event-window cap to a cleared ENTER that asked for one (the entry itself is unaffected; activation through the session machine lands with the S2 paper run); paper-entry and position monitor register all four versions. Live activity for S2–S4 still needs intelligence and tracked-wallet data plus the model keys.
- 2026-09-08 · M6 · CryptoPanic and LunarCrush adapters and the worker `intel-ingest` role (§3.4–3.5, §6.6, §10.1–10.4, §18.3, D64, D65): `libs/intelligence` `providers/` with a transport port structurally identical to the market lib's (the lib never touches the network; keys are redacted from every logged URL), a per-minute limiter, `CryptoPanicClient.recentPosts` (posts → `RawSourceEvent`: URL, publication time as given, title, currencies as symbols, vote counts as a bounded sentiment hint; only metadata and the title are stored per provider terms) and `LunarCrushClient.coinMetrics` (normalized coin metrics → one SOCIAL event per symbol and hour bucket with the provider's timestamp as source time, so a repeat inside the hour only advances `last_seen_at`; raw posts are not ingested in v1 per §3.4). `RawSourceEvent.symbols` lets the entity index resolve provider tickers like cashtags. Worker `roles/intel-ingest`: tracked symbols → news batches and social metrics → the shared normalize/cluster/upsert pipeline, oldest source time first, evidence naming none of our assets not stored, one provider's failure does not stop the other; env `CRYPTOPANIC_API_KEY`, `LUNARCRUSH_API_KEY`, `INTEL_INGEST_INTERVAL_MS` (5 min), per-provider requests per minute; the role is disabled with a logged reason when both keys are absent. Specs: adapters on recorded responses (mapping, errors, 404, limiter), the role on fakes (syndicated copy clustered as DUPLICATE with novelty < 0.2, unmatched news dropped, repeat is SEEN_AGAIN, provider failure isolated). Live run-rate for both plans goes to `docs/costs.md` once the operator provides keys.
- 2026-09-08 · M6 · Worker `trading-actions` consumer and one exit path for discretionary position actions (§11.8–11.9, §6.10D, ADR-0001 handoff durability; INV-14, §31 "one execution boundary"): the `action_cycle.cleared` message the agents role enqueues with the cycle is drained through `processMessage` (exactly once per `cycle:<id>` idempotency key, contract-digest checked, retries with backoff, dead letter after five attempts); before anything moves the handler re-reads the database and skips with a recorded reason unless the cycle is still authorizable (CLEARED + CONFIRM at its latest cutoff, this position), has neither a risk evaluation nor an intent yet, its proposal exists with the same action and is unexpired, and the position is open and REVIEWED. REDUCE/EXIT run `executeClearedExit`, split out of the position monitor's mandatory-exit path so both share `allocateLots` and `executeExitIntent` (deterministic evaluation recorded and linked once via `recordRiskEvaluation`, size from the proposal's fraction, the intent naming the cleared cycle and cutoff, paper adapter, lot accounting on FINALIZED); ADJUST_PROTECTION can only call `tightenStop`, which never loosens; HOLD and anything else is skipped. Worker env `TRADING_ACTIONS_INTERVAL_MS` (15 s); S1 is now also among the position monitor's known versions. Unit spec covers execution, every skip reason without side effects, and tighten-only protection.
- 2026-09-08 · M6 · S1 contextual momentum, Trading Skill v1 binding, model gateway and the worker `agents` role (§11.1–11.2, §11.7–11.9, §12.1, D30, D39, D40, D43, D65): contracts `policy/s1` (`S1@1.0.0`, `trading-skill@1.0.0`, the v1 bindings tools-v1/guide-v1/ctx-v1/automations-v1/cycle-v1, `MODEL_POLICY_V1` with distinct providers for proposer and adversary, `DEFAULT_SPEND_LIMITS`) and worker env `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`AGENT_PROPOSER_MODEL`/`AGENT_ADVERSARY_MODEL`/`AGENT_MODEL_PRICING`/`AGENTS_INTERVAL_MS`/`AGENTS_BATCH_SIZE`. `libs/skills` `tradingSkillVersion` (refuses to build if any bound version drifted). `libs/strategies` `s1StrategyVersion` (T2_CONTEXTUAL, 3 min decision budget, 20 min candidate age, PAPER/OBSERVE only, bound to skill, guidelines and automation set). `libs/agents` `gateway/`: `ModelHttpTransport`, provider-independent prompts (§11.5 guidelines and the §11.8 adversary check list; evidence and the proposal rendered as quoted data; output JSON Schema generated from the contracts), `AnthropicReasoningModel` (forced tool call) and `OpenAIReasoningModel` (strict JSON-schema response), `createReasoningModel` (null with the reason when a key is absent), cost from a pricing table. `libs/db` `automations-repo` (`installAutomationSet` per strategy/skill binding, `recordAutomationRun`, `automationHistory`, `listCandidateTargets` with status deliberately unfiltered so S1 sees exactly the opportunity set S0 saw, `listPositionTargets` with the D39 consecutive-failure count). Worker: `agents/sources` binds the point-in-time reads, the paper book, cohorts and a Jupiter preview at the deterministic size; `roles/agents` runs one tick (session and spend facts → candidate targets → automations → discretionary cycle → one-transaction persist with the position-review transition and, for a cleared REDUCE/EXIT/ADJUST_PROTECTION, the trading-actions outbox message → automation run row → spend charge per window unit); `main` registers S1 next to S0 in the paper-entry loop so a cleared S1 ENTER is paper-filled through the same adapter, installs default budgets (platform model USD/day, strategy cycles/hour and USD/day, provider requests/minute) and disables the role with a clear reason when either key is missing. Unit spec covers end-to-end candidate and position ticks, cooldown/budget/activity skips, PROTECTION_ONLY on an unresolved review, the outbox on a cleared EXIT and per-target error isolation; integration spec covers the automations repo. The role is dormant on the attended P1A worker until the operator provides model keys; run-rate goes to `docs/costs.md` then.
- 2026-09-08 · M6 · Point-in-time reads in `libs/db` `context-repo` (§11.3, §18.3; INV-13 in SQL): `featureSnapshotAt`, `marketSnapshotAt`, `eligibilityAt`, `safetyAt`, `positionContextAt` (the position as it was at the cutoff: invisible if opened later, mark from the latest price observed at or before it, thesis/invalidation/horizon from the latest proposal that existed then), `onchainFlowAt` (tracked-wallet net quote flow and distinct buyers/sellers over 1h/4h/24h ending at the cutoff, counting only events first seen by then and never a wallet flagged `is_owned`, so own activity cannot become evidence) and `cohortPeersOf`. Every query bounds by `asOf` and there is no unbounded variant. Integration spec seeds rows on both sides of a cutoff and checks each read sees only the earlier side and that an owned-wallet buy is excluded.
- 2026-09-08 · M6 · Point-in-time context builder and read-tool handlers (§11.3, §11.4, §11.13, §18.3, D40; INV-13 in the agent packet): `libs/skills` `context/sources` (the read interface every packet and tool depends on; every read takes `asOf` and has no write path), `context/builder` (`buildTradingSkillContext`: candidate or position target resolved at the cutoff, then features, market snapshot, eligibility, held-asset safety, on-chain context with own wallets excluded and events first seen at or before the cutoff, all as typed `EvidenceItem`s; stale snapshots are labelled with their age, missing values read `missing`, never zero; allowed actions are machine ∩ skill ∩ strategy; the scope's citable ids are exactly the packet's; a candidate discovered after the cutoff or a missing target raises `ContextUnavailableError`), and `context/handlers` (`createToolHandlers`: the eight read tools bound to the same sources at the run's cutoff, evidence returned becomes citable, execution preview side follows the target and carries the deterministic size, `submitActionProposal` hands the typed proposal to a per-run sink). Property test: an event first seen after the cutoff never enters the packet or a tool response, whatever the source returns. The repository-backed sources (as-of reads) and the worker role that runs cycles land next; provider adapters still wait on operator keys.
- 2026-09-08 · M6 · Automations, guidelines v1 and prompt-injection defence (§11.1, §11.5, §11.7, §11.13, D39, D43): contracts `AutomationSet` `automations-v1` with one rule per required §11.7 trigger type (five candidate, eleven open-position, three system; interval, cooldown, priority, enabled modes, context deadline), reassessment heartbeat per speed tier (T0 5 m, T1 10 m, T2 30 m, T3 2 h), a 5 % price-excursion trigger and the D39 retry backoff (60 s doubling to 15 m, HIGH alert after three unresolved). `libs/agents` `automations/engine`: `decideAutomation` (mode → activity state → one cycle per target → target cooldown → per-type interval → PROTECTION_ONLY backoff → spend gate; every skip carries a disposition and next-eligible time), `pickTrigger` (highest priority wins; security and protective-order changes outrank the heartbeat) and `evaluatePositionTriggers` (heartbeat due, excursion, milestone, horizon checkpoint, event flags). `libs/skills` `guidelines/v1` (`guide-v1`, the fourteen §11.5 rules, pinned by test) and `prompt/render` (evidence rendered as delimited, escaped data blocks; delimiters, role markers and control characters are neutralised; `PROMPT_INJECTION_FIXTURES` and a property test show no evidence text can open or close a block or start a role line; prompts are deterministic per context). The tool schemas and scope rules remain the enforcement layer; the prompt is the second line.
- 2026-09-08 · M6 · Agents persistence (§6.10, D39, D43; ADR-0001 amendment "handoff durability"): `libs/db` `agents-repo` with `ensureSkillVersion`/`loadSkillVersion`, `persistDiscretionaryOutcome` (terminal cycle, every `AgentRun`, proposals, adversarial reviews, tool invocations and tool refusals, the position's next review state as computed by the worker's position-review machine, and the `trading-actions` outbox message, all in one transaction; a non-terminal cycle is refused; a failed write leaves the position untouched), `loadCycleSummary`, and the D43 spend side (`ensureSpendBudget`, `listActiveSpendBudgets`, `listSpendUsageAt`, `spendWindow`, `chargeSpendUsage`, `pauseSpendWindow`). Migration `20260908002200_tool_refusals_spend_usage.sql` adds append-only `agents.tool_refusals` (INV-16 audit of calls that never reached a handler), `ops.charge_spend_usage` (atomic upsert per budget window, service_role only) and a guard so usage counters only increase inside a window and a paused window stays paused; pgTAP suite 15; applied to the hosted project. Integration spec covers the atomic write, PROTECTION_ONLY flip on an UNRESOLVED cycle, rollback on a failed write, and window charging.
- 2026-09-08 · M6 · Discretionary action-cycle runner and spend circuit breakers (§11.2, §11.8–11.9, §11.13, D30, D39, D40, D43; INV-14/19/20 now also proven through the runner, INV-21 owner module `libs/agents/src/budget` built): contracts `TradingSkillContext` (one point-in-time packet: quoted `EvidenceItem`s with ids, allowed actions, cutoff version and time, deadline, strategy facts, revision round and prior objections), `AdversarialReviewInput`, `ModelCallMetadata` and `DiscretionaryCyclePolicy` `cycle-v1` (refresh on revision, 45 s per call, 2 s minimum remaining budget, min confidence 0.5). `libs/agents` adds `ReasoningModel` (§11.2 interface: identity, propose, review; adapters honour an abort signal and throw `ModelTimeoutError`), `runDiscretionaryCycle` (spend gate before any model call → context at cutoff v1 → proposer → typed validation plus the skills scope rules → machine PROPOSED → adversary on the identical packet → verdict; CHALLENGE mints cutoff v2, rebuilds the packet with the objections and runs both again there; every call, failed or not, becomes an `AgentRun` with tokens, cost, latency and schema errors; outage → UNRESOLVED(ADVERSARY_UNAVAILABLE), overrun → UNRESOLVED(TIMEOUT), insufficient budget or unbound skill → EXPIRED, malformed/hallucinated/out-of-scope output → UNRESOLVED(MALFORMED_OUTPUT); the adversary may cite only packet or proposal evidence, must answer the current cutoff and cannot carry a proposal; IGNORE clears with no adversary run) and `evaluateSpendGate` (platform → strategy → provider order; cycles per hour, model USD per day, provider requests per minute, paused windows; only windows containing now count). Property test: for any proposer/adversary behaviour at most two rounds run, exposure clears only with CONFIRM at the latest cutoff, proposer and adversary always share a cutoff, and an open position ends REVIEWED only when the cycle can authorize. The worker role that persists cycle, proposals, reviews and runs in one transaction and the provider adapters wait on operator model keys.
- 2026-09-08 · M5a fix · Worker roles re-acquire a fenced lease instead of draining the process. During the attended P1A run a network fault at 09:23 UTC (ECONNRESET, then DNS failure for the Supabase pooler) failed every role's lease heartbeat; each role stopped as the lease contract requires, and with no loop left the process exited at 09:25 UTC and stayed down until noticed at 14:48 UTC. `loopUnderLease` now retries acquisition with bounded backoff (15 s doubling to 120 s), treats an acquire/release failure like a fence, and returns only on an operator stop; the fence semantics inside a held lease are unchanged (`*_lease_retry` / `*_lease_unavailable` / `*_lease_failed` events). Paper session `e502e4df` persisted through the outage; no position was open.
- 2026-09-08 · M6 · Trading Skill tool manifest and registry (§11.3–11.4, §11.13; INV-16 now mapped, 25 of 28): contracts `ToolManifest` `tools-v1` with the nine §11.4 tools (eight READ_ONLY, `submitActionProposal` PROPOSAL_ONLY), a strict argument schema per tool (ids only; a proposal is the typed `TradingActionProposal`, so amounts, destinations, mints or a SHORT direction are schema errors), `ToolScope` (what one agent run may name: its cycle's candidate or position, its asset allow-list, versions, trigger, cutoff and the evidence the context builder seeded), `ToolRefusal` audit rows and `FORBIDDEN_TOOL_CAPABILITIES`. `libs/skills` replaces its stub with `loadManifest` (refuses duplicates and any entry whose name reads like sql/http/shell/transfer/sign/secret/limits/live/automation/prompt), `checkScope`/`checkProposal` (server-side id resolution; a proposal must match the cycle's cutoff version, strategy, skill and trigger, name the cycle's own candidate for ENTER/IGNORE or position for HOLD/REDUCE/EXIT/ADJUST_PROTECTION, carry a REDUCE fraction only for REDUCE and a protection intent for ADJUST_PROTECTION, not be expired at the cutoff, and cite only evidence ids the run was shown, so hallucinated ids are refused rather than dropped) and `ToolRegistry` (the handler map must cover the manifest exactly and an extra handler is a construction error; invoke goes name → manifest → schema → scope → per-run budget → handler; every call, refused or served, leaves an audit record with request hash, cutoff version and latency; one proposal per run; `close` fences the run). Property tests: any name outside the manifest is refused before a handler runs; any extra argument key is a schema refusal. Handlers for the read tools land with the context builder and model gateway once operator keys exist.
- 2026-09-08 · M6 · Intelligence layer core without provider keys (§6.6, §10.1–10.4, §18.3, D64; INV-13 now mapped, 24 of 28): contracts `NormalizationPolicy` `intel-v1` (pinned domain → source-quality table, trusted-time providers, dedupe window and similarity thresholds, fresh-catalyst age ceiling); `libs/intelligence` replaces its stub with `EntityIndex` (mint > unique cashtag > unique bare symbol > unambiguous name; shared symbols and short names never match), `normalizeEvent` (provider-agnostic `RawSourceEvent` → event draft with the two clocks kept apart, source quality only from the domain table, payload and URL hashes, a source time ahead of first-seen downgraded to LOW), `assignCluster` (same URL or same wording → DUPLICATE, same assets with shared words → CORROBORATION, cluster id = first event's id, novelty = 1 − similarity; order-independent, property-tested) and `catalystAgeMs`/`freshWindowVerdict` (age from trustworthy source time only; recycled or old content cannot open a fresh window; a new first-seen never resets age). `libs/replay` replaces its stub with `point-in-time/visibility` (events by first-seen, candles by close plus lag, labels by first observation; property: nothing visible at t was first seen after t). `libs/db` `intelligence-repo`: `upsertEvent` inserts once per (provider, sourceId) and a repeat may only advance `last_seen_at`; `listEventsForClustering`; `listEventsVisibleAt(assetId, at)` enforces `first_seen_at <= at` in SQL with no variant without `at`; `listAssetEntities`. Migration `20260908002100_events_two_clocks.sql` extends the M1 guard (last_seen_at only forward, cluster assignment set once, provenance immutable, no deletes; pgTAP suite 14) and is applied to the hosted project. Adapters (CryptoPanic, LunarCrush) and the model gateway wait on operator keys; the P4 acceptance items (syndicated story → one cluster, both clocks retained, replay cannot expose future evidence) are covered by these tests ahead of live data.
- 2026-09-08 · M5b fix · **Defect found on the hosted paper book and fixed the same hour**: bumping the S0 strategy versions (1.1.0 at 03:58 UTC, 1.2.0 at 04:07) left the position monitor unable to manage the open STONK lot opened under `S0_RAW@1.0.0`, because it resolved a lot's strategy version only from the worker's current in-memory versions; the lot was marked every 30 s but its hard stop, breached at about 04:09, could not execute (`position_monitor_failed: no strategy S0_RAW@1.0.0`). Fix: `loadStrategyVersion` reads the immutable `research.strategy_versions` row and the monitor falls back to it for any lot whose version is no longer registered (D7 immutable versions: an older lot is managed under its own contract); worker spec covers an older-version lot exiting and an unknown version reporting an error. After the restart at 04:13 the stop exit filled at 04:14 (`HARD_STOP`, proceeds 179.43 USDC, realized −20.57 USDC, position closed, lot closed, sleeve released): the first full paper round trip. Rule recorded for future version bumps: every role that acts on stored rows must resolve the row's own version, never the current one.
- 2026-09-08 · M5b · **Gate met**: feature snapshots now carry the §8.6 suppression flag from our own LIVE fills inside the versioned window (`features.spec.ts` inside/outside the window; paper fills never touch the chain and stay excluded), completing the self-influence wiring; every P3 deliverable and acceptance item is recorded in `docs/probes/m5b-evidence-2026-09-08.md`; plan §8 M5b ticked. Remaining trigger families (smart-money, catalyst, social) wait on tracked-wallet flow features and M6 intelligence and are research breadth, not M8a prerequisites (ADR-0007).
- 2026-09-08 · M5b · Early-acceleration trigger family and multi-family candidate detection (§9.2, §9.7, D63): contracts `EarlyAccelerationTriggerPolicy` `early-accel-v1` (rising 5m return acceleration and 15-bar volume growth, trend turning and persistent, no breakout yet and price below the upper Bollinger band, not extended, RSI room, liquidity, executability; trade-count acceleration and SOL relative strength are optional evidence) and the shared `CandidateLifecyclePolicy`; `evaluateEarlyAccelerationTrigger` is pure, explainable and property-tested (deterministic, cold inputs never fire, score in [50, 100] and monotone in acceleration); the detector's dedupe/cooldown/self-influence/entry-gate tail is shared by both families, an open candidate of any family inside the window aggregates the move (§9.7), cooldown stays per family; the candidates role scans both families per asset (warm-up counted once per asset, per-family detected/rejected in the cycle log). S0 now paper-trades the full deterministic trigger set: `S0_TRIGGER_FAMILIES` filters the awaiting query and S0 versions move to `S0_RAW@1.2.0`/`S0_SAFE@1.2.0` whose thresholds record both trigger policy versions and the families (1.1.0 rows stay immutable). Smart-money accumulation waits on tracked-wallet flow features; catalyst and social families wait on M6 intelligence.
- 2026-09-08 · M5b · Regime classifier, universe/cohort relative strength, candidate labels and the D32 candidate-age contract (§8.4, §8.5, §12.3, D32, D62): contracts `MarketRegimePolicy` `regime-v1` (fixed rule order: VOLATILITY_SHOCK → BROAD_SELLOFF → SOL_LED_RALLY → NARRATIVE_ROTATION → RISK_ON_TREND → LOW_LIQUIDITY_CHOP → none; POST_EVENT_INSTABILITY waits on M6 events; fewer than eight warm assets yields no label, never a default) and `FEATURE_ENGINE_V2` `features-v2` adding `rs_universe_1h`/`rs_cohort_1h` (the lookback record is now partial: a feature absent from a version is not produced by it); `libs/signals` `classifyRegime` and `relativeStrength` are pure over the minute's warm vectors (permutation-invariant, property-tested); the features role now computes every asset's vector first and then a cross-asset pass that labels the regime on each snapshot and fills relative strength from the universe median and the asset's strongest cohort median (cohort memberships from `cohorts-v1`, SOL from the reference series); every candidate's `triggerDetails` records the regime and market sessions it was detected under. S0 strategy versions move to `S0_RAW@1.1.0`/`S0_SAFE@1.1.0` bound to `features-v2` (immutable versions: the 1.0.0 rows, their cycles and the open lot stay as they are). `expireS0` + `persistS0Expiry`: a candidate older than the strategy's `maxCandidateAgeMs` at decision time produces EXPIRED cycles for both variants and leaves DETECTED as stale, never a late decision (worker spec). Cohort exposure now ignores duplicated membership rows (found by the property test).
- 2026-09-08 · M5b · Deterministic taxonomy cohorts and rolling correlation clusters (§6.3, §8.4, §13.1, D23; ADR-0007 unknown-is-most-restrictive; the M8a-prerequisite half of M5b): contracts `CohortTaxonomyPolicy` with `DEFAULT_COHORT_TAXONOMY` `cohorts-v1` (seven human-maintained cohorts and MANUAL memberships for well-known Solana mints; editing it is a versioned reviewed change) and `CorrelationClusterPolicy` `clusters-v1` (24 h window, 5 m log returns, ≥96 aligned samples, average-linkage Pearson ≥0.6, min cluster size 2); `libs/risk/cohorts` computes clusters as a pure function of stored closes (permutation-invariant, property-tested) and cohort/cluster exposure fractions for the risk core, judging a multi-cohort asset against its most used cohort and leaving unlisted or unclustered assets unknown; `libs/db` `cohorts-repo` installs a taxonomy version idempotently for mints that exist in `core.assets`, lists ACTIVE non-LLM memberships per version, stores one cluster set per window and reads only a fresh set (integration-tested, including the table refusing an LLM suggestion as ACTIVE); worker role `cohorts` installs the taxonomy at start and recomputes clusters hourly (`COHORTS_INTERVAL_MS`); the paper-entry role now feeds real cohort and cluster usage into `evaluateEntry` instead of null, so `maxCohortExposureFraction`/`maxClusterExposureFraction` bind and `requireCohortCapacity` can be switched on for the live safety subset. Relative strength versus cohort/universe, the regime classifier, session labels and further trigger families follow as research breadth.
- 2026-09-08 · M5a · **Gate met**: first attended P1A paper trade on the hosted project at 03:33 UTC. Momentum trigger fired on STONK (score 82); `S0_SAFE` rejected it on `OVEREXTENDED_1H`, `S0_RAW` (ungated baseline) cleared; the deterministic risk core sized 200 USDC at the max-position cap; the paper adapter finalized the fill (shortfall −23 bps, price improved) and the position opened with one MONITORED_EXIT lot, marked 30 s later by the position monitor; Level B probes captured for entry reference, decision, executable and exit mark. Reconstruction check re-run against the real stored decision and fill: identical. `docs/probes/m5a-pipeline-evidence-2026-09-08.md` updated; plan §8 M5a ticked.
- 2026-09-08 · M3 · Profile 0 process run of both financial services and exit-gate evidence (`docs/probes/m3-exit-gate-evidence-2026-09-08.md`): throwaway env files (gitignored) for execution-service and risk-authorizer against the local test database and devnet RPC; observed startup order (env → signer/wallet match → fenced journal → recovery → listen), matching contract digests after rebuild, authenticated health/recover/authorize, bare and unknown routes refused, a real `PAUSE_NEW_ENTRIES` signed by `traderctl` through the out-of-band plane, cleared through the internal plane, hash-chained journal lines. Two env-schema defects found only by running the processes are fixed with spec coverage: the executor's listen-address regexes had lost their backslash, and the executor's forbidden-credential list wrongly refused `SOFTWARE_SIGNER_KEY_PKCS8` (it is the executor's alone; worker, web and risk-authorizer still refuse it). M3 remains unticked: Turnkey adapter (Probe A/C) and adversarial review #1 are owed.
- 2026-09-08 · M3 · risk-authorizer process wiring and authorize API (§13.7, §15.5, §15.8, D21, D45, D52; ADR-0009 P1): `AuthorizerService` loads the immutable rows for one cleared cycle through injected sources (`libs/db` `authorizer-repo`: cycle, proposal, candidate asset + eligibility + feature snapshot, account with settlement decimals, verified custody accounts, newest release for the strategy version, its newest attestation, newest signed projection, open authorizations), refuses PAPER accounts and missing rows, reads chain truth itself (`chain/reads.ts`: wallet SOL, settlement and per-custody token balances, mint hard state through the allowlisted read-only RPC; a failed read denies rather than falling back to the projection), runs `authorizeEntry`, and persists evaluation + intent + stored envelope (`recordAuthorization`) or an audit line for the denial; the ledger is rebuilt from stored open authorizations at start so a restart cannot sign the same capital twice. `authorizeEntry` now returns the deterministic evaluation record with the envelope. The HMAC-authenticated API serves `GET /v1/health` and `POST /v1/authorize {actionCycleId, accountId}` only: every bound field comes from rows and chain reads the authorizer loads, so a caller cannot widen an authorization. `main.ts` validates the environment fail-closed, imports the signing key and projection keys, builds the RPC client from the allowlist and the Postgres client, restores the ledger and listens (`INTERNAL_API_LISTEN`, `INTERNAL_API_SECRETS`). The reference quote source is a stub returning no quote until M7 wires the worker's captured probe (the authorizer holds no DEX client). Projection sequence rule clarified: an older sequence than one already accepted is a rollback; the same sequence may back several authorizations inside its freshness window (verify.ts, spec updated). Tests: authorized once with verifiable envelope and persisted intent, redelivery denied, ledger restored across restart, paper/unknown/chain-failure refusals, API behind the secret with no other route.
- 2026-09-08 · M3 · `traderctl` CLI skeleton and `recoveryctl` runbook skeleton (D25 planes 1 and 2, D53, D54, §15.8, §15.10): `tools/traderctl.mjs` signs one `EmergencyCommand` (`pause`, `close-asset <mint> [--max]`, `close-all`, plus `health`) with the operator's emergency key from an operator-side env file that never lives in a deployable, using the built contracts library so the envelope is the one the executor verifies, with a two-minute expiry and a fresh nonce, and posts it to the out-of-band endpoint; `tools/recoveryctl.mjs` has no signing capability at all: `enumerate` reads wallet SOL, SPL holdings and recent signatures through a closed read-only RPC method set (chain first, never the database) and `runbook` prints the D54 order (declare, pause if possible, revoke the executor identity first, enumerate chain and vaults, time-boxed break-glass principal, close/recover, sweep to the pre-registered cold wallet whose address no CLI argument or env var supplies, rotate/redeploy/reconcile, operator closure). `docs/runbooks/recoveryctl.md` records the two planes, their separate credentials and what the skeleton deliberately lacks until Probe A/C (provider-side revocation, incident identity, vault recovery, sweep).
- 2026-09-08 · M3 · execution-service process wiring (§15, §21.3, D10, D12, D22, D25, D47): `main.ts` now validates the environment fail-closed, refuses to start without internal API secrets, a cluster mismatch between guardrails and environment, a signer whose public key is not the guardrails' trading wallet, or a signer backend that is not built yet (TURNKEY waits on Probe A/C), builds the read-only RPC clients (primary chain observer and custody reader, independent simulation endpoint, warning when both share an origin), the Jupiter order and quote clients and the Postgres client, opens the fenced journal (a held fence or a corrupt chain is fatal), runs restart recovery before listening, then serves the internal API and the out-of-band endpoint (warning when a listener is not loopback) and releases the fence on SIGINT/SIGTERM. Mode facts are read from the open `ops.runtime_sessions` row on every decision and fail closed to OFF/OBSERVE/paused when Postgres is unavailable (risk reduction stays ungated); the decision quote comes from the cycle's DECISION probe. `libs/db` gains `executor-repo` (`loadTradeIntent`, `loadApprovalGrant`, `executorModeFacts`, `decisionQuoteForCycle`, `persistExecution` reconciling orders, attempts, fills and intent lifecycle after the journal already holds the truth; integration-tested against the local database). The pipeline's mode-facts source may be asynchronous; the internal API persists executed attempts best effort and journals RECONCILED_INTO_DB. Reviewed artifact-allowlist change: `postgres` enters the execution-service bundle (GUARDRAILS Part 4 permits `db` read paths). Still owed in M3: risk-authorizer process wiring and its authorize API, `traderctl`/`recoveryctl` skeletons, Turnkey adapter, adversarial review #1.
- 2026-09-08 · M3 · Authenticated internal API, out-of-band signed command endpoint and worker client (§15.2, §15.8, D25 plane 1): `service-auth` in contracts signs and verifies service-to-service requests with HMAC-SHA256 over method, path, timestamp, nonce and body hash, refusing skewed timestamps and any nonce seen inside the window (`NonceWindow`), identifying secrets only by a hash-prefix key id; `apps/execution-service/src/api/internal.ts` serves exactly four verbs (`GET /v1/health`, `POST /v1/execute`, `POST /v1/recover`, `POST /v1/pause/clear`) behind that authentication, loads the immutable intent row and any approval itself, and answers 503 with entries stopped when Postgres is unavailable (D22); there is no route that signs bytes, transfers SOL or calls a program, and the test asserts those paths 404. `api/out-of-band.ts` is a second listener that accepts only a `SignedEmergencyCommand` (authenticated by the pinned operator key alone, no shared secret, no database) and reports a health line; `libs/execution` gains `ExecutorClient` for the worker. Env: executor `INTERNAL_API_LISTEN`, `INTERNAL_API_SECRETS`, `OUT_OF_BAND_LISTEN`, `SOFTWARE_SIGNER_KEY_PKCS8` (exclusive to the executor; refused in worker, web and risk-authorizer environments), `JUPITER_API_KEY`; worker `EXECUTION_SERVICE_URL`, `INTERNAL_API_SECRET`. Loopback tests: end-to-end execute through the API, redelivery is a duplicate, replayed nonce / tampered body / foreign secret / skewed clock / bare request refused, emergency close through the out-of-band plane with replay and unpinned-key refusals, local pause cleared only through the authenticated plane after review. Process wiring (`main.ts` for both financial services) is the next package.
- 2026-09-08 · M3 · `EMERGENCY_CLOSE` path (D22, D25 plane 1, §15.10, §15.10A; INV-12 now mapped): `verifyEmergencyCommand` accepts an out-of-band command only from a key id pinned in the guardrails, for this cluster, inside its validity window and with an unused nonce; `planEmergencyClose` in `libs/execution` turns chain custody (new `CustodyReader`, RPC implementation over `getTokenAccountsByOwner`) into bounded sell actions that can only convert a held non-settlement asset into the deployment settlement mint for at most the chain-confirmed amount, capped again by the command and the per-transaction guardrail, under the protective slippage ceiling (property-tested: never a buy, never a settlement mint as input, never above custody); `evaluateShadowStops` applies the durable shadow's hard/trail/time/unreviewed stops to fresh marks with missing marks reported rather than sold. `ExecutorPipeline.emergency()` journals the command, applies `PAUSE_NEW_ENTRIES` locally (survives restarts through PAUSE_APPLIED/PAUSE_CLEARED lines; cleared only by the reconciliation/operator path), refuses a monitor request carrying a shadow older than the last synced sequence, runs each close through the same live adapter path (order, structure, simulation, semantic deltas, sign, persist-before-submit) with `ExecutionBounds` in place of an envelope, releases the exposure ledger in proportion to what was sold against chain-held quantity, and applies the local pause after any close so entries cannot resume before operator review. The validator and adapter now take `ExecutionBounds` (a `RiskAuthorizedIntent` satisfies it); the chase check is skipped only when no decision quote can exist. Harness cases: signed close of a held asset with no database, close-all leaving settlement untouched, command cap above custody clipped to chain truth, partial close with proportional release, unaccepted key / wrong cluster / expired / not held / settlement target / nonce replay after restart refused, PAUSE blocking entries while a close still runs, shadow stop hit closing at chain quantity and a stale shadow refused.
- 2026-09-08 · M3 · Executor pipeline and the §24.3 execution harness (§15.3–15.4, §15.10, §21.3; D12, D21; ADR-0009 P3/P4; INV-01, INV-04, INV-05, INV-23 gain harness tests): `apps/execution-service/src/pipeline` sequences authority verification → idempotency claim journaled as ATTEMPT_PREPARED → deployment caps against the replayed exposure ledger → the live adapter with ATTEMPT_SIGNED written before anything leaves the process and the mode gate re-read then ATTEMPT_SUBMITTED written immediately before `/execute` (the adapter's gate hook now runs after the durable signed record, not before signing) → ATTEMPT_RESULT and the ledger event; a timed-out signer is retried with the identical canonical bytes; a redelivered envelope that fails only on its own nonce reports the attempt it already has instead of a denial. `recover()` resolves every signed or submitted attempt from chain truth on restart (landed → finalized once; provably dead by block height and empty signature history → NOT_LANDED and released; otherwise left landable with the key still claimed) and releases prepared-but-unsigned reservations once the authorization expires. `libs/execution` gains `ChainObserver` (`getSignatureStatuses` with history, `getBlockHeight`; allowlisted, read-only) and the INV-23 `proveDead` rule. The harness (`src/harness`) runs the real pipeline over a fake Jupiter/Solana with balances, block height and persistent signature statuses: successful fill; failed before submission; unknown result and landed-but-timeout; partial/changed amount; stale blockhash; slippage failure; duplicate delivery before and after restart; quote invalid between risk check and execution; signer timeout then identical retry; signer policy refusing a foreign program even when a compromised executor calls it directly; crash after durable SIGNED_NOT_SUBMITTED, after submit before response persistence, and at the prepared and submitted boundaries; tampered simulation moving an unrelated token; unexpected signer; DB tamper and cap breaches; a pause landing between signing and submit. Still owed to later milestones: Trigger deposit/cancel/withdraw lifecycle and explicit stop slippage (M6 protection), direct-pool emergency adapter and stale emergency-route cases (M8b), signer-unavailable-with-provider-protected-lots (M6). execution-service now depends on `solana-hard-state` (base58 for the fake chain).
- 2026-09-08 · M3 · Live `ExecutionAdapter` with persist-before-submit (§15.4 in full, D12, D21, D45, D49; ADR-0009 P3/P4/P7): `JupiterOrderHttpClient` in `libs/execution` speaks Swap V2 `/order` (assembled transaction, reference-size order for measured impact) and `/execute` (the transport gains POST); `LiveExecutionAdapter` sequences an order for exactly the authorized pair and amount → order-versus-authorization checks → structural checks → independent simulation over every static key with wallet-owned pre/post state → semantic delta assertions → re-check of validity and the trade intent's expiry → the executor's own pre-submit gate → sign → durable SIGNED_NOT_SUBMITTED journal → `/execute` → provider confirmation → finality from the executor's own chain read. A provider "Failed" leaves the attempt SUBMITTED for reconciliation until a block-height-expired-and-no-signature proof moves it to NOT_LANDED (INV-23); a request without a verified envelope for this intent, or LIVE_APPROVAL without a grant hash, never reaches the router. It passes the same eleven-scenario paper/live parity table as the paper adapter over stubbed transports (the authority row mirrors: PAPER refused), and refuses a widened amount, substituted mint or taker, lowered minimum output, foreign program, over-spend, ownership change, excess SOL debit or closed mode gate with nothing signed and nothing journaled. `chaseWorseBps` is shared with the fill model; `SimulationReader` is the simulation seam. Execution harness (§24.3 crash-at-every-boundary), emergency close, process wiring and the Turnkey adapter follow.
- 2026-09-08 · M3 · Independent simulation and semantic balance-delta assertions (§15.4 steps 5–6, D45; ADR-0009 P7 landing-time half): `SimulationRpcClient` in `libs/execution` speaks only `simulateTransaction` (signature verification off, post-state for named accounts) and `getMultipleAccounts` against the allowlisted independent endpoint and can never send; an SDK-free SPL token-account decoder; `assertSemanticDeltas` compares wallet-owned pre- and post-state and refuses an input decrease above the authorized maximum, an output increase below the order's minimum, any unrelated token decrease, SOL leaving beyond modelled fees and rent, an owner change, an introduced delegate or close authority, a freeze, a closed account, a failed simulation or missing wallet state, and reports per-mint deltas and unexpected new accounts as a `SimulationReport`. Property test: a passing report implies spend ≤ authorized and receive ≥ minimum. The live adapter that sequences build → validate → simulate → sign → journal → submit → reconcile is the next M3 package.
- 2026-09-08 · M3 · Trading-wallet signer boundary, transaction codec and pre-sign validation (D47, D55, §15.4 steps 1–4 and 7, §15.7A, ADR-0007/0008 shape set, ADR-0009 P7 build-time half): `TradingWalletSigner` interface with `SignerHealth`, `SigningRequest` (message hash bound) and `SignatureResult` in contracts; `SoftwareDevSigner` in `libs/execution` over a throwaway PKCS#8 Ed25519 key that refuses to exist for live capability on mainnet and returns the identical, deduplicated signature for a retried identical message (§15.4 deterministic retry); an SDK-free legacy/v0 transaction codec (exact round trip, property-tested) so the executor decides what it signs without a router or wallet package; `checkTransactionStructure` (wallet is fee payer and sole signer, every program static and on the allowed set, a program behind a lookup table rejected as the signer policy would, lookup tables only where the Release allows, no System transfer to an unapproved recipient, nothing pre-signed by others) and `checkOrderAgainstAuthorization` (pair, amount ceiling, slippage, impact, minimum-output floor, taker, quote age and expiry, block height, authorization expiry, transaction bytes hash to what the build reported). Known program ids pinned in `validate/programs.ts`. Independent simulation and semantic balance-delta assertions (P7 landing-time half), the live adapter and the Turnkey adapter follow.
- 2026-09-08 · M3 · Execution-service authority, caps and durable journal (D12, D21, D33, D51, §13.7, §15.3, §15.10, §26.2; ADR-0009 P3/P4; INV-01, INV-04, INV-05, INV-06, INV-08, INV-09, INV-10 now mapped): `authority/verify.ts` verifies the risk-authorizer envelope only against key ids pinned in the deployment guardrails, recomputes the intent hash, checks expiry and the durable nonce ledger, proves the stored `trading.intents` row still says what was signed (any edited bound field is DB_TAMPER_DETECTED), binds a LIVE_APPROVAL grant to the exact authorization hash, and reads the mode gate last (`authority/mode-gate.ts`: pause, local pause, activity, authority, live capability; risk reduction never gated); `caps/exposure-ledger.ts` is the append-only `ExecutorExposureLedger` (authorized notional until confirmed, then the larger of notional and cost basis; signer-dependent MONITORED_EXIT exposure tracked separately; replayable) and `caps/check.ts` applies the §26.2 deployment caps with mark-to-market escalation only ever stricter and the D33 signer-outage cap; `journal/journal.ts` is the hash-chained JSON-lines journal written before any submission, with a fencing token that refuses a second writer and a stale holder, hash re-derivation on load, unresolved-attempt recovery and a used-nonce set; `idempotency/intents.ts` rebuilds the intent registry from the journal so a redelivery after a crash never creates a second entry. `libs/risk` gains the signer-outage cap policy. The executor imports no risk lib (boundary); the signer, transaction validator and submission path are the next M3 packages.
- 2026-09-08 · M3 · Risk-authorizer core, first Track A package while Track B waits on the market (D21, D45, D52, §13.7, §15.3, §15.5; INV-02, INV-03, INV-07, INV-14, INV-17, INV-18, INV-28 now mapped): `release/verify.ts` recomputes the Release digest from its binding and requires a verified, trusted, unexpired, purpose-correct attestation (LIVE_AUTO needs versioned skill/guideline/automation sets); `projection/verify.ts` accepts a signed `RiskStateProjection` only from a pinned projector key with an advancing sequence, fresh timestamp, matching Release and policy, and independent chain reads it may not exceed (settlement, gas, per-custody, slot); `authorize/authorize.ts` then requires a CLEARED CONFIRM cycle at its latest cutoff proposing ENTER/ADD, the proposal bound to it, eligibility from the projection, the capital-attestation ceiling, D45 mint/freeze authority absent on chain for live authority, the authorizer's own read of the session gate, and recomputes the maximum amount with the deterministic risk core using its own append-only `AuthorizationLedger` for pending exposure (P1); it signs exactly the bound action with an intent hash, nonce and expiry, and never authorizes a cycle twice. Contracts: `OpenLotSummary.costBasisBaseUnits` for the per-token cap; `libs/risk` `capitalAttestationVerdict`. Tests cover tampered payloads, unpinned keys, sequence rollback, stale projections, wider-than-chain balances, unattested or retired releases, uncleared cycles and double authorization. The process wiring (DB reads, RPC reads, internal API) is the next M3 package.
- 2026-09-08 · M5a · Evidence record `docs/probes/m5a-pipeline-evidence-2026-09-08.md`: gate-item status (STARTING warm-up, reconstruction, parity and fill-model items pass; first attended paper trades still open), the three M4 defects the session gate exposed, hosted state and the Lite budget limit.
- 2026-09-08 · M5a · SOL relative strength and own-fill wiring (§9.1, §8.6, D26, INV-11): `REFERENCE_SERIES_MINTS` (wrapped SOL) in contracts; the ingest and feature roles keep reference mints tracked and featured whatever their eligibility (being a reference never makes a mint tradable), and the candidates role reads SOL's 1h return from its latest feature snapshot (`latestFeatureValueByMint`, ten-minute window) so the trigger's relative-strength condition now has evidence. The self-influence guard receives real own fills through `listRecentOwnFills`, LIVE accounts only: paper fills never reach the chain and are deliberately excluded.
- 2026-09-08 · M5a · Level B capture started (§18.1 captured-market replay, §17.1–17.2, D48): migration `20260908002000_quote_probes.sql` adds the immutable `market.quote_probes` table (purpose ENTRY_REFERENCE / DECISION / EXECUTABLE / EXIT_MARK, the quote fields, the moment taken, and the cycle, intent, position and attempt it served; pgTAP suite 13); `QuoteProbe` contract with `quoteProbeOf`; `libs/db` `insertQuoteProbes` / `listQuoteProbesForCycle`. The paper-entry role captures the risk reference quote and the adapter's decision and executable quotes; the position monitor captures every exit mark and each exit's decision and executable quotes. Capture is best effort and never blocks a decision. Candidate snapshots, evidence first-seen times and eligibility probes were already persisted, so M10 replay now has every contemporaneous input the blueprint names.
- 2026-09-08 · M4 fix · Eligibility feed health survives restarts and only judges classes it actually called: the in-process health state is seeded from the persisted `ops.provider_health` rows (`restoredEligibilityHealthState`, `loadFeedHealth`), and a class with no call this cycle and nothing known publishes NO_DEMAND with no effect instead of FAILED/BLOCK. Before this, a restart followed by a cycle that touched no Birdeye class left TOKEN_OVERVIEW/TOKEN_SECURITY blocking entries for up to the six-hour re-evaluation window, which kept the session in STARTING (found by the M5a session role).
- 2026-09-08 · M4 fix · A mint account absent from chain is recorded as a BLOCKED hard reject (`MINT_NOT_INITIALIZED`, re-read on the slow cadence) instead of being retried at the head of every eligibility batch: the retry loop starved the batch and, after a restart, left TOKEN_OVERVIEW/TOKEN_SECURITY with no success at all, so the feed classes stayed FAILED and the session cold-start gate FEEDS_FRESH could never pass (found by the M5a session role). The earlier test that asserted "an error, not a record" now asserts the BLOCKED record.
- 2026-09-08 · M4 fix · Candle ingestion starvation under the Lite budget (found by the M5a session cold-start gate: WARMUP_SUFFICIENT never passed because 1m candles for every eligible asset had stopped hours earlier while CANDLES reported HEALTHY). Cause: the ingest planner ordered same-priority assets by id, so with ~260 CU per 5-minute cycle the same few dead tokens (whose fetches add nothing) took every slot and eligible assets were deferred forever. Fix: the planner serves the stalest asset first within a priority (`libs/market/src/ingest/plan.ts`), the worker backs off an asset whose candle fetch wrote nothing (doubling from the cycle interval, capped at six hours; positions never backed off) and reports `candlesBackedOff`, and tracked assets rank held, then ELIGIBLE, then newest so a capped list never drops the strategies' universe. Verified offline: the provider returned 211 fresh 1m candles for an eligible asset the worker had not requested since 19:14Z. Budget note for the operator: on Lite, continuous 1m candles for ~20 eligible assets cost about 3× the monthly allowance; features will be warm only shortly after each asset's refresh until the tracked set is narrowed or the tier raised.
- 2026-09-08 · M5a · Thin operational UI (§20.1, §20.2, §20.4, §20.19, §20.21): status bar chips for equity, exposure and day P&L from the paper account's portfolio snapshots (absent until the first snapshot, never zero); Control Room session widget (activity/authority/pause/presence chips, D63 cold-start gate table while STARTING, wind-down blockers, last transitions, START SESSION request), paper-book summary (equity, exposure, unrealized, drawdown), open positions, action-cycle counts per strategy over 24 h, open alerts and pending control requests; Positions page (open with marks, tighten-only stop, safety and review state; recently closed with realized P&L); System Health page (provider feeds with effect on entries, worker role leases with heartbeats, LIVE custody reconciliation, contract digest). All reads are RLS-scoped projections through the existing viewer policy; the browser writes only control requests.
- 2026-09-08 · M5a · Minimal runtime session (D2, D60–D63, §20.21, §21.2B, §21.3, §23.3): `SessionPolicy` (`session-v1`) and named D63 cold-start gates in contracts; pure gate evaluation and attended-presence rules in `libs/risk/src/runtime-session/cold-start.ts` (property: STARTING cannot leave while any fact is unhealthy); `libs/db` session repository (open session, gate results, presence heartbeat, stored cold-start facts, wind-down facts, control-request queue with audited resolutions, step-up check); worker role `session` (`SESSION_INTERVAL_MS`, `SESSION_AUTOSTART`): acts on operator START/END/PAUSE requests and RESUME only with a verified step-up assertion, drives STARTING → WATCH on all gates, WATCH ↔ ACTIVE on presence, WIND_DOWN → OFF only with zero open lots and nothing in flight. Entries now require a running, unpaused session (`SESSION_NOT_ACTIVE` risk reason) and WIND_DOWN closes every paper lot through the position monitor. Operator tools `tools/session-presence.mjs` (heartbeat) and `tools/session-control.mjs` (start/end/pause requests).
- 2026-09-08 · M5a · Position monitor MONITORED_EXIT and portfolio snapshots with marks (§13.4–13.5, §11.11, §17.2, D31, D39, D44, INV-22): worker role `position-monitor` (`POSITION_MONITOR_INTERVAL_MS`) takes a real exit quote at the full quantity as the executable mark, stores unrealized P&L, and applies the deterministic exit policy (hard stop, time stop, target, partial tier, trailing stop that only tightens; candle high since open as the trail reference); a held asset in CRITICAL_EXIT or EXIT_RECOMMENDED exits unconditionally. Every exit is a position action cycle (deterministic EXIT/REDUCE proposal, non-blocking deterministic review, risk evaluation), a lot-scoped SELL intent under a fresh idempotency key, a paper attempt and fill, and final accounting on FINALIZED only (`libs/db` positions repository: lots reduced by exactly the named quantities, realized P&L pro rata, sleeve committed released, position closed at zero). Paper equity and exposure now use marks (`PaperBook.markValue`, realized P&L per sleeve). Fill model refuses an executable quote for any amount other than the intent's (`QUOTE_AMOUNT_MISMATCH`); the scripted parity quote client now quotes the requested amount at the requested slippage like the provider.
- 2026-09-08 · M5a · Paper book and the first paper-trade path (§13, §17, §6.11–6.20, D9, D24, D44; ADR-0009 P1): migration `20260908001900_account_mode.sql` (`trading.accounts.mode` LIVE|PAPER, immutable; a PAPER account can never carry custody rows; pgTAP suite 12) and chain reconciliation now lists LIVE accounts only; `libs/db` paper repository (paper account and per-strategy sleeves, cleared cycles awaiting entry with their point-in-time inputs, the book: settlement balance, exposure at cost, pending/in-flight exposure, fees, drawdown references; immutable risk evaluation linked once per cycle; intents, journaled attempts, fills, positions with sleeve lots, portfolio snapshots); worker role `paper-entry` (`PAPER_TRADING_WALLET`, `PAPER_STARTING_CAPITAL_BASE_UNITS`, `PAPER_ENTRY_INTERVAL_MS`): every CLEARED S0 cycle gets exactly one deterministic risk evaluation from the live book; an allowed one becomes an intent executed through the paper adapter, and a fill opens the position and its MONITORED_EXIT lot. Paper equity marks open positions at cost until the position monitor supplies marks.
- 2026-09-08 · M5a · Paper execution adapter with an explicit fill model and the paper/live parity suite (§17.1–17.4, §18.4, D12, D48, D49; plan M5a additions): `PaperFillPolicy` (`paper-fill-v1`: modelled submission/confirmation/finalization delays, adverse-execution allowance per execution path, fee assumptions) in contracts; `libs/execution/src/adapter`: pure `modelPaperFill` (decision quote → real executable quote after the modelled delay → live pre-submit chain: intent expiry, decision-quote age, route, impact, chase → allowance applied once → below executable minimum = NOT_LANDED; shortfall measured against the decision quote separately from the modelled attribution), `PaperExecutionAdapter` over the shared order-attempt and intent-registry machines (pseudo-sign, journal before SUBMITTED, modelled CONFIRMED/FINALIZED timestamps, PAPER authority only, idempotent redelivery), and `parity.ts`: an eleven-scenario table the live adapter must pass over the same inputs in M7. Property tests: fill ≤ executable expected, ≥ executable minimum, independent of the decision price. CI clock-discipline canary retargeted to `libs/strategies/src/s0/gate.ts`; strategies integration spec isolated from the candidates spec expiry sweep.
- 2026-09-08 · M5a · S0 baseline as action cycles (§12.1, §6.10D, §6.11, §6.21; D30 for T0_FAST; D7): `S0SafetyGatePolicy` (`s0-gate-v1`) and the two strategy version ids in contracts; `libs/strategies/src/s0` with the deterministic proposer (typed ENTER, no amount, no destination), the independent counter-signal/safety gate (staleness, missing inputs fail closed, self-influence, sell-route/impact/liquidity exit viability, 1h over-extension, RSI exhaustion, volume anomaly, regime, session) and `decideS0`, which runs the shared action-cycle machine and records the gate as the adversary: SAFE rejects on any objection and never clears; RAW records the same objections as a non-blocking review and clears, bounded to OBSERVE/PAPER by its strategy version. Persistence in `libs/db` (`ensureStrategyVersion`, point-in-time candidate+snapshot listing, atomic cycle+proposal+review write with the candidate moved by the SAFE outcome, DETECTED-only guard against double decisions). Worker role `s0` (`S0_INTERVAL_MS`, `GIT_SHA` on registered versions). Property test: identical stored inputs reproduce the identical cycle/proposal/review (exit-gate reconstruction). 20 projects green.
- 2026-09-08 · M5a · Deterministic risk core (§13.1–13.6; §31 "risk sizing deterministic"; INV-02 mapped; ADR-0007 unknown cohort = most restrictive; ADR-0009 P1 pending exposure counted; D39 tighten-only stops): versioned `RiskPolicy` (`risk-v1`, `libs/contracts/src/policy/risk.ts`, reason-code list `RISK_REASONS`); §13.3 sizing in bigint base units with the binding constraint named (`libs/risk/src/sizing/size.ts`); pure entry evaluation producing an immutable `RiskEvaluation` (`libs/risk/src/rules/entry.ts`: kill conditions first, portfolio caps, trade-level rules incl. quote age/impact/slippage/chase/Token-2022/reward-to-risk, single in-flight authorization, pending exposure spent before sizing); stop models with the percentage cap and take-profit/trailing/time exits that never loosen a stop (`libs/risk/src/exits/policy.ts`). Property tests: size ≤ every cap and loss-at-stop ≤ risk budget; stop distance ≤ cap; trailing stop monotone. Contracts lock regenerated; 20 projects green.
- 2026-09-08 · M5a · Momentum-continuation trigger and candidate detection (§9.1, §9.7, §6.9, §8.6; INV-03, INV-11): versioned `MomentumTriggerPolicy` (return, relative volume, EMA trend, ATR extension, RSI exhaustion, liquidity, executability, optional SOL relative strength; explainable score with a floor for passing every gate); pure `detectMomentumCandidate` — cold required feature = no scoring (D63), dedupe window and cooldown as silent skips, a fired trigger refused by the entry gate or the self-influence guard is recorded as a REJECTED candidate with its reason so filter value stays measurable; candidates repo (scan inputs join the latest vector with the latest eligibility record for ELIGIBLE assets, open/terminal lookups, expiry TICK) + integration test; worker role `candidates` (`CANDIDATES_INTERVAL_MS`; own-fill suppression wired with an empty fill source until the paper book exists); INV-03 and INV-11 gain the detector and role specs · contracts 59, signals 15, db 29, worker 45 green
- 2026-09-08 · M5a · Foundation package: canonical base-unit arithmetic in contracts (`libs/contracts/src/money`: bigint base units, named rounding per operation, exact decimal↔base-unit conversion by string arithmetic, pro-rata allocation that conserves the total, u64 ceiling; property tests — ADR-0009 P6); versioned feature engine (`FEATURE_ENGINE_V1` spec with per-indicator lookbacks and the indicators required for scoring; `libs/signals/src/features`: point-in-time over contiguous closed 1m candles, gaps truncate, every indicator null until its lookback exists, §8.1 momentum/volatility/RSI/EMA/MACD/Bollinger/VWAP/breakout/retest/persistence/drawdown/anatomy, §8.2 relative volume/acceleration/trade size/divergence, §8.3 liquidity and probe features from the latest eligibility record; `warmupStatus` for D63; D62 session labels); feature-snapshot repo + integration test; worker role `features` (one vector per eligible/evaluating asset per closed minute, linked to the market snapshot, `FEATURES_INTERVAL_MS`) · contracts 59, signals 10, db 28, worker 41 green
- 2026-09-08 · plan · Operator review of plan v4 applied (`docs/reviews/review-plan-v4-external-recommendations-2026-09-07.md`, verified against the repo, narrowed by the operator's self-review): ADR-0007 Profile 2 restricted capability set + `S0_LIVE_SAFETY` M8a dependency + shared chain-foundation ownership; ADR-0008 external signer policy mandatory for the Profile 2 shape set, Probe A split four ways (supersedes ADR-0004's "preferred" row); ADR-0009 financial-boundary properties P1–P7 with owners (single-writer shape for P1, audit-chain route for P2, no epochs); ADR-0010 applicability-based readiness (invariant map gains required_by/applicable_*/evidence, checker computes coverage per profile: P2+LIVE_SIGNING = 21 applicable, 11 mapped today; INV-01/05/12 wording fixed; `tools/check-bypass.mjs` disables five protections in turn and requires the mapped tests to fail, wired into CI; M8a target-environment drills, `ReadinessRow` binding, severity wording); ADR-0011 freshness from the strategy speed tier (`FreshnessRequirements` policy, T1_STANDARD default; provider tier sizes budgets only); ADR-0001 amendment (all unsuccessful outcomes normalise to protection, BUDGET_PAUSED equivalence tests, lot-scoped review behind SINGLE_SLEEVE_PER_MINT); plan text: header state, M3/M5a/M5b/M7/M8a/MP/M9/M10/M11/M12 edits, Probe D (Trigger auth), probe authorization boundary; hosted migration history repaired (18 versions, schema verified equivalent first); costs basis + projected steady state; chain-library capability-boundary test · 20 projects green
- 2026-09-07 · M4 · **M4 closed.** Operator upgraded Birdeye to Lite; security data flows and two defects it exposed are fixed: Birdeye holder shares are fractions (normaliser now converts 0.5367 → 53.67 %, spec case), and raw largest-account concentration counted pool vaults/staking PDAs as holders — `readMintChainState` now classifies the largest accounts' owners (two `getMultipleAccounts` reads; an owner with no account or a program-owned owner is program-controlled) and reports holder-only top-N plus `programControlledFraction`/`excludedAccounts` (contract fields, lock regenerated); an analytics concentration disagreement is graded (`ANALYTICS_CONCENTRATION_DISAGREES`) while security-flag contradictions stay `CHAIN_ANALYTICS_MISMATCH`; `ELIGIBILITY_REEVALUATE_AFTER_MS` / `ELIGIBILITY_BLOCKED_REEVALUATE_AFTER_MS` env knobs; ten majors seeded as a MANUAL watchlist; bot wallet paused via `tracking_active`. Live: 9 ELIGIBLE (USELESS 100, ALON 100, TOESCOIN 100, ANTFUN 90, PENGU 85, …) of 104 assets at 19:19Z, `BIRDEYE:TOKEN_SECURITY` HEALTHY. Exit gate walked in `docs/probes/m4-exit-gate-2026-09-07.md` (all items pass), run-rate USD 39/month in `docs/costs.md`, plan §8 ticked · 20 projects, 106 pgTAP green
- 2026-09-07 · M4 · Interim adversarial review applied (`docs/reviews/review-m4-interim-market-chain-eligibility.md`, fresh session; 2 HIGH, 9 MEDIUM, 11 LOW): signature pagination forwards `before` (a funded wallet could never reconcile), an unmeasurable primary route is `PRIMARY_ROUTE_UNKNOWN`/`EXIT_PATH_UNVERIFIED` and never a mandatory exit, the safety baseline is the entry baseline for the position's life, BLOCKED assets re-evaluate daily, token age and unknown authority are hard rejects with `minGrade` at the entry gate, wallet-event ingest refuses every owned address at the database (triggers register trading wallets and custody as owned), the reconciliation cursor never passes an unparsed signature, the CU ledger syncs to the shared store total, `Amount.safeParse` never throws, health counts accepted data, one Jupiter and one RPC client per process (reconciliation at `finalized`), immutability on emergency snapshots and owned addresses; fix-status table in the review · 20 projects, 106 pgTAP green
- 2026-09-07 · M4 · Feed health made honest and per-wallet tracking switch: ingestion publishes only the classes it serves and marks classes with no consumer (no positions, no candidates, no role) as FAILED with no effect (`NO_DEMAND`), the eligibility role publishes TOKEN_SECURITY/TOKEN_OVERVIEW from its own calls (a 401 on security is FAILED and blocks entries); token-for-token swaps derive a SELL plus a BUY; chain reads moved to the Helius RPC locally (chain-derived top-10 concentration now populates: SCRIB 0.93, TRONK 1.0 → TOP10_CONCENTRATION_ABOVE_MAX); migration 001800 adds `intelligence.wallets.tracking_active` so a bot wallet (≈300 events / 2 min observed) can be paused without touching immutable history · 20 projects green
- 2026-09-07 · M4 · Tracked-wallet event stream (§3.2, §6.7, §9.3; D8, D26): `WalletEvent` contract with two clocks (`blockTime` vs `firstSeenAt`), Helius `transactionHistory` (Parsed Events, cursor by `afterSignature`, newest page on first sight), pure `deriveWalletEvents` (BUY/SELL from base-vs-quote flows, transfers otherwise, failed transactions and OWNED wallets yield nothing — property-tested, added to INV-11), migration 001700 (`intelligence.wallet_events` append-only + unique per wallet/signature/movement/kind, `wallet_cursors`, `intelligence.ingest_wallet_events()` idempotent and refusing owned or untracked wallets), pgTAP suite 11, wallet-events repo + integration test, worker role `tracked-wallets` (`TRACKED_WALLETS_INTERVAL_MS` default 120 s; owned wallets skipped from the row flag or the owned-address registry); webhook delivery reuses the same parser and deriver once a public receiver exists (hosted profiles) · pgTAP 105
- 2026-09-07 · M4 · Birdeye compute-unit ledger persisted (§21.1, D43): `ops.provider_spend` per provider and calendar month with atomic `ops.charge_provider_spend()` (migration 001600, pgTAP suite 10), `ComputeUnitLedger.restore()` + charge sink, worker restores the month's spend at start (`birdeye_ledger_restored`) and writes every charge through in order, so a restart loop can no longer reset the purchased allowance · pgTAP 97, market 26, db 32 green
- 2026-09-07 · M4 · Chain/custody reconciliation + owned-address registry (D9, D26, §3.2, §8.6, §13.6): `OwnedAddress`, `ChainMovement`, `ChainTransactionFacts`, `CustodyReconciliation` contracts and the versioned `ReconciliationPolicy`/`SelfInfluencePolicy`; read-only RPC gains `getBalance`, `getTokenAccountsByOwner`, `getSignaturesForAddress`; `libs/onchain` (Helius Parsed Events client over an injected transport with the key only in the query string and redacted from errors, tolerant parser for parsed-events and legacy enhanced payloads, `OwnedAddressRegistry`, pure `reconcileCustody`: ledger vs chain per mint, unexpected/unregistered token accounts, unknown movements via the execution custody classifier, unparsed signatures and backlog never clean, SOL accounted from the previous baseline through fees/transfers with a rent tolerance, UNAVAILABLE without pause on chain-read failure); `libs/signals` self-influence guard (own flows excluded, own signature or owned wallet as evidence rejected, impact-sized suppression window for aggregate-metric triggers); migration 001500 (owned_addresses cluster/account, append-only `trading.custody_reconciliations` + `custody_movements`, `reconciliation_cursors`, `trading.record_reconciliation()` pausing running sessions + CRITICAL notification + audit row in one transaction), pgTAP suite 09; reconciliation repo + integration test; worker role `reconciliation` (registers the wallet and custody rows as owned, `RECONCILIATION_INTERVAL_MS` default 60 s, `HELIUS_API_KEY` optional: absent means every wallet transaction pauses); INV-11 mapped (14/28) · 91 pgTAP, onchain 13, signals 5, worker 33, db 25 green, lock regenerated · live 2026-09-07 with the operator's free Helius key: 25 mainnet signatures on a Meteora DLMM pool parsed, `transactionStatus` OK/ERROR agreed with the RPC `err` flag on every sample, 6–7 transfers per swap; Parsed Events v1 returns no `accountData`, so SOL is accounted from fee + native transfers under the 0.005 SOL rent tolerance; worker role runs on the hosted stack after migration 001500 (zero trading accounts yet)
- 2026-09-07 · M4 · Held-asset safety + independent exit compatibility (§7.5, D34): `HeldAssetSafety`/`ExitCompatibility`/`SafetyBaseline` contracts with reason codes graded CRITICAL/EXIT_RECOMMENDED/DEGRADED, versioned `SafetyPolicy` (`safety-v1`), pure `evaluateHeldAssetSafety` whose exit-compatibility computation takes no eligibility input by construction (spec proves an asset refused for entry still reports `canReduceNow`), migration 001400 (`trading.position_safety_evaluations` append-only + `trading.record_position_safety()` moving the `positions.safety_state` pointer in one transaction, closed positions refused), pgTAP suite 08, safety repo + integration test, worker role `held-asset-safety` (chain re-read, security/overview refresh at CRITICAL ledger priority, whole-position sell quote through the shared Jupiter client, emergency-pool owner re-verification, baseline from previous safety → entry eligibility → first sighting; `HELD_ASSET_SAFETY_INTERVAL_MS` default 60 s), INV-21 gains the safety module · 80 pgTAP, risk 55, worker 27, db 24 green, lock regenerated
- 2026-09-07 · M4 · Route probes + emergency exit routes (P2 slice, ADR-0003 honoured): the one shared Jupiter quote client now exists in `libs/execution` (`JupiterSwapClient` implementing the contract's `JupiterQuoteClient`; `/swap/v1/quote` on the keyless lite host or the keyed host, paced and retried, no-route errors typed, quote-only `buildOrder` until M3) with price impact measured as the sized rate's shortfall against a 1/100 reference quote rather than trusting the provider's `priceImpactPct`; contracts gain `QuoteRouteHop`/`QuoteRoutePlan`/`QuoteOptions` and `settlementMints` in the eligibility policy; `runRouteProbes` buys at every standard size (impact) and confirms the sell route back to USDC then SOL at the largest routed size; `discoverEmergencyRoute` restricts the router to the five §14.6 direct-pool families and single hops, quotes capacity through the same pool at every size, and the worker verifies the pool account's owner program on chain before persisting the §6.2 snapshot (`core.emergency_exit_route_snapshots`, pool state ref = owner + sha256 of account data); engine records the snapshot id; worker `eligibility` role runs probes → discovery → engine; tests: recorded Jupiter fixtures, fake quote client for probes/discovery, role spec proving ELIGIBLE end to end with a chain-verified snapshot, NO_EXIT_ROUTE when a token cannot be sold back, and no snapshot when the pool owner is not the claimed program · execution 28 tests, worker 22, db 30 · first live cycle at 1 quote/s on the keyed host: routes confirmed for all five tokens with measured impact (e.g. GME 6/18/87 bps at $250/$1k/$5k), three Meteora DLMM emergency routes persisted after on-chain owner verification at slots 445102064–445102449, OTC's capacity 293/927/2661 bps showing why the snapshot exists; NO_ROUTES_FOUND and gateway 429 at 10 rps observed and handled (`JUPITER_REQUESTS_PER_SECOND`, default 1)
- 2026-09-07 · M4 · Chain truth + eligibility engine (P2 slice): `libs/solana-hard-state` now real — dependency-free base58, read-only JSON-RPC client with an origin allowlist and a closed method set (getAccountInfo, getTokenSupply, getTokenLargestAccounts, getSlot), SPL Token and Token-2022 mint decoding straight from account bytes (TLV extensions: transfer fee with the conservative max of the two schedules, transfer hook, permanent delegate, default-frozen, non-transferable, mint close authority, pausable, unknown types reported), `readMintChainState` with chain-derived top-1/5/10/20 concentration at the minimum slot seen; contracts `MintChainState`, `TokenSecurityReport` (analytics corroboration only), `EligibilityPolicy` (versioned thresholds, `DEFAULT_ELIGIBILITY_POLICY` = eligibility-v1) and `EligibilityReason` codes; `libs/risk` deterministic `evaluateEligibility` (chain truth authoritative, D45 mismatch blocks, stale/missing security fails closed, liquidity floor and top-10 cap hard, route probe required, missing data → EVALUATING never ELIGIBLE, hard rejects → BLOCKED with grade 0, soft findings grade only) and the INV-03 entry gate `entryAllowed` (fresh, eligible, non-hard-rejected, same policy version) — INV-03 mapped (13/28); Birdeye `token_security` adapter; `libs/db/server` eligibility repository (append-only record + status pointer in one transaction, evaluation queue); worker role `eligibility` under its own lease sharing the Birdeye CU ledger with ingestion; `SOLANA_RPC_URL`, `ELIGIBILITY_INTERVAL_MS` in worker env, manifests and examples; RPC client paces to a configured rate and retries 429/5xx; when an endpoint refuses `getTokenLargestAccounts` (the public mainnet RPC does) concentration is recorded as unavailable, never zero, and the asset stays EVALUATING · first live cycle against mainnet slot 445097868: JitoSOL and ZEC BLOCKED on chain-read mint authority, wSOL BLOCKED on the native mint's zero supply field, PUMP (Token-2022, no hook/delegate) and ANTFUN EVALUATING pending security corroboration (Birdeye Standard excludes `token_security`), concentration (needs an RPC that serves the method, e.g. Helius) and route probes · no migration; route probes and emergency route snapshots are the next package
- 2026-09-06 · M4 · Market-data foundation (P1 slice, Track B): contracts for market-data providers (`DataClass`, `FreshnessContract`, `FeedHealth`, `DiscoveredToken`, `PriceQuote`, `TokenOverview`, `CandleGap`, `RetentionRule`, `ProviderTier`; nullable everywhere a provider may omit a value, never zero); `libs/market`: Birdeye REST adapter over an injected transport with token-bucket pacing to the purchased tier, a monthly compute-unit ledger with a CRITICAL reserve for position prices, bounded retry honouring Retry-After, documented-shape validation and normalisers that reject zero/inconsistent/misaligned candles with reasons; Jupiter Price V3 secondary price adapter (keyless lite host or `x-api-key`), gap detection + backfill planning (property-tested), D63 LIVE/BACKFILL tagging, freshness→health evaluation with tier-sized default contracts (a never-succeeded feed is FAILED and blocks entries), discovery merge keyed by mint, point-in-time snapshot builder (returns, ATR, realized volatility, relative volume; closed candles only; nulls for missing history), risk-first ingest planner cut to CU/request budgets; `libs/db/server` market repository (first_observed_at never moves, closed candles immutable, open bucket replaceable by a newer observation only, BACKFILL never overwrites LIVE, snapshots/feed health) with integration tests; migration 001300 `ops.retention_policies` seeded per §25, `market.prune_candles()`, monthly partition maintenance, both scheduled with pg_cron, pgTAP suite 07; worker role `market-ingest` under a lease (discovery → assets, candle continuity, snapshots, provider health) with tests against an in-memory repo and scripted Birdeye; `BIRDEYE_API_KEY`/`BIRDEYE_TIER`/`JUPITER_API_KEY`/`WORKER_ROLES`/`MARKET_INGEST_INTERVAL_MS` in the worker env and manifests; Birdeye/Jupiter tiers and CU costs recorded in `docs/costs.md` · lock 228 schemas, pgTAP 73, market 29 tests, worker 17, db 19
- 2026-09-06 · M2 · Review fixes (interim adversarial review `docs/reviews/review-m2-interim-containers-and-step-up.md`, PASS WITH REQUIRED FIXES): R2-01 only the first passkey with TOTP alone, and only with a TOTP verified ≤5 min ago (`ops.session_recent_totp` on the `amr` claim), later passkeys need an existing-passkey assertion, first passkey cools 1 h (`usable_from`, `PASSKEY_COOLING`), every registration raises a CRITICAL notification by trigger; R2-04 `ops.consume_step_up_challenge()` consumes + records atomically (service role only), `begin_step_up` no longer deletes; R2-02 all three entrypoints validate their environment fail-closed before telemetry (`env_invalid`, names only) and `--print-digest` serves the D50 checks, CI proves each service refuses a foreign credential without echoing it; R2-03/05/16/17 artifact scan gains liveness canary (path comments present, `minify` off in every configuration), allowlist for risk-authorizer/execution-service (`tools/artifact-allowlist.json`), zero-or-more wildcards, `__require`/dynamic-require detection, one shared ban list (`tools/forbidden-packages.mjs`) for ESLint and the scan with extra LLM SDK names; R2-06 `safeNext` guards every post-auth redirect (proxy, sign-in, MFA) with property tests; R2-08 registration positive path tested against a software `webauthn.create` ceremony and round-tripped into an assertion; R2-09 `boundPayload`; R2-10 `policy.StepUpPolicy` in the digest; R2-11 length bounds + `stepUpAssertionRef: Uuid`; R2-12 `.dockerignore` patterns at any depth; R2-13 explicit tag push; R2-14 audit pgTAP scoped to its own rows; R2-15 plan evidence names corrected; R2-18 `/api/*` exemption removed; ADR-0006 amended; migration 001200 · lock regenerated
- 2026-09-06 · M2 · **M2 closed.** Exit gate met (see plan §8 evidence); run-rate 0/month recorded in `docs/costs.md` with provider tiers; M2 is not a review gate (next review #1 at M3). Operator applied migrations 001000/001100 to the hosted project and disabled email sign-up (2026-09-06) · CI green at `93c3fb1`
- 2026-09-06 · M2 · Auth + step-up substrate (ADR-0006): Supabase TOTP MFA enabled (local config; hosted default) and every browser control write now requires an aal2 session in RLS (`ops.has_aal2()`); sign-in routes through `/sign-in/mfa` until the factor is verified; Settings → Operator Security with TOTP enrolment/removal and passkey list; Supabase native passkeys rejected as experimental per §5.7, SimpleWebAuthn v14 selected and pinned in the worker; migrations 001000/001100 add `REGISTER_PASSKEY`/`REVOKE_PASSKEY`, `ops.operator_passkeys`, `ops.step_up_challenges` (issued by `ops.begin_step_up()` with server-side randomness, bound to one exact request via canonical hash, 5-minute life, rate-limited), immutable `ops.step_up_assertions` (worker-written only); contracts gain `ControlRequestKind/State`, `ControlRequest`, `OperatorPasskey`, `StepUpChallenge`, `StepUpEvidence`, `StepUpAssertion` and the D41 policy table (`STEP_UP_POLICY`, `stepUpRequired`, `stepUpBindingHash`) with property tests pinning the fast set; worker `verifyStepUp`/`verifyPasskeyRegistration` tested against a software ES256 authenticator (genuine assertion accepted; wrong key, wrong challenge, foreign origin/RP/type, missing UV, non-advancing counter, changed payload/kind, consumed/expired/foreign challenge, unknown/revoked/other-operator passkey and credential substitution all rejected); pgTAP suite 06 (aal1 blocked, aal2 fast path, NO_PASSKEY, challenge issuance, browser cannot write passkeys/challenges/assertions, stranger isolation, assertion immutability) · lock regenerated
- 2026-09-06 · M2 · Containers + artifact checks: the three Node services build as self-contained esbuild bundles (one `main.js`, no node_modules at runtime); `deploy/Dockerfile` (one recipe, `SERVICE` build-arg, pinned `node:24.20.0-alpine`, non-root, refuses to build if the bundle fails the scan); `tools/check-artifacts.mjs` scans built bundles for banned packages per trust level (mirrors the ESLint ban lists) and for any bare third-party require (self-containment), with planted-marker negative tests in CI; `tools/startup-digest.mjs`; `deploy/profile-0/docker-compose.yml` runs Profile 0/1A with one credential file per service, read-only root, no capabilities, executor journal volume; `.github/workflows/images.yml` builds all three images, starts each without credentials and fails unless every image reports the locked contract digest (D50), then pushes to GHCR with `GITHUB_TOKEN` only; CI also runs each bundle standalone against the lock · scan green on all three bundles, 3 negative cases rejected, compose config valid, local image build verified
- 2026-09-06 · M2 · Web skeleton on the D58 stack: project-owned oklch design tokens with separate authority/activity/paused channels and a live treatment on the shell; Supabase Auth via `@supabase/ssr` (anon key only, RLS-scoped reads, role from `ops.operators`), Next 16 `proxy.ts` session refresh + route protection (health stays public), email/password sign-in with no public sign-up; §20.1 shell with persistent status bar (activity, authority, PAUSED override, entries gate, attended/presence, feeds freshness, providers, db, equity/exposure/day P&L shown as absent not zero, alerts), §20.1 navigation, Base UI scope selector; PAUSE and END SESSION post `ops.control_requests` as the operator (sole browser write surface); Control Room placeholder reading profiles, pending requests and the contract digest; status labelling unit-tested; smoke-tested against the local stack: unauthenticated redirect, sign-in page, public health, authenticated Control Room with role · 20 projects green
- 2026-09-06 · M2 · Observability + realtime contract: `libs/observability` redaction by key name and value shape (Supabase keys, JWTs, OpenAI/Anthropic keys, PKCS#8 hex, base58 secret keys, DB-URL passwords, PEM blocks) with a fast-check property that no known secret survives wherever embedded; structured JSON logger with correlation context; `observability/server` initTelemetry (OpenTelemetry tracer, optional Sentry with redacting beforeSend, propagation headers); the three Node services start through it; `RealtimeEvent` + `realtimeChannel` contract for §20.23 broadcast events (ids only, never values); lock now 198 schemas · 20 projects green
- 2026-09-06 · M2 · Audit package: `libs/db/server` audit writer over the hash-chained ledger, `verifyAuditChain`, external checkpoint replication (`FileCheckpointReplicator` for the executor-local journal volume) with `checkpointAuditChain` refusing a broken chain and `verifyAgainstExternalCheckpoint` for the Audit Log "last verified checkpoint"; runtime-session repository persisting a machine-decided transition and its audit row in one transaction (mode changes audited); integration tests incl. rewrite detection when audit.checkpoints is also rewritten and rollback when the session is unknown · 16 db tests, 20 projects green
- 2026-09-06 · M2 · Config trust classes + deployment profiles: per-service env schemas in `libs/contracts/config` that fail closed on forbidden credentials (web refuses service-role/DB URL; worker refuses risk key/signer; risk-authorizer refuses service-role/signer/LLM/provider keys; executor refuses risk key and forbids SOFTWARE_DEV signer with live capability on mainnet), `ExecutorGuardrails` §26.2 schema, `DeploymentProfileManifest` + `checkManifest` rules (exclusive credentials, never-in-service, web-on-vercel, live-credentials-isolated, three-distinct-hosts); `config/profiles/P0–P4.json`; tests prove credential separation per profile and that the checker catches a leaked signer credential; `.env.example`; config schemas join the contract-set digest (196 schemas) · green
- 2026-09-06 · M2 · Queue consumer package: migration 000900 (ops.processed_messages exact-once claims, ops.dead_letters, lease functions acquire/heartbeat/release) + pgTAP lease suite; `libs/db/server` entry point (Postgres driver, web forbidden by lint): pgmq client, risk-first drain scheduler with starvation bound (property-tested), idempotent processor with transaction-scoped claim, exponential backoff via set_vt and dead-lettering incl. contract-digest mismatch, LeaseManager + runWithLease fencing, drainOnce; integration tests against local Supabase prove exactly-once on redelivery, crash-mid-handler recovery by another worker, retry→dead-letter, digest rejection, lease takeover; CI database job runs them · db + 20 projects green
- 2026-09-06 · M2 · Supabase schema authority: 8 SQL migrations for every §6 table across core/market/intelligence/signals/agents/trading/risk/research/ops/audit (enums mirroring contracts, u64 amount domain, immutability + versioned-artifact guards, hash-chained append-only audit with verify_chain(), monthly candle partitions, D23 LLM-cohort constraint, action-cycle target/reason constraints), four logged pgmq queues, RLS with ops.operators role resolution and ops.control_requests as the only browser write surface, Realtime broadcast read policy; pgTAP suites (audit chain + tamper detection, immutability, RLS from anon/stranger/operator, queues) green locally; generated `libs/db` types + queue mapping; CI job starts Supabase, runs pgTAP and checks generated types are in sync; local ports moved to 553xx (Windows reserves 54318–54517) · `supabase test db` PASS, 20 projects green
- 2026-09-05 · M1 · **M1 closed.** Adversarial review #0 re-verified fixes at `26c7820`: Gate PASS; residuals low and recorded (digest sees refinement presence not predicate body; no schema-level unresolvedReason⇔UNRESOLVED refine; approval binding leaves revokedAt to the executor). Operator to acknowledge: GUARDRAILS Part 4 tag-table edit (trust:risk/trust:execution) and the "contract-only in M1" reading of the shared Jupiter client bullet · plan §8 M1 ticked
- 2026-09-05 · M1 · Review #0 fixes: IGNORE forbidden on open-position cycles and action gated by target (candidate ENTER/IGNORE, position HOLD/REDUCE/EXIT/ADJUST_PROTECTION, ADD opt-in); position review requires an affirmative position action to become REVIEWED; order attempt recognises a transaction that landed after crash-before-persist and allows re-landing the same bytes; idempotency keys are permanent (redelivery after COMPLETED returns the existing intent); second CHALLENGE → REVISION_EXHAUSTED and dead cycle states removed; nested signed-payload objects strict with a deep injection test; digest fingerprint v2 sees strictness/refinements/check params; approval binding check + INV-10 test; EmergencyCommand cluster + mint refinement; Uuid lowercase; Amount u64-bounded; direction LONG; custody mint check; GUARDRAILS tag table gains trust:risk/trust:execution · 89 tests green, lock regenerated
- 2026-09-05 · M1 · State machines as pure code with fast-check property tests: action cycle (cutoff versions, one revision, UNRESOLVED reasons; ADR-0001), position review (PROTECTION_ONLY/BUDGET_PAUSED, tighten-only unreviewed stop), order attempt (persist-before-submit, staged confirmed/finalized, REORG_PENDING, proven-death retry), intent idempotency registry, position lots (conservation, same-mint isolation), custody movement classification, candidate lifecycle, runtime session (activity × authority × sticky PAUSED, D61 OFF guard, live-arming preconditions), mandatory-exit classifier (context-independent); cross-machine INV-20 test; INV-04, 05, 14, 15, 19, 20, 21, 22, 23 mapped (12/28) · 33 tests green
- 2026-09-05 · M1 · `/libs/contracts` foundation: primitives, enums, Clock, canonical JSON + Ed25519 signed envelopes (WebCrypto), every §6 entity and §6.14/6.14A/6.15/D25 envelope as strict Zod schemas, `ExecutionAdapter` + Jupiter client contract (ADR-0003), contract registry + contract-set digest with `contract-set.lock.json` (187 schemas), fixtures + cross-boundary roundtrip harness; digest embedded and logged by all four apps; §18.2 clock-discipline lint rule spread into clock-bound projects (negative test in CI); INV-06, INV-10, INV-24 mapped · 34 contract tests, `nx run-many -t lint typecheck test build` green
- 2026-09-05 · M1 · Nx 23.2 workspace scaffolded: 4 apps + 16 libs with trust/scope tags, `@nx/enforce-module-boundaries` constraints and package bans from GUARDRAILS.md Part 4 (negative tests: lib import and `react` import into risk-authorizer both rejected), ADR-0005 pinned versions (TS 6.0, Vitest 4.1, ESLint 9.39 after ESLint 10 broke `eslint-plugin-react`), zod 4 + fast-check 4 added · `nx run-many -t lint typecheck test build` green for 20 projects
- 2026-09-05 · M0 · Repo bootstrapped: git, docs layout, GUARDRAILS.md (§31/§32 verbatim + ground rules + trust tags) imported by CLAUDE.md and pointed to by AGENTS.md, ADR-0001–0004, invariant-test-map.yaml with 28 §24.6 invariants and checker, guardrails CI workflow, Node 24/pnpm pins · `node tools/check-invariant-map.mjs` OK
