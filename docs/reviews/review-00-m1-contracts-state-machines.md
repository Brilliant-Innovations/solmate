# Review #0 — M1 (contracts and state machines) — 2026-09-05

**Reviewer:** Claude (fresh subagent session, same model family as author; cross-model review still recommended)  **Author of reviewed work:** Claude Code session_01PpKPK8zanC3JVocsDgzxvD
**Commit range:** a542613..2bc1291 (initial review); re-verified against 2bc1291..26c7820 (fix commit `26c7820`, "M1: address adversarial review #0 findings")

## Re-verification summary (26c7820)

Every fix was checked by reading the code, not the author's summary. Executed against `26c7820`:

- `pnpm nx run-many -t lint typecheck test build --skip-nx-cache --output-style=static` — 20 projects green; in-scope suites: contracts 40 tests, execution 16, agents 13, risk 6, signals 2 (89 total across the workspace).
- `node tools/check-invariant-map.mjs` — OK, 28 invariants, 12 mapped; INV-10 now also maps to `libs/contracts/src/envelopes/approval.spec.ts`.
- Probe against the rebuilt `libs/contracts/dist` (no files written): `Uuid` rejects uppercase; `Amount` rejects `2^64` and accepts `2^64-1`; `EmergencyCommand` rejects `PAUSE_NEW_ENTRIES` with a mint, `EMERGENCY_CLOSE_ASSET` with `mint: null`, and a missing `cluster`; accepts `EMERGENCY_CLOSE_ALL` with `mint: null`; the memoized digest equals `contract-set.lock.json`; `ActionCycleState` is now `TRIGGERED, CONTEXT_BUILT, PROPOSED, REVISION_REQUESTED, CLEARED, REJECTED, EXPIRED, UNRESOLVED`.
- Probe of the v2 digest: loose vs strict, presence of a refinement, and check parameters all change the digest (author's test confirmed); a change to a refinement's **predicate body** does not (functions are not fingerprintable). Recorded as a low residual (#4).

Result: the three high findings are fixed with tests that exercise the previously missing cases; all medium findings are fixed or recorded as operator-accepted deferrals; the remaining open items are low. **Verdict changes from Gate BLOCKED to Gate PASS** (see Verdict).

## Scope and method (initial review)

Read in full: `libs/contracts/src/**` (36 files) and `contract-set.lock.json`; `libs/agents/src/action-cycle/**`; `libs/execution/src/state/**`; `libs/signals/src/candidate/**`; `libs/risk/src/runtime-session/**` and `libs/risk/src/mandatory-exit/**`; root `eslint.config.mjs`; `invariant-test-map.yaml`; every per-project `eslint.config.mjs`; the four app entrypoints. Compared against blueprint §6, §11.6–11.11A, §13.7, §14.5–14.7, §15.3–15.4, §15.12, §24.6, D8, D12, D21, D25, D30, D31, D39, D40, D49, D52, D60, D61, GUARDRAILS.md Parts 1–4, ADR-0001, ADR-0003.

Executed at `2bc1291` (no source, test, config or plan file was modified by the reviewer at any point):

- `pnpm nx run-many -t test -p contracts agents execution risk signals --skip-nx-cache` — green.
- `pnpm nx run-many -t lint -p contracts agents execution risk signals --skip-nx-cache` — green.
- `node tools/check-invariant-map.mjs` — OK, 28 invariants, 12 mapped.
- Boundary lint negative probes via `eslint --stdin` (nothing written to disk): a `trust:risk` file importing `openai`, `@sol-agent-trader/agents`, `@turnkey/sdk-server` and calling `Date.now()` produced 2 errors (module boundary, clock discipline); a `trust:risk-authorizer` file importing `agents`, `execution`, `@anthropic-ai/sdk`, `wallet-ui` produced 3 boundary errors. The rule is live, not decorative.
- Zod 4.5.4 probe: `z.toJSONSchema(schema, { io: 'output' })` emits `additionalProperties: false` for **both** `z.object` and `z.strictObject`; `strictObject.extend()` stays strict; nested `z.object` inside a strict parent silently strips unknown keys.

Severity scale: critical = capital or key exposure with no compensating control; high = violates a stated invariant / §31-protected decision in code that exists now, or leaves a §32 question answered "yes"; medium = fidelity or proof gap that must be closed before the module is relied on; low = hygiene, documentation, or enhancement.

## Findings

Disposition column: **FIXED** (verified in `26c7820`), **DEFERRED** (operator-accepted, tracked), **OPEN** (remains; low only). Original findings text is preserved so the record shows what was found; the verification note follows each disposition.

| # | §32 item | Severity | Class (§31) | Finding | Evidence | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | AI boundary: "Can a `HOLD` decision keep exposure open without adversarial scrutiny?" / "Can any discretionary action reach authorization without a cleared cycle?" | **high** | DEFECT | An open-position reassessment cycle could terminate `CLEARED` with **no adversary run** by proposing `IGNORE`, and the position-review machine then marked the position `REVIEWED` because it accepted any `CLEARED` terminal regardless of `action`. Net effect: exposure retained under a "reviewed" label with zero adversarial scrutiny (§31 "every discretionary autonomous exposure action, including open-position HOLD, receives adversarial review"; §11.9 line 2149; D30 lines 339–351; §24.6 line 4391). | Originally `machine.ts:129-131`, `position-review.ts:60-66`, `protection-only.spec.ts:36`. | **FIXED.** `machine.ts:63-77,152-154`: `allowedActions()` gates `PROPOSED` by `cycleTarget()` (candidate → `ENTER`/`IGNORE`; position → `HOLD`/`REDUCE`/`EXIT`/`ADJUST_PROTECTION`; `ADD` only with `options.allowAdd`); `IGNORE` on a position is `ACTION_NOT_ALLOWED`; a cycle with both ids is treated as a position. `position-review.ts:46,69-72`: `CLEARED` → `REVIEWED` only for the four position actions, otherwise `NOT_A_POSITION_REVIEW_ACTION`. `canAuthorize` (`machine.ts:201-206`) now also requires `verdict === 'CONFIRM'` and a target-valid action. Tests: `action-cycle.spec.ts` gating test (all five wrong candidate actions and both wrong position actions rejected with the exact rejection object; HOLD stays `PROPOSED` and cannot authorize); `protection-only.spec.ts` IGNORE-on-position case (both machines refuse); `position-review.spec.ts` rejects `IGNORE`/`ENTER`/`ADD`/`null` from `PROTECTION_ONLY`. |
| 2 | Trading correctness: "Can the process die after signing/before submit … and still reconcile without duplicate entry?" / "Can a transaction land even though the API response says failure?" | **high** | DEFECT | The order-attempt machine rejected `OBSERVED confirmed`/`finalized` from `SIGNED_NOT_SUBMITTED` while accepting `CONCLUSIVELY_DEAD` from that state. A transaction that landed after a crash between `/execute` and persisting `SUBMITTED` was unrepresentable, `countsAsExposure()` false, and the attempt could never reach `NOT_LANDED` (§6.18 line 1477; §14.5 lines 2573–2575; D49 line 515). | Originally `order-attempt.ts:87-99` vs `:107`. | **FIXED.** `order-attempt.ts:78` introduces `LANDABLE = {SIGNED_NOT_SUBMITTED, SUBMITTED, CONFIRMED_PROVISIONAL, REORG_PENDING}`; `:99-108` accepts `confirmed`/`finalized` from any landable state, setting `landedWithoutSubmissionRecord = true` when the source was `SIGNED_NOT_SUBMITTED` and leaving `submittedAt` null so reconciliation can flag the gap; `:110-113` makes `MISSING_OR_CONFLICTING` a no-op before confirmation (still landable) and a reorg only from `CONFIRMED_PROVISIONAL`. Because `signedTxHash` is only set by `SIGNED` from `PREPARED`, the recognised transaction is necessarily the same bytes. Test: "crash after sign" block in `order-attempt.spec.ts` covers confirmed, finalized, missing-before-confirm and exposure/accounting predicates; the existing random-sequence property still holds (submit needs journal, accounting needs finalized, retry needs proven death). |
| 3 | Trading correctness: "Can one candidate create two live entries after a timeout/retry?" | **high** | DEFECT | The idempotency registry released the key once the prior intent was *any* terminal state, including `COMPLETED`. The canonical pgmq failure (execute, die before ack, redeliver) registered a new `CREATED` intent under the same key (D12 lines 202–204; §24.6 line 4375). | Originally `intent.ts:36-43`; `intent.spec.ts:22` asserted the hole as intended. | **FIXED.** `intent.ts:38-45`: a known key always returns `DUPLICATE` with the existing entry, whatever its state; keys are permanent. Doc comment (`:6-10`) states that a retry after a conclusive failure is a new *attempt* of the same intent, never a new intent under the same key. Tests: `intent.spec.ts` walks `CREATED→AUTHORIZED→EXECUTING→COMPLETED` asserting `DUPLICATE` at each step and after `COMPLETED`, `reg.size === 1`; the property asserts `reg.size === distinct keys` and that every duplicate returns the first intent id. Note (trade-off, acceptable): an `EXPIRED`/`CANCELLED` intent also locks its key, so a legitimate retry must mint a new key (e.g. new cycle/attempt number) — conservative and consistent with D12. |
| 4 | Security: "Can two deployables with different contract digests both become live?" / D50 "any change to any wire shape changes it" | medium | DEFECT | The contract-set digest could not see strictness or refinements: Zod 4.5.4 `toJSONSchema` emits `additionalProperties:false` for `z.object` and `z.strictObject` alike, and `.refine` is unrepresentable. Strict→loose on `RiskAuthorizedIntent` left the lock unchanged (D50 line 526; §24.6 line 4395). | Originally `contract-set-digest.ts:24-26`; `contract-set-digest.spec.ts:37-45`. | **FIXED** (with one documented residual). `contract-set-digest.ts:17` bumps the format to `zod4-jsonschema-xzod-canonical-sha256-v2`; `:54-72` uses the `override` hook to annotate every node with `x-zod {type, catchall, checks[], literal}` read from `_zod.def`, so strictness (`catchall: 'never'` vs `null`), refinement presence and check parameters (min/max/pattern/format/…) change the digest. Lock regenerated (187 schemas; verified equal to the memoized digest). Test proves loose, strict, refined, `max(20)` and `max(10)` all differ, and that removal of a schema changes the digest. **Residual (low, OPEN as #4a):** a change to a refinement's predicate *body* (e.g. lowercase→uppercase test in `Uuid`) is not detected — functions are not fingerprintable; the digest sees "a custom check exists", not what it checks. Acceptable: predicate changes are source changes caught by review and the M8a artifact digest of the built code; record it so nobody assumes otherwise. |
| 5 | Security: "Can database tampering … loosen any execution field after risk authorization?" | medium | DEFECT | Nested non-strict objects inside signed payloads (`RiskStateProjection.sourceDigests[]`, `drawdown`, `capitalAttestation`; `ShadowPosition.stop`) silently stripped unknown nested keys; the strictness test only injected at the top level. | Originally `risk-state-projection.ts:63,73,83`; `journal.ts:50`; `contract-set-digest.spec.ts:67-72`. | **FIXED.** All four converted to `strictObject` (diff verified). The strictness test now walks every object node of every envelope fixture (arrays descended) and injects `__injected` at each path, asserting rejection; the only exemptions are the two documented open `JsonRecord` fields (`ExecutorJournalEntry.payload`, `QueueMessageEnvelope.payload`), for which a second test proves parse keeps unknown keys so `canonicalize(parse(x)) === canonicalize(x)` — no strip-then-rehash ambiguity. |
| 6 | AI boundary: "Can the model nominate an asset that was not the candidate?" / §11.6 `ADD` disabled by default | medium | MISSING REQUIREMENT | `PROPOSED` did not check action against target or strategy permission; `ADD` passed for any strategy; `ACTION_NOT_ALLOWED` was declared but never emitted; the event carried no target ids. | Originally `machine.ts:32,45,114-133,160-164`. | **FIXED** by the #1 gating (`allowedActions`, `ACTION_NOT_ALLOWED` now emitted with `target`, `ADD` opt-in via `TransitionOptions.allowAdd`). Residual (low, folded into #17-type hygiene): the `PROPOSED` event still does not carry the proposal's own `candidateId`/`positionId`, so binding "the proposal's target equals the cycle's target" remains the caller's job at the point where `TradingActionProposal` is validated; the machine now guarantees the *kind* of target matches the action, which closes the exposure-relevant part. |
| 7 | AI boundary / D39 "strategy policy may only tighten" | medium | DEFECT | `TIGHTEN_UNREVIEWED_STOP` accepted `NaN`/±∞/negative; `NaN` poisoned the monotonic guard so any later level was accepted. | Originally `position-review.ts:52-57`; test excluded `NaN`. | **FIXED.** `position-review.ts:61` rejects `!Number.isFinite(level) || level < 0` with `INVALID_STOP_LEVEL` before the tighten check. Property test now draws `NaN`, ±∞, `-1`, `-0.5` at weight 1/9 and asserts rejection without state change. |
| 8 | ADR-0001 / D39 six causes | medium | DEFECT | `REVISION_EXHAUSTED` unreachable (second `CHALLENGE` labelled `DISAGREEMENT`); `ADVERSARY_REVIEW`/`REVISED` dead states; schema did not enforce reason⇔`UNRESOLVED`. | Originally `machine.ts:151-153`; `enums.ts:44-71`; `agents.ts:154-156`. | **FIXED** (states, reason) / **OPEN (low)** (schema refinement). `machine.ts:189` emits `UNRESOLVED(REVISION_EXHAUSTED)`; `enums.ts` drops the two dead states with a doc comment defining `PROPOSED` = awaiting review and `REVISION_REQUESTED` = awaiting the single revision. `DISAGREEMENT` is now produced only by position-review for a `REJECTED` terminal (`position-review.ts:84`), which matches D39's "disagreement" reading; the cycle itself ends `REJECTED`, as §6.10D lists. Not done: a `superRefine` on `ActionCycle` coupling `unresolvedReason` to `state === 'UNRESOLVED'` — low, tracked as #8a. |
| 9 | Security: "Can a stale/replayed approval authorize a different authorization hash?" (INV-10 mapping) | medium | MISSING REQUIREMENT | INV-10 was mapped only to generic envelope tamper tests; no approval-binding, expiry or nonce logic or test existed; the map could not express partial coverage. | Originally `invariant-test-map.yaml:90-97`; `signing.spec.ts:75-131`. | **FIXED** (helper + tests + map) / **OPEN (low)** (map `coverage` field). `envelopes/approval.ts:15-17` `deriveAuthorizationHash()` recomputes from a verified envelope's payload; `:54-70` `checkApprovalBinding()` checks hash, intent, `[grantedAt, expiresAt)` window, nonce ledger and step-up, with typed failure reasons. `approval.spec.ts`: tampered field changes the derived hash; each failure reason exercised; property over random hash pairs × time offset × replay flag asserts `ok ⇔ hash match ∧ within window ∧ not replayed`. INV-10 now maps to `approval.spec.ts` as well. Residual (low, #9a): `checkApprovalBinding` does not consult `ApprovalRecord.revokedAt` (not in the signed grant by design) — the executor must check revocation against the record; and the invariant map still lacks a `coverage: full/partial` field, so "partial pending executor" must be read from this review rather than the map. |
| 10 | Security: "Can a DB-independent emergency command increase exposure or send to an arbitrary address?" (D25/D22) | medium | DEFECT | `mint` nullable with comment-only requirement for `EMERGENCY_CLOSE_ASSET`; no `cluster` binding. | Originally `emergency-command.ts:15-28`. | **FIXED.** `emergency-command.ts:20` adds `cluster: SolanaCluster`; `:31-34` `.refine((c) => (type === 'EMERGENCY_CLOSE_ASSET') === (mint !== null))` — mint required for CLOSE_ASSET and forbidden otherwise. Verified by probe (four cases). Fixture updated; deep-strictness test still passes through the refinement wrapper. |
| 11 | Security / GUARDRAILS Part 4 tag vocabulary | medium | TRADE-OFF / DECISION | `trust:risk` and `trust:execution` tags were not in the closed Part 4 table. Several Part 4 "must never import" rows are not expressible as package bans (keypair/`sendTransaction` capability, service-role key module, news/social SDKs) and are covered only at M8a. | `libs/risk/package.json`, `libs/execution/package.json`; `eslint.config.mjs:113-124`; GUARDRAILS Part 4. | **FIXED** (documentation) — GUARDRAILS.md Part 4 tag table now lists both tags with their allowed dependencies and bans (diff verified). Note for the operator: this is an edit to the canonical guardrail file by the implementing session; it documents already-enforced *stricter* constraints and touches no §31 item, so I accept it, but the operator should acknowledge it explicitly (ground rule 9 makes this file the single source of law). The non-lintable bans remain a stated M8a dependency — **OPEN (low, informational)**. |
| 12 | Research validity / ADR-0003 guard (plan M1 item) | medium | MISSING REQUIREMENT | The plan's M1 scope names "one shared Jupiter quote/order client in `/libs/execution`"; only the `JupiterQuoteClient` interface exists. | `execution-adapter.ts:126-129`; `Execution_Plan_v4.md:113`. | **DEFERRED** (operator-accepted). The contract (interface plus normalized `Quote`/`OrderBuild` shapes) is fixed in M1; the implementation lands with the first consumer (M3/M5a). I agree this is acceptable **provided** the plan bullet is read as "contract fixed in M1", and the M5a/M3 checklists carry the client explicitly so the single-quote-path guard has a concrete symbol before the paper adapter exists. Recommend the operator tick M1's bullet as "contract" and add the client to M5a's acceptance. |
| 13 | Security: canonical JSON ambiguity | low | ENHANCEMENT | Sound canonical form (effectively RFC 8785 minus bigint). Cross-type collisions only (`1n`≡`"1"`, `[undefined]`≡`[null]`, `-0`≡`0`); no contract/type tag in the signed message. | `canonical.ts:16-51`; `signed-envelope.ts:34-36`. | **OPEN (low).** Add `contract: '<registry name>'` to the signed message for domain separation; document the bigint/string equivalence. |
| 14 | Security: "Is `payloadHash` trusted anywhere?" | low | MISSING REQUIREMENT | `authorizationHash` appeared in five places with no definition or recomputation rule. | `approval.ts:12`; `trading.ts:148,228,248`; `execution-adapter.ts:85`. | **FIXED.** Defined in `approval.ts:9-17` as the SHA-256 of the canonical `RiskAuthorizedIntent` payload (= `payloadHash` of a *verified* envelope), always recomputed via `deriveAuthorizationHash`, never read from storage or from the grant. |
| 15 | Trading correctness: amounts | low | ENHANCEMENT | `Amount` unbounded; SPL quantities are u64. | `primitives.ts:36-39`. | **FIXED.** `.max(20)` plus `BigInt(v) <= 2^64-1` refine; probe confirms boundary. |
| 16 | Blueprint §6 fidelity (check A) | low | MISSING REQUIREMENT | `direction` (LONG) absent (§6.11 line 1303); `skillVersionId` nullable vs required (§11.6 line 2040); no `observedAt` on projection (§6.14A line 1391); shadow lacks account id / per-lot custody location (§6.16C lines 1535–1537); `SpendUsage` lacks operating cost (§6.16B line 1529); `SessionTransition` lacks per-transition exposure (§6.22A line 1563). | `trading-action.ts`, `risk-state-projection.ts`, `journal.ts`, `ops.ts`. | **PARTIALLY FIXED / OPEN (low).** `direction: z.literal('LONG')` added to `TradingActionProposal` (fixture updated). The remaining five items are deferred by the author and stay open as low; none affects authority or exposure. |
| 17 | AI boundary: `canAuthorize` on DB-loaded rows | low | ENHANCEMENT | Did not require `verdict === 'CONFIRM'` or a target-valid action. | `machine.ts:160-164`. | **FIXED.** `machine.ts:201-206` requires `CLEARED` ∧ `CONFIRM` ∧ discretionary ∧ target-valid ∧ cleared cutoff = latest. Not added: a non-empty `adversaryRunIds` check (deterministic T0 gates may legitimately have `runId: null`), so the CONFIRM requirement is the right invariant. |
| 18 | Trading correctness: fallback landing paths | low | TRADE-OFF | `SUBMITTED` from `SUBMITTED` rejected despite §14.6 step 7 and `submissions[]`. | `order-attempt.ts:82-85`. | **FIXED.** `order-attempt.ts:94-97` allows re-submission of the same signed bytes (bytes cannot change within an attempt), appending the path; still requires the journal; `submittedAt` keeps the first timestamp. Tested. |
| 19 | INV-24 breadth | low | ENHANCEMENT | Roundtrip harness covers 14 of 187 registered schemas. | `fixtures/index.ts:343-358`. | **OPEN (low, deferred by author).** Generate a fixture or arbitrary per registered schema. |
| 20 | Module hygiene | low | ENHANCEMENT | Circular imports `entities/trading.ts` ↔ `envelopes/*`; position-review placement in `libs/execution`; agents devDependency on execution for the cross-machine test. | `entities/trading.ts:37-40`; `envelopes/execution-adapter.ts:4-5`. | **OPEN (low, deferred by author).** Not blocking; module-init order works and is exercised by tests. |
| 21 | Runtime / custody edge cases | low | ENHANCEMENT | `attended` unused; `WIND_DOWN_COMPLETE` blind to OFFLINE_PROTECTED deadline/watchdog (D61 lines 645–647); `classifyMovement` ignored mint vs custody mint. | `runtime-session/machine.ts:33,105-112`; `custody.ts:32-43`. | **PARTIALLY FIXED / OPEN (low).** Custody mint check added (`custody.ts`: `RegisteredCustody.mint`, `MINT_MISMATCH`; tests updated). Runtime-session items deferred by the author; stay open as low. |
| 22 | Contract hygiene | low | ENHANCEMENT | `Uuid` mixed case; `Instant` precision; `AuthorizationDenial.reasonCodes` re-declares the regex. | `primitives.ts:16,20`; `risk-authorized-intent.ts:70`. | **PARTIALLY FIXED / OPEN (low).** `Uuid` is lowercase-only via refine (probe confirms). `Instant` precision and the `ReasonCode` reuse are deferred; open as low. |

Residual sub-items opened during re-verification (all low): **#4a** digest blind to refinement predicate bodies; **#8a** no schema-level `unresolvedReason ⇔ UNRESOLVED` refinement; **#9a** `checkApprovalBinding` does not consult revocation (executor must), and the invariant map has no `coverage` field.

### Check B — ADR-0001 separation (explicitly required)

- The action-cycle machine (`libs/agents/src/action-cycle/machine.ts`) imports only `ActionCycle`-related types; it has no reference to `PositionReviewState`, `PROTECTION_ONLY`, `BUDGET_PAUSED`, `unreviewedStop` or any position field. The new `cycleTarget()`/`allowedActions()` reason about *which kind of thing the cycle is about*, not about position state. Position behavior does **not** live in the action cycle. **Confirmed at both commits.**
- The position-review machine (`libs/execution/src/state/position-review.ts`) consumes only a cycle's terminal state, action and reason; it contains no cutoff, revision, verdict or proposal logic. Action-cycle logic does **not** live in position-review. **Confirmed.**
- `UNRESOLVED` always carries a reason from the D39 set. At `26c7820` the machine produces `ADVERSARY_UNAVAILABLE`, `TIMEOUT`, `BUDGET`, `MALFORMED_OUTPUT`, `REVISION_EXHAUSTED`; position-review labels a `REJECTED` terminal `DISAGREEMENT` and an `EXPIRED` terminal `TIMEOUT`, and rejects `UNRESOLVED` with a null reason. The property test asserts non-null. **Confirmed**; schema-level coupling remains a low open item (#8a).
- The behavioral leak found at `2bc1291` (any `CLEARED` treated as reviewed) is closed (#1).

### Check C — signed-envelope security summary

- Signature covers canonical `{algorithm, keyId, payload, signedAt}`; `payloadHash` is re-derived and never trusted; forged `payloadHash` cannot rescue a tampered payload. **Sound.**
- Unknown keys fail before signature checks; malformed meta fails closed. **Sound.**
- Canonical JSON: no exploitable ambiguity; collisions in #13 are cross-type and unreachable through strict schemas. **Sound.**
- Strictness: every signed payload is now strict at every nesting level except the two documented open `JsonRecord` fields, which provably do not strip (#5 fixed). The digest now sees strictness and refinement presence (#4 fixed; #4a residual).
- Approval binding now has a pure, tested checker (#9 fixed); `authorizationHash` has one definition and one derivation (#14 fixed).
- Ed25519 via WebCrypto, non-extractable by default; not a wallet key path. **Sound.**

### Check E — §32 checklist as applied to pure contracts/state machines

Evaluable now, result in parentheses; items not listed require runtime code and are deferred to reviews #1–#4. Updated for `26c7820`.

- Trading correctness: two live entries after retry (**#3 fixed** — key permanent); tx lands though API says failure / die after sign (**#2 fixed** — landable from `SIGNED_NOT_SUBMITTED`, flagged); provider fill reducing another strategy's lot (OK — lot isolation property); confirmed disappearing before finalized causing replacement (OK — retry only from `NOT_LANDED` with both proofs); Trigger vault movement vs unexplained custody movement (OK — now also mint-checked, #21); stop slippage cap during gap (not evaluable).
- Data correctness: revised metadata overwriting what the strategy knew (OK by shape); duplicate news (OK by shape); own wallet as evidence (OK by shape; INV-11 unmapped, module absent).
- AI boundary: discretionary action reaching authorization without cleared cycle (OK — `canAuthorize` requires `CLEARED` ∧ `CONFIRM` ∧ target-valid); HOLD without scrutiny (**#1 fixed** — `IGNORE` impossible on a position; review requires a position action); adversary creating/upgrading a proposal (OK); infinite disagreement loop (OK — `revisionRound ≤ 1`, second `CHALLENGE` → `REVISION_EXHAUSTED`); model outage preventing hard exit (OK — classifier ignores context); model nominating a non-candidate asset (**#6 fixed** for action/target kind; proposal-id binding remains the validator's job, low); confidence affecting size (OK — no amount in proposal); evidence refresh with different cutoffs (OK — property-tested); speed tier skipping the control (OK by shape; T0 gate pending); spend exhaustion disabling mandatory exit (OK).
- UX: PAPER vs LIVE_AUTO confusion (not evaluable); stale data as zero (OK by shape).
- Security: DB tampering after authorization (mechanism OK; executor reconstruction pending); forged projection (mechanism OK; authorizer pending); stale approval (**#9 fixed** at pure level; executor call pending); emergency command increasing exposure / arbitrary address (OK — no recipient; **#10 fixed** mint/cluster); executor bypass by browser (OK — boundary lint verified live); wallet connector becoming signer (OK by shape); risk-authorizer artifact containing LLM/wallet SDK (source half OK; artifact half at M8a); secret material in logs (not evaluable).
- Research validity: two deployables with different digests both live (mechanism OK; **#4 fixed**, #4a residual).

### Check F — trust boundaries vs GUARDRAILS Part 4

Faithful transcription for every named package and every allow/deny row; negative probes confirm the rule fires; `trust:risk`/`trust:execution` now documented in Part 4 (#11). No project can import something Part 4 forbids by name. Rows not expressible by package name are an M8a artifact-scan dependency and are stated as such.

### Check G — compositional cases (re-run against `26c7820`)

| Case | Result |
| --- | --- |
| Evidence refresh during `REVISION_REQUESTED`, then `CONFIRM` at the stale cutoff | Correct (unchanged): review rejected `INVALID_FROM_STATE`; re-proposal at v1 is `CUTOFF_MISMATCH`; only propose@v2 then review@v2 clears. |
| `IGNORE` clearing a cycle that has a `positionId` | **Fixed.** `ACTION_NOT_ALLOWED {action: IGNORE, target: POSITION}`; and position-review independently refuses a `CLEARED IGNORE`. Both tested. |
| Order attempt observed `finalized` directly from `SUBMITTED` | Correct (unchanged). |
| Crash after sign, before `SUBMITTED` persisted; tx lands | **Fixed.** `CONFIRMED_PROVISIONAL`/`FINALIZED` with `landedWithoutSubmissionRecord: true`, `submittedAt: null`, counts as exposure. Tested. |
| Runtime session `END_SESSION` while `PAUSED` | Correct (unchanged): → `WIND_DOWN`, `PAUSED` sticky. |
| Mandatory exit while position is `BUDGET_PAUSED` | Correct (unchanged): classifier is a pure function of triggers. |
| Redelivery of a `COMPLETED` intent's message | **Fixed.** `DUPLICATE` with the completed entry; registry size unchanged. Tested. |
| `TIGHTEN_UNREVIEWED_STOP` with `NaN` | **Fixed.** `INVALID_STOP_LEVEL`, no state change. Property-tested. |
| Same signed bytes re-landed over a second path | Now allowed (#18); paths appended; journal still required. Tested. |

## What is proven vs partial (the 12 mapped invariants, at `26c7820`)

"Proven at pure level" means the mapped property test genuinely establishes the invariant for the pure code that exists; "partial" means the test proves a mechanism or a narrower proxy and the invariant's full statement depends on executor/authorizer/worker code not yet written. Partial is acceptable at M1 provided it is stated here (the map still cannot state it — #9a).

| Invariant | Status at contract/pure-code level | What remains |
| --- | --- | --- |
| INV-04 no duplicate active entry per idempotency key | **Proven at pure level** (strengthened beyond the statement): one registry entry per key, ever; redelivery in any state returns the first intent; property asserts `size === distinct keys`. | DB unique index on `idempotency_key`; executor idempotency module must call the registry before any authorization request. |
| INV-05 no live trade while PAUSED or OBSERVE/PAPER | **Proven at pure level.** `liveExecutionAllowed` false whenever paused, non-live authority or capability disabled; 500 random sequences. | Executor mode gate must call it; durable `PAUSED` persistence. |
| INV-06 DB tampering cannot change executable fields | **Partial (mechanism proven).** Any payload change or forged `payloadHash` detected; signed payloads strict at every level; digest sees strictness. | Executor reconstruction from immutable records and verification against the pinned key (M3). |
| INV-10 stale/replayed approval cannot authorize a changed envelope | **Proven at pure level** for binding, window and nonce (`checkApprovalBinding` property: `ok ⇔ hash match ∧ in window ∧ not replayed`); `deriveAuthorizationHash` proven to change on any field change. | Executor must verify both envelope signatures first, derive the hash from the verified authorization, consult the durable nonce ledger and `ApprovalRecord.revokedAt` (#9a). |
| INV-14 no discretionary live intent without a cleared cycle | **Proven at pure level.** `canAuthorize` requires `CLEARED` ∧ `verdict CONFIRM` ∧ discretionary ∧ target-valid action ∧ cleared cutoff = latest; safe on DB-loaded rows. | Authorizer must call it and verify the cycle row's integrity; proposal-target id binding at the validator. |
| INV-15 mandatory risk reduction cannot be blocked by an adversary | **Proven at pure level** for the classifier (decision is a pure function of triggers; context provably ignored). | Execution path must not *await* the adversary; the classifier proves the policy, not the scheduler. |
| INV-19 v1 proposal cannot be cleared under v2 without revision | **Proven at pure level.** Cutoff mismatch rejected for both roles; refresh invalidates a pending proposal; `CLEARED ⇒ clearedCutoffVersion = latest` over random sequences. | Worker must feed real run ids/cutoffs; replay reconstructs from `cutoffs[].consumedByRunIds`. |
| INV-20 unresolved open-position cycle cannot persist a cleared HOLD; enters PROTECTION_ONLY | **Proven at pure level.** Every non-`CLEARED` ending of a position cycle → `PROTECTION_ONLY`/`BUDGET_PAUSED`; `IGNORE`/`ENTER`/`ADD` cannot be proposed on a position; position-review refuses any `CLEARED` that is not a position action. Cross-machine test plus per-machine tests. | Persistence layer must write `reviewState` transactionally with the cycle terminal; HIGH alert after consecutive-failure threshold (runtime). |
| INV-21 budget exhaustion cannot suppress mandatory exits | **Proven at pure level** (`budgetExhausted` provably ignored by the classifier). | Automation runtime (`libs/agents/src/budget`, not yet built) must route mandatory triggers around the budget gate. |
| INV-22 no promotion to final P&L without `finalized` | **Proven at pure level.** `finalAccountingAllowed` only in `FINALIZED`; `FINALIZED` only via a `finalized` observation, from any landable state including the crash-after-sign path. | Accounting writer must consult it. |
| INV-23 REORG_PENDING cannot be retried while landable | **Proven at pure level.** `canRetry` only from `NOT_LANDED`; death needs expired block height ∧ empty signature history; `finalized` from `REORG_PENDING` still valid; missing-before-confirm is not a reorg. | Executor replacement logic; RPC-level proof of death. |
| INV-24 encode/decode preserves semantics and digest | **Partial.** Roundtrip + fixed point proven for 14 fixtures; digest stable, memoized, lock-checked; changes on field, strictness, refinement-presence and check-parameter changes. | Breadth 14/187 (#19); refinement predicate bodies invisible (#4a); cross-deployable digest match at M8a. |

## Verdict

**Gate PASS** (at `26c7820`).

All three high findings (#1, #2, #3) are fixed in code with tests that exercise the exact failure cases found, and each fix was re-read and, where useful, probed independently. All medium findings are fixed (#4, #5, #6, #7, #8, #9, #10, #11) or recorded as an operator-accepted deferral with a concrete follow-up (#12). No unresolved critical or high finding remains. The open items are low: #4a, #8a, #9a, #13, #16 (five fidelity fields), #19, #20, #21 (runtime-session edges), #22 (Instant precision, `ReasonCode` reuse), and the informational M8a dependency in #11.

Two things the operator should acknowledge explicitly, because they were changed by the implementing session rather than decided in advance: the GUARDRAILS.md Part 4 tag-table addition (#11), and the reading of the M1 "shared Jupiter client" bullet as contract-only with the implementation moved to M3/M5a (#12).

What is good and should not be regressed: canonical JSON and envelope verification are sound; terminal states are absorbing and property-tested across random interleavings; the cycle machine now refuses target-inappropriate actions and the position machine independently refuses to treat a non-position decision as a review (defense in depth across the ADR-0001 boundary); the order-attempt machine treats every state after signing as landable; idempotency keys are permanent; `PAUSED` stickiness and live-arming preconditions are enforced in one place; the boundary lint is real; the digest now carries strictness.

Recommended for review #1 (M3): re-check INV-06/INV-10/INV-14 at the executor/authorizer call sites against the helpers proven here; confirm `landedWithoutSubmissionRecord` raises a reconciliation alert; confirm the nonce ledger and `revokedAt` are consulted; add `coverage` to the invariant map.
