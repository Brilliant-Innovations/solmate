# Interim adversarial review — M2 containers/artifacts and auth/step-up substrate

| | |
| --- | --- |
| Reviewer | Fresh Claude session (same model family as the author). Interim review requested by the operator; **not** the formal M3 gate (review #1). |
| Date | 2026-09-06 |
| Commits reviewed | `f6843da` (containers, GHCR images, built-artifact checks) and `7c7ac10` (auth + step-up substrate). Read in full: every file listed in the two `git show --stat` outputs plus, for context, `20260906000800_rls.sql`, `20260906000100_foundation.sql` (`ops.has_role`, `core.forbid_update`), `20260906000600_ops_audit.sql` (`ops.control_requests`), `libs/contracts/src/digest/*`, `libs/contracts/src/config/env.ts`, `apps/*/src/main.ts`, `eslint.config.mjs`, `apps/web/src/app/(auth)/sign-in/actions.ts`. |
| Checklist | `docs/implementation/GUARDRAILS.md` Part 3 (§32), Part 4; blueprint §5.7, D41, §15.6, §20.26, §23.2–23.3, §24.8, D50, D58, D65; ADR-0005, ADR-0006. |
| Files modified | None. This file only. |

## How I verified

| Command | Result |
| --- | --- |
| `pnpm nx test @sol-agent-trader/worker --skip-nx-cache` | 10/10 pass (`verify.spec.ts`). |
| `pnpm nx test @sol-agent-trader/contracts --skip-nx-cache` | 54/54 pass; lock matches (208 schemas, digest `9e0800…e0e3`). |
| `pnpm exec supabase test db` against the already-running local stack (`supabase_db_solmate`; not reset, not restarted) | Suites 02–06 **ok** (06 step-up: 14/14). Suite 01 fails 3/9 (`have: 6, want: 2`) because the local database already holds audit rows from earlier smoke tests; the suite assumes an empty `audit.events`. Not a step-up defect; see R2-14. 57 tests total, matching the plan claim. |
| `node` probe of `scanBundle()` from `tools/check-artifacts.mjs` with 18 crafted bundle fragments | Results recorded under R2-03/R2-04/R2-17. |
| `grep` over `apps/worker/dist/main.js` | 639 `// node_modules/.pnpm/...` path comments, 40 `// libs/...` comments, zero `@simplewebauthn` (the worker's `main.ts` does not import `verify.ts`), one `__require()` with no argument, no `require(<variable>)`. |
| GoTrue source (`internal/api/mfa.go`) and Supabase TOTP docs | Confirmed: `AAL2 required to enroll a new factor` only when `numVerifiedFactors > 0`; `AAL2 required to unenroll verified factor`. A user with **no** verified factor may enrol at aal1. |
| `gh run view 34063365545` | **UNVERIFIED** — `gh` is not installed in this environment. Would verify with `gh run view 34063365545 --repo Brilliant-Innovations/solmate --log`. |
| Plan §8 evidence file names | Checked with `ls`/`grep`; see R2-15. |

## Findings (most severe first)

| ID | Sev | Class (§31) | Where | Attack / failure scenario (verified by reading or running) | Recommended fix |
| --- | --- | --- | --- | --- | --- |
| R2-01 | **HIGH** | DEFECT (D41 "operator/credential changes require step-up"; §32 "Can a stolen web session arm live mode alone?") | `libs/contracts/src/policy/step-up.ts:36` (`REGISTER_PASSKEY: 'AAL2_ONLY'`), `supabase/migrations/20260906001100_step_up.sql:120-123` (`begin_step_up` skips the passkey check for `REGISTER_PASSKEY` unconditionally), `apps/worker/src/step-up/verify.ts:141-151` (`verifyPasskeyRegistration` has no "existing passkey" rule), ADR-0006 §2 point 4 | ADR-0006 says AAL2_ONLY exists "so the **first** passkey can be added with TOTP alone", but nothing in the policy table, the DB function or the verifier restricts it to the first. Any aal2 session — including one hijacked after the operator verified TOTP (cookie theft, XSS in a future component, malware) — can call `begin_step_up('REGISTER_PASSKEY', …)`, register an attacker-controlled authenticator, and then satisfy every `REQUIRED` kind (`ARM_RELEASE`, `RESUME_NEW_ENTRIES`, `APPROVE_AUTHORIZATION`, `PROMOTE_RELEASE`) and `REVOKE_PASSKEY` the operator's real key. Passkey step-up therefore adds nothing over "aal2 session" once the M9 handler lands on this substrate; the hole is in the shipped policy, not in future UI. | (1) Split the class: `REGISTER_PASSKEY` is `AAL2_ONLY` only while the user has zero active passkeys; with one or more it is `REQUIRED` (assertion from an existing passkey accompanies the registration). Enforce in three places: `begin_step_up` (already knows whether a passkey exists), `verifyPasskeyRegistration` (take `activePasskeyCount` and a verified `StepUpVerdict` when > 0), and the policy table (`'FIRST_ONLY_AAL2'`). (2) For the very first passkey require a *fresh* TOTP (check the `amr` claim timestamp for `totp` within 5 min inside `begin_step_up`, or re-verify in the same flow), raise a CRITICAL notification with out-of-app delivery, and impose a cooling period (or `traderctl` admin confirmation) before a newly registered passkey may authorise `REQUIRED` kinds. (3) Amend ADR-0006 and pin with a pgTAP case ("second passkey needs step-up") and a worker spec case. |
| R2-02 | **MEDIUM** | DEFECT (claimed control not wired) | `deploy/profile-0/README.md:29` ("Every service refuses to start when it sees a credential outside its trust class"), `deploy/profile-0/.env.worker.example:1-2`, `.env.risk-authorizer.example:2`; `apps/{worker,risk-authorizer,execution-service}/src/main.ts` | `parseWebEnv` / `parseWorkerEnv` / `parseRiskAuthorizerEnv` / `parseExecutionServiceEnv` exist in `libs/contracts/src/config/env.ts` but `grep -rn "parse.*Env" apps/` finds **no caller**. The three entrypoints read `DEPLOYMENT_PROFILE`, `SENTRY_DSN_*`, `SERVICE_INSTANCE_ID` and exit. A worker container started with `TURNKEY_API_PRIVATE_KEY` or `RISK_AUTHORIZATION_KEY_PKCS8` in its env file boots happily. The M2 exit gate ("manifests prove separate credentials") is still met by `config/profiles` + `checkManifest` tests, but the README and env examples describe a runtime control that does not exist. | Call the service's `parse*Env(process.env)` first thing in each `main.ts` (fatal on failure, before any network/telemetry init). Log the contract digest **before** env validation or have `images.yml`/`ci.yml` pass a minimal valid env, otherwise the "starts without credentials" step will break. Add a CI negative test: run the worker bundle with `TURNKEY_API_PRIVATE_KEY=x` and assert exit 1 and a redacted error. Until then, correct the README/env comments. |
| R2-03 | **MEDIUM** | MISSING REQUIREMENT (§24.8 "inspect the built artifacts"; scan must stay meaningful) | `tools/check-artifacts.mjs:50-62, 74-96`; `apps/*/package.json` build target (`minify: false` at `options` level only) | The scan's only signal for bundled code is esbuild's `// node_modules/...` path comment. (a) Nothing asserts those comments are present: with `minify: true` (or any future `legalComments`/`minifyWhitespace` change in the `production` configuration, which already overrides `esbuildOptions`) esbuild drops them, every marker regex finds nothing, and the scan reports `ok: true` on a blind bundle. (b) The policy is a denylist by exact name: `openai-edge`, `@langchain/*`, `cohere-ai`, `groq-sdk`, `@mistralai/*`, `ollama`, `together-ai`, `replicate` (probed: `openai-edge` → no finding) all pass for `risk-authorizer`, so "no LLM SDKs" (§24.8, Part 4) is not what is enforced. (c) `import("openai")` and `__require("openai")` are caught, but a string that is only a package name is not — acceptable. | Add liveness canaries: fail unless the bundle contains ≥ 1 `// node_modules/` comment **and** ≥ 1 `// libs/contracts/` comment, and read the Nx build options to assert `minify === false` for the `production` configuration. For `risk-authorizer` and `execution-service` switch to an **allowlist**: extract every `node_modules/.pnpm/<pkg>@` package from the comments and fail on any package not in a checked-in allowlist (`tools/artifact-allowlist.json`), keeping the denylist as a second layer. Extend the CI planted-marker test to also plant a `require("openai")` and an unknown allowlist package. |
| R2-04 | **MEDIUM** | DEFECT (latent; caller is M9 but the DB shape is shipped) | `20260906001100_step_up.sql:82` (FK `challenge_id references ops.step_up_challenges (id)` without `on delete`), `:125-126` (`delete … where consumed_at is null and expires_at < now()`), `apps/worker/src/step-up/verify.ts:76` (checks `consumedAt` but nothing sets it) | Two problems with consumption. (1) Nothing atomically consumes a challenge: `verify.ts` reads `consumedAt` from a row snapshot; two worker instances processing two control requests carrying the same `challengeId` both see `null` and both verify. The only serialisation point is `step_up_assertions.challenge_id unique`, which protects only if the caller inserts the assertion **before** acting, in the same transaction — undocumented. (2) If the caller records a *failed* verification (`verified=false`) without setting `consumed_at` (the ADR says the worker "writes the verdict"), that expired challenge is now referenced by an assertion. The operator's next `begin_step_up` tries to delete it → FK violation `23503` → **the operator can never begin step-up again** (permanent DoS of every `REQUIRED` control for that user, until a backend cleans up). | Add a backend-only `ops.consume_step_up_challenge(id, verified, failure_reason, …)` that does `update … set consumed_at = now() where id = $1 and consumed_at is null returning *` and inserts the assertion in one statement/transaction; document that the worker acts only after that returns a row. Remove the `delete` from `begin_step_up` (the rate limit needs only `expires_at > now()`), or add `on delete` semantics that cannot break the FK. Add pgTAP: an expired challenge with an assertion does not block `begin_step_up`. |
| R2-05 | LOW | DEFECT | `tools/check-artifacts.mjs:52-55` (`seg = '[^/"'\\s@]+'`, `*` → `seg`) | Trailing `*` requires at least one character, so the **exact** package names are missed: probes show `node_modules/lunarcrush/`, `node_modules/cryptopanic/`, `@solana/wallet-standard/` (exact) → no finding, while `lunarcrush-x`, `@solana/wallet-standard-features` are caught. (`@birdeye/sdk` is caught only through the pnpm-folder form `@birdeye+sdk@`.) These patterns go beyond Part 4's explicit bans, so no Part 4 rule is bypassed today. | Expand a trailing `*` to `[^/"'\s@]*` (zero or more) and add these exact names to the CI planted-marker test. |
| R2-06 | LOW | DEFECT (open redirect after auth) | `apps/web/src/proxy.ts:38-41, 48-51` sets `next` = `request.nextUrl.pathname`; `apps/web/src/app/(auth)/sign-in/actions.ts:14` (`redirect(next.startsWith('/') ? next : '/')`, earlier commit); `apps/web/src/app/(auth)/sign-in/mfa/actions.ts:10` | A request to `https://app//evil.example/x` has pathname `//evil.example/x`; the proxy redirects to `/sign-in?next=//evil.example/x`; after a successful sign-in the action redirects to `//evil.example/x` (protocol-relative → attacker host). `verifyTotp` blocks `//` but not `/\evil.example`, which WHATWG URL parsing also treats as an authority. Phishing vector for the operator's credentials/TOTP, not a data-path issue. | Resolve `next` with `new URL(next, request.url)` and accept only when `.origin` equals the app origin and the path contains no backslash; or accept only known route prefixes. Apply in both actions and in `proxy.ts` when writing `next`. |
| R2-07 | LOW | TRADE-OFF (bootstrap window) | `apps/web/src/proxy.ts:46-47` (`mfaPending` only when `nextLevel === 'aal2'`), `apps/web/src/components/totp-enrollment.tsx:29` | Verified against GoTrue: enrol at aal1 is allowed while the user has **no** verified factor. An operator account that has not yet enrolled is one stolen aal1 session away from aal2 (attacker enrols their own TOTP), after which R2-01 applies. The app does not force enrolment at first sign-in. Sign-up is disabled on the hosted project, so the window is the gap between admin provisioning and first enrolment. | Redirect any signed-in user with an operator row and no verified factor to `/settings#totp` until enrolled; document in the operator runbook that provisioning and first enrolment happen in one sitting; consider `ops.operators.mfa_enrolled_at` written by the worker on first aal2 sighting for the Operators screen (§20.26). |
| R2-08 | LOW | MISSING REQUIREMENT (ground rule 7: tests per work package) | `apps/worker/src/step-up/verify.spec.ts:186-195` | `verifyPasskeyRegistration` has only negative tests (garbage → `ASSERTION_INVALID`, wrong kind). The positive path — that the stored `credentialId`, COSE `publicKeyCose`, `signCount`, `transports`, `aaguid` are extracted correctly from a genuine `webauthn.create` ceremony — is untested, and it is the root of trust for every later assertion. The authentication tests are genuinely meaningful (a real ES256 signature is accepted; a different key, wrong challenge, origin, RP, type, UV flag and counter are each rejected; DER and COSE encodings in the software authenticator are correct). | Extend the software authenticator with `attestationObject` (`fmt: none`, authData with attested credential data) and assert the returned credential round-trips into a passing `verifyStepUp`. Also add: `response.rawId` mismatch alone → `CREDENTIAL_ID_MISMATCH`; stored `signCount 0` + reported `0` → accepted (SimpleWebAuthn semantics, so the limitation is documented). |
| R2-09 | LOW | MISSING REQUIREMENT (contract ambiguity) | `libs/contracts/src/policy/step-up.ts:61-63`, `apps/worker/src/step-up/verify.ts:79`, ADR-0006 §2 point 2 | The ADR files the evidence as `payload.stepUp`, but the challenge is issued before the evidence exists, so the browser hashes `payload` **without** `stepUp`. `verifyStepUp` hashes whatever `request.payload` the caller passes; nothing in contracts defines the "bound payload". A caller that passes the stored payload verbatim gets `BINDING_MISMATCH` forever (fails closed, not open). | Add `boundPayload(payload)` (omit `stepUp`, and any registration response) to `policy/step-up.ts`, use it in `stepUpBindingHash`, and test that `{...p, stepUp}` and `p` hash equal. |
| R2-10 | LOW | ENHANCEMENT (D50 coverage) | `libs/contracts/src/digest/registry.ts:252-262`, `libs/contracts/src/policy/step-up.ts:21` | The registry collects only `z.ZodType` exports; `STEP_UP_POLICY` is a plain object, so flipping `ARM_RELEASE` to `FAST` does not change the contract digest. Web (prompts) and worker (enforces) built from different commits could disagree without readiness noticing. Worker enforcement fails closed, so exposure is limited. | Register a schema that pins the table, e.g. `StepUpPolicy = z.object(Object.fromEntries(kinds.map(k => [k, z.literal(STEP_UP_POLICY[k])])))`, or include `canonicalHash(STEP_UP_POLICY)` as a registry entry. |
| R2-11 | LOW | MISSING REQUIREMENT (§23.2 strict Zod at external boundaries) | `libs/contracts/src/entities/step-up.ts:60-80, 28`; `20260906001100_step_up.sql:49` | Browser-supplied `WebAuthnAuthenticationResponse.{id,rawId,clientDataJSON,authenticatorData,signature,userHandle}` and `StepUpEvidence.credentialId` are `Base64Url` with **no length bound**; `OperatorPasskey.publicKeyCose` and DB `public_key_cose` have no maximum. A multi-megabyte `payload.stepUp` passes contract validation into `control_requests.payload` and into the worker's decode. `stepUpAssertionRef` is a free string, not `Uuid`. | Add `.max()`: `credentialId`/`id`/`rawId` ≤ 1024 (matches DB), `clientDataJSON`/`authenticatorData` ≤ 4096, `signature` ≤ 1024, `userHandle` ≤ 256, `publicKeyCose` ≤ 2048 (contract and DB check). Type `stepUpAssertionRef` as `Uuid`. Regenerate the lock. |
| R2-12 | LOW | DEFECT | `.dockerignore:23-30` | Docker matches `.env.*`, `*.pem`, `*.key`, `*.p12`, `*.keypair.json`, `id.json`, `*.log` at the context **root only**. A future `apps/web/.env.local`, `supabase/.env` or `apps/x/id.json` enters the build stage (not the runtime image, which copies only `main.js`, but it lands in the build layer and any layer cache). Today only root `.env.local` exists and is excluded. | Prefix with `**/` (`**/.env`, `**/.env.*`, `**/*.pem`, …), keep `!.env.example`. |
| R2-13 | LOW | TRADE-OFF | `.github/workflows/images.yml:62-73, 91-93` | `load: true` then `docker image push --all-tags <image>` pushes every local tag for that name. Safe on ephemeral GitHub runners (verified reasoning: `cache-from: gha` creates no local tags), unsafe if the job ever moves to a persistent/self-hosted runner. The digest check is sound: `main.ts` computes the digest from the compiled registry (`getContractSetDigest`), it does not read the copied lock file, and `set -euo pipefail` plus `test "$reported" = "$locked"` fails the job on mismatch. `permissions` are minimal for a GHCR push. | Push the explicit tags from `steps.meta.outputs.tags` (loop `docker push "$t"`), or use a second `build-push-action` step with `push: true` after verification. |
| R2-14 | LOW | DEFECT (test isolation) | `supabase/tests/01_audit_chain.test.sql:10,17,19` | Suite 01 asserts absolute counts (`count(*) = 2`) and so fails on any database that already holds audit rows (3/9 failed here). The plan claims "57 tests green from `supabase db reset`", which is accurate only for a fresh reset; the CI `database` job always starts fresh, so CI is green. Step-up suite 06 is isolated and passes. | Scope assertions to the rows the test inserts (`where actor_ref in ('test-operator','worker-1')`, compare `previous_hash` of the second inserted row to the first inserted row by id) and compare `verify_chain().checked` to a baseline captured at the start. |
| R2-15 | LOW | MISSING REQUIREMENT (evidence accuracy) | `docs/implementation/Execution_Plan_v4.md` §8 M2 line | Cited evidence files do not exist under those names: `libs/db/src/server/runtime-session-repo.spec.ts`, `processing.spec.ts`, `leases.spec.ts`. The underlying tests **do** exist: `audit.integration.spec.ts` ("persists a runtime transition and its audit row atomically") and `queue-processing.integration.spec.ts` (lease recovery, worker death). GHCR run 34063365545 could not be checked here (no `gh`). | Correct the file names in the §8 line (implementation sessions may attach evidence links). |
| R2-16 | LOW | ENHANCEMENT | `tools/check-artifacts.mjs:21,33-34` vs `eslint.config.mjs:12-29` | Comment says the lists "mirror eslint.config.mjs; change both or neither", but `PROVIDER_CLIENTS` (`lunarcrush*`, `cryptopanic*`, `@birdeye*`, `helius-sdk`, `@helius-labs/*`) exists only in the artifact scan; a source import of `helius-sdk` in `risk-authorizer` passes lint and is caught only at build. | Export both lists from one module (`tools/forbidden-packages.mjs`) imported by both. |
| R2-17 | LOW | ENHANCEMENT | `tools/check-artifacts.mjs:43,86-93` | Self-containment check misses `__require("pg-native")` (esbuild's wrapper for dynamic requires; `\brequire\(` does not match after `_`), `require(variable)`, and uppercase legacy names (`require("JSONStream")` fails `PACKAGE_SPECIFIER`, so it is skipped rather than flagged). Current bundles contain none of these (`__require()` appears once, without arguments). | Match `(?:__)?require\(`; flag any `require(` whose argument is not a string literal; drop the lowercase-only restriction (flag anything that is not a builtin/relative path). |
| R2-18 | LOW | ENHANCEMENT | `apps/web/src/proxy.ts:47` | `!path.startsWith('/api/')` exempts **every** `/api/*` route from the aal2 redirect for an `mfaPending` session. Only `/api/health` exists today and it is already in `PUBLIC_PATHS`; any future API route inherits aal1 access silently. Server actions are POSTs to page paths and are covered by the matcher, so an `mfaPending` session cannot reach `requestPauseNewEntries` (and RLS would refuse anyway). | Remove the blanket `/api/` clause; rely on `PUBLIC_PATHS`. Optionally re-check `mfaPending` in `(app)/layout.tsx` for defence in depth. |

Items examined and found sound (no finding): `ops.begin_step_up` is `security definer` with `set search_path = ''`, every reference schema-qualified, output columns do not shadow any unqualified body reference, `42501` for aal1/non-operator, `P0001 NO_PASSKEY`; randomness is `extensions.gen_random_bytes(32)` → 43-char base64url, unique-constrained; the browser role has `select` only on the three tables (pgTAP proves `insert`/`update` → `42501`); `ops.step_up_assertions` immutable by trigger; `binding_hash` is unchecked in the DB, which ADR-0006 accepts (the worker is the verifier and DB rows are not authority); `verifyStepUp` enforces user, consumed, expiry, kind, binding hash, passkey ownership, revocation, `credentialId`/`id`/`rawId` equality, `requireUserVerification: true` (both paths), exact `expectedOrigin` list and `expectedRPID`; duplicate credential ids are stopped by the DB `unique` (caller's job, as `verify.ts` documents nothing about it — say so in the handler); the Dockerfile copies only `main.js` + lock, runs as `node`, removes npm/corepack/yarn (nothing in the bundles needs them), `/journal` is chowned to `node` so the named volume inherits it; compose uses one `env_file` per service, `read_only`, `cap_drop ALL`, `no-new-privileges`, `tmpfs /tmp`, journal volume only on the executor; the web deployable holds only `NEXT_PUBLIC_*` values (`grep` for service-role/DB URL/PKCS8 in `apps/web/src` is empty), imports `@sol-agent-trader/db` for types only and no `db/server`; `getOperatorSession` derives `aal` from `getAuthenticatorAssuranceLevel()` after `getUser()` validated the token; the CI planted-marker test proves the pnpm-folder regex is live (one marker style only — see R2-03 for the extension); no §31 protected decision is changed by either commit; no Part 4 dependency constraint is crossed (`@simplewebauthn/server` is worker-only and is not even in the worker bundle yet).

## §32 checklist coverage (questions relevant to these two commits)

| §32 question | Answer |
| --- | --- |
| Can a stolen web session arm live mode alone? | **Yes, once M9 wires the handler on this substrate**: aal2 session → `REGISTER_PASSKEY` (AAL2_ONLY) → attacker passkey → `ARM_RELEASE` (R2-01). Today no UI issues these kinds. |
| Can browser code call executor directly? | No. Browser writes only `ops.control_requests` under RLS; no executor endpoint exists yet. |
| Can database tampering forge … without the projector key? / Can a stale/replayed approval authorize a different hash? | Step-up analogue: browser role cannot write passkeys/challenges/assertions (pgTAP 06); a challenge is bound to `(kind, canonicalHash(payload))`, checked by the worker; replay across requests fails on binding/kind; replay of the same assertion for an identical request within 5 min is blocked only by the caller consuming the challenge (R2-04). |
| Can secret material appear in logs/errors/traces? | Not introduced here; the TOTP secret is shown once in the browser by design. `verify.ts` returns `err.message` from SimpleWebAuthn (no key material). |
| Can a raw/local dev signer accidentally satisfy mainnet Live Readiness? | Out of scope for these commits; `parseExecutionServiceEnv` has the rule but is not wired (R2-02). |
| Can an LLM/provider dependency, wallet/signing primitive … appear inside the built `risk-authorizer` artifact despite source-level rules? | Denylisted names: no (scan verified on the real bundle, planted marker rejected). Non-listed LLM/provider clients: **yes** (R2-03); exact-name provider clients with trailing `*`: yes (R2-05); scan goes blind if path comments disappear (R2-03). |
| Can two deployables with different contract digests both become live? | Not through these images: each image computes and reports the digest at startup, CI compares to the lock and across the three images (R2-13 notes the push mechanics). `STEP_UP_POLICY` itself is outside the digest (R2-10). |
| Can the operator mistake PAPER for LIVE_AUTO anywhere in the app? | Not touched by these commits (shell treatment from `d7eb2f7`). |
| Can a critical alert disappear as a toast without acknowledgement? / mobile pause | Not in scope. |
| Can mandatory risk-reduction actions be blocked or delayed by step-up? | No: `PAUSE_NEW_ENTRIES`, `MANUAL_CLOSE`, `MANUAL_REDUCE`, `EMERGENCY_CLOSE_ALL`, `REJECT_AUTHORIZATION`, `ACKNOWLEDGE_ALERT`, `END_SESSION`, `RUN_READINESS_DRILL` are `FAST` (pinned by `step-up.spec.ts`); they still need aal2 per ADR-0006, which is a deliberate, recorded tightening. |
| Can a challenge be predicted or obtained for another user? | No: server-side `gen_random_bytes(32)`, `auth.uid()`-scoped, self-only select policy (pgTAP: stranger reads 0 rows). |
| Can a user consume or alter a challenge, or forge an assertion/passkey row? | No from the browser role (pgTAP 06, `42501`). From the worker, yes by design (service role). |
| Is the consumed flag enforced before verification and who sets it? | Checked before crypto in `checkChallenge`; **nobody sets it yet** (R2-04). |
| Sign-count semantics with stored 0 / reported 0 | Accepted by SimpleWebAuthn (counter check skipped when both are 0); standard for platform passkeys; not a defect, undocumented (R2-08). |
| Does `credentialId` comparison cover `rawId`? | Yes (`verify.ts:101`). |
| Does registration check duplicate credential ids? | No; the DB `unique (credential_id)` does, so it is the caller's job to surface `23505` as a rejection. |
| Does the web app hold a forbidden secret? | No (grep; `WebEnv` forbids them but is not invoked — R2-02). |
| Can an aal1 session with an enrolled factor reach an app page or server action? | No: proxy matcher covers all non-static paths including server-action POSTs; only `/sign-in/mfa` and `/api/*` are reachable (R2-18). RLS refuses control writes regardless. |
| Open redirect via `next`? | Yes, protocol-relative and backslash forms (R2-06). |
| Can TOTP enrolment be abused from a stolen aal1 session? | Only for a user with no verified factor (GoTrue rule verified) — bootstrap window (R2-07). Unenrol of a verified factor needs aal2 (GoTrue). |
| Contract lock / loose step-up schemas? | Missing length bounds and a free-form ref (R2-11); `strictObject` is used where the browser supplies data; base64url regex is correct (no padding). |
| Does the Dockerfile leak? | Build context excludes root secrets only (R2-12); runtime image carries `main.js` + lock only. |
| Does the compose file leak one service's env to another? | No; one `env_file` each, shared block carries no secrets. |
| Does the digest verification fail on mismatch / can it be fooled by printing the lock? | Fails (exit 1 under `set -e`); cannot be fooled — apps compute from the compiled registry, `main.ts` never reads the lock. |
| Does the planted-marker test prove the scan is live? | For the pnpm-folder marker style, yes; not for `require()` markers, self-containment, or the "comments present" precondition (R2-03). |
| Do the pgTAP tests exercise the negative cases they claim? | Yes for 06: aal1 vs aal2 differ only by the claim (same statement `throws_ok` then `lives_ok`), `42501` on browser writes, `P0001 NO_PASSKEY`, stranger isolation. Test 14 (immutability) accepts any error code; rate limit (10 open) and viewer-role refusal are untested. |
| Any §31 protected decision silently changed? | No. |
| Any Part 4 boundary crossed? | No. |
| Plan §8 M2 evidence accurate? | Three spot-checks: `apps/web/vercel.json` exists; `config/profiles/P0–P4.json` + `checkManifest` tests exist (`config.spec.ts`); 11 migrations / 6 suites / 57 tests exact. Three cited spec file names are wrong (R2-15); GHCR run id unverified. |

## Verdict

**PASS WITH REQUIRED FIXES.** The substrate is well-shaped: server-side challenges, self-scoped RPC, browser-read-only tables, a real cryptographic verifier with genuine negative tests, self-contained bundles with a build-time scan, and hardened containers. But the shipped step-up *policy* contains a hole that defeats the purpose of passkey step-up (R2-01), and two supporting controls are asserted in documentation without existing in the running code or being robust against a trivial configuration change (R2-02, R2-03).

Required before M4 (or any handler/UI) builds on this substrate:

1. **R2-01** — amend `STEP_UP_POLICY`, `ops.begin_step_up` and `verifyPasskeyRegistration` so that only the first passkey may be added with TOTP alone, with fresh-TOTP + notification + cooling period for that first registration; update ADR-0006; pin with pgTAP and worker specs.
2. **R2-04** — provide the atomic consume-and-record function and remove the FK-colliding `delete` from `begin_step_up`; document the caller contract in `verify.ts`.
3. **R2-02** — wire `parse*Env` into the three entrypoints (digest log first) with a CI negative test, or correct the README/env-example claims immediately.
4. **R2-03** — add the "path comments present / minify false" canary to `check-artifacts.mjs` and an allowlist for `risk-authorizer` and `execution-service`.

Recommended before review #1 at M3: R2-05, R2-06, R2-09, R2-11, R2-12, R2-14, R2-15. The rest are hygiene.
