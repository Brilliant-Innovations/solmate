# market

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build market` to build the library.

## Running unit tests

Run `nx test market` to execute the unit tests via [Vitest](https://vitest.dev/).
