# risk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build risk` to build the library.

## Running unit tests

Run `nx test risk` to execute the unit tests via [Vitest](https://vitest.dev/).
