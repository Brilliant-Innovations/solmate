# onchain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build onchain` to build the library.

## Running unit tests

Run `nx test onchain` to execute the unit tests via [Vitest](https://vitest.dev/).
